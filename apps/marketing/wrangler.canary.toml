# Canary Worker for marketing site — deployed first, verified, then promoted to production
name = "mattbutlerengineering-marketing-canary"
compatibility_date = "2026-03-25"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"
