/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Number of Rialto design tokens, injected at build time (see token-count.config.ts). */
declare const __RIALTO_TOKEN_COUNT__: number;

/** Number of Rialto components, injected at build time (see component-count.config.ts). */
declare const __RIALTO_COMPONENT_COUNT__: number;
