# Skills — apps/rialto-web

Showcase site for the Rialto design system.

## Available Skills

- `rialto-visual-diff-review` — When a visual regression spec fails, how to triage (intentional design change vs. regression), how to update screenshots if intentional, how to file an issue if not

## Root Skills

See root `.claude/skills/` for cross-cutting skills inherited via `package.json` acmm.inherit.

- `/implement-queue` — drain ready backlog (claim batch → parallel TDD worktree agents → PRs → serial merge train)
- `/site-audit` — crawl live site with smoke/sweep/scout modes
