---
"@mattbutlerengineering/rialto": minor
---

Toast: optional `action` button (`action?: { label, onClick }` on `ToastData`) — renders an inline action in the toast and dismisses on activation. Powers undoable flows like the drivers demo's delete Undo.
