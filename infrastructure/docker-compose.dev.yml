# Development overrides
# Usage: docker compose -f docker-compose.yml -f docker-compose.dev.yml up

services:
  postgres:
    ports:
      - "5432:5432"  # Expose postgres directly in dev

  # Users service (example - uncomment when created)
  # users:
  #   build:
  #     context: ..
  #     dockerfile: infrastructure/docker/users.Dockerfile
  #   environment:
  #     DATABASE_URL: postgresql://${POSTGRES_USER:-mbe}:${POSTGRES_PASSWORD:-mbe_dev_password}@postgres:5432/${POSTGRES_DB:-mbe}
  #     AUTH_AUTHORITY: ${AUTH_AUTHORITY}
  #     AUTH_AUDIENCE: ${AUTH_AUDIENCE}
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   labels:
  #     - "traefik.enable=true"
  #     - "traefik.http.routers.users.rule=PathPrefix(`/api/v1/users`)"
  #     - "traefik.http.services.users.loadbalancer.server.port=3001"

  # Web app (example - uncomment when created)
  # web:
  #   build:
  #     context: ..
  #     dockerfile: infrastructure/docker/web.Dockerfile
  #   labels:
  #     - "traefik.enable=true"
  #     - "traefik.http.routers.web.rule=PathPrefix(`/`)"
  #     - "traefik.http.routers.web.priority=1"
  #     - "traefik.http.services.web.loadbalancer.server.port=3000"
