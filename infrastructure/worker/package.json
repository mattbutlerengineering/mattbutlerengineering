{
  "name": "@mbe/edge-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@mbe/config": "workspace:*",
    "@vitest/coverage-v8": "catalog:",
    "vitest": "catalog:"
  }
}
