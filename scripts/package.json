{
  "name": "@mbe/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cd .. && vitest run --config scripts/vitest.config.mjs"
  },
  "dependencies": {
    "@mbe/gh-client": "workspace:*"
  },
  "devDependencies": {
    "vitest": "catalog:"
  }
}
