---
phase: 14-playground-app
plan: 03
type: execute
wave: 3
depends_on: ["14-02"]
files_modified:
  - .github/workflows/deploy-static.yml
  - infrastructure/worker/edge-router.js
  - infrastructure/pulumi/index.ts
autonomous: false
requirements:
  - PLAY-01
  - PLAY-03

user_setup:
  - service: auth0
    why: "Gen app needs its own Auth0 SPA application for login"
    env_vars:
      - name: AUTH0_GEN_CLIENT_ID
        source: "Auth0 Dashboard -> Applications -> Create Application (SPA) named 'mattbutlerengineering-gen' -> Settings -> Client ID. Add https://mattbutlerengineering.com/gen/callback to Allowed Callback URLs, https://mattbutlerengineering.com/gen to Allowed Logout URLs and Allowed Web Origins."
    dashboard_config:
      - task: "Create Auth0 SPA application 'mattbutlerengineering-gen'"
        location: "Auth0 Dashboard -> Applications -> Create Application"
      - task: "Add AUTH0_GEN_CLIENT_ID to GitHub repo secrets"
        location: "GitHub -> Settings -> Secrets and variables -> Actions"

must_haves:
  truths:
    - "deploy-static.yml deploys apps/gen to Workers Static Assets on push to main"
    - "Edge router routes /gen* to the GEN Service Binding"
    - "GEN Service Binding is uncommented in Pulumi index.ts"
    - "After wrangler deploy + pulumi up, mattbutlerengineering.com/gen serves the playground app"
  artifacts:
    - path: ".github/workflows/deploy-static.yml"
      provides: "Gen deploy job with build env vars and wrangler deploy"
      contains: "deploy-gen"
    - path: "infrastructure/worker/edge-router.js"
      provides: "/gen route using env.GEN Service Binding"
      contains: "env.GEN"
    - path: "infrastructure/pulumi/index.ts"
      provides: "Uncommented GEN Service Binding"
      contains: 'name: "GEN"'
  key_links:
    - from: "infrastructure/worker/edge-router.js"
      to: "infrastructure/pulumi/index.ts"
      via: "GEN Service Binding name must match"
      pattern: "GEN.*mattbutlerengineering-gen"
    - from: ".github/workflows/deploy-static.yml"
      to: "apps/gen/wrangler.toml"
      via: "wrangler deploy --config apps/gen/wrangler.toml"
      pattern: "wrangler.*deploy.*apps/gen"
---

<objective>
Add CI/CD deployment for apps/gen, configure edge router routing for /gen, and uncomment the GEN Service Binding in Pulumi — completing the infrastructure needed to serve the playground at mattbutlerengineering.com/gen.

Purpose: Without this plan, the app only works locally. This connects it to the production deployment pipeline.
Output: CI deploys gen app on push, edge router routes /gen traffic, checkpoint confirms end-to-end.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-playground-app/14-CONTEXT.md
@.planning/phases/14-playground-app/14-RESEARCH.md
@.planning/phases/14-playground-app/14-02-SUMMARY.md

@.github/workflows/deploy-static.yml
@infrastructure/worker/edge-router.js
@infrastructure/pulumi/index.ts
@apps/gen/wrangler.toml

<interfaces>
<!-- Existing patterns to follow exactly -->

From .github/workflows/deploy-static.yml (hospitality job pattern):
```yaml
deploy-hospitality:
  name: Deploy Hospitality
  needs: detect-changes
  if: needs.detect-changes.outputs.hospitality == 'true'
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - uses: pnpm/action-setup@v4
    - uses: actions/setup-node@v4
      with: { node-version: 22, cache: "pnpm" }
    - run: pnpm install --frozen-lockfile
    - name: Build hospitality
      run: pnpm build --filter=@mbe/hospitality
      env:
        VITE_AUTH_AUTHORITY: https://dev-ytbgmz5ls3wh4xdx.us.auth0.com
        VITE_AUTH_CLIENT_ID: ${{ secrets.AUTH0_HOSPITALITY_CLIENT_ID }}
        VITE_AUTH_AUDIENCE: https://api.mattbutlerengineering.com
        VITE_AUTH_REDIRECT_URI: https://mattbutlerengineering.com/hospitality/callback
        VITE_API_URL: https://mattbutlerengineering.com
    - name: Deploy to Workers Static Assets
      run: npx wrangler@3 deploy --config apps/hospitality/wrangler.toml
      env:
        CLOUDFLARE_API_TOKEN: ${{ secrets.MBE_CLOUDFLARE_API_TOKEN }}
        CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
```

From infrastructure/worker/edge-router.js (routing pattern):
```javascript
if (url.pathname.startsWith("/hospitality")) {
  binding = env.HOSPITALITY;
  prefix = "/hospitality";
} else if (url.pathname.startsWith("/rialto")) {
  binding = env.RIALTO;
  prefix = "/rialto";
}
```

From infrastructure/pulumi/index.ts (Service Binding pattern):
```typescript
{ name: "MARKETING", service: "mattbutlerengineering-marketing" },
{ name: "HOSPITALITY", service: "mattbutlerengineering-hospitality" },
{ name: "RIALTO", service: "mattbutlerengineering-rialto-web" },
// GEN Service Binding — uncomment when apps/gen Worker is deployed (Phase 14):
// { name: "GEN", service: "mattbutlerengineering-gen" },
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add gen deploy job to CI and update edge router + Pulumi</name>
  <files>
    .github/workflows/deploy-static.yml
    infrastructure/worker/edge-router.js
    infrastructure/pulumi/index.ts
  </files>
  <action>
Three changes, all following established patterns exactly:

**1. deploy-static.yml — Add gen app detection and deploy job:**

In `detect-changes` job:
- Add output: `gen: ${{ steps.result.outputs.gen }}`
- Add filter in `paths-filter`:
  ```yaml
  gen:
    - 'apps/gen/**'
    - 'packages/rialto/**'
    - 'packages/rialto-catalog/**'
    - 'packages/auth/**'
  ```
- Add in `Set outputs` step: `echo "gen=true" >> "$GITHUB_OUTPUT"` for workflow_dispatch, and `echo "gen=${{ steps.filter.outputs.gen }}" >> "$GITHUB_OUTPUT"` for push

In `on.push.paths`, add:
- `"apps/gen/**"`
- `"packages/rialto-catalog/**"` (if not already present — gen depends on the catalog)

Add new job `deploy-gen` (identical pattern to deploy-hospitality):
```yaml
deploy-gen:
  name: Deploy Gen
  needs: detect-changes
  if: needs.detect-changes.outputs.gen == 'true'
  runs-on: ubuntu-latest
  steps:
    - uses: actions/checkout@v4
    - uses: pnpm/action-setup@v4
    - uses: actions/setup-node@v4
      with:
        node-version: 22
        cache: "pnpm"
    - run: pnpm install --frozen-lockfile
    - name: Build gen
      run: pnpm build --filter=@mbe/gen
      env:
        VITE_AUTH_AUTHORITY: https://dev-ytbgmz5ls3wh4xdx.us.auth0.com
        VITE_AUTH_CLIENT_ID: ${{ secrets.AUTH0_GEN_CLIENT_ID }}
        VITE_AUTH_AUDIENCE: https://api.mattbutlerengineering.com
        VITE_AUTH_REDIRECT_URI: https://mattbutlerengineering.com/gen/callback
        VITE_API_URL: https://mattbutlerengineering.com
    - name: Deploy to Workers Static Assets
      run: npx wrangler@3 deploy --config apps/gen/wrangler.toml
      env:
        CLOUDFLARE_API_TOKEN: ${{ secrets.MBE_CLOUDFLARE_API_TOKEN }}
        CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
```

**2. edge-router.js — Add /gen route:**

Insert a new `else if` branch BEFORE the catch-all `else` (marketing) block:
```javascript
} else if (url.pathname.startsWith("/gen")) {
  binding = env.GEN;
  prefix = "/gen";
} else {
  binding = env.MARKETING;
}
```

Also update the JSDoc comment at the top to include `/gen/*` in the routing list.

**3. infrastructure/pulumi/index.ts — Uncomment GEN Service Binding:**

Change:
```typescript
// GEN Service Binding — uncomment when apps/gen Worker is deployed (Phase 14):
// { name: "GEN", service: "mattbutlerengineering-gen" },
```
To:
```typescript
{ name: "GEN", service: "mattbutlerengineering-gen" },
```

Remove the comment line above it.

**IMPORTANT ordering note for production deploy:** The gen Worker must exist before `pulumi up` is run with the GEN binding. The CI workflow handles this naturally — `wrangler deploy` creates the Worker, then a separate `pulumi up` (triggered by infrastructure changes) updates the edge router bindings. If deploying manually, run `wrangler deploy --config apps/gen/wrangler.toml` FIRST, then `pulumi up`.
  </action>
  <verify>
    <automated>cd /Users/mbutler/github/mattbutlerengineering && grep -q "deploy-gen" .github/workflows/deploy-static.yml && grep -q 'env.GEN' infrastructure/worker/edge-router.js && grep -q '"GEN"' infrastructure/pulumi/index.ts && echo "All three changes present"</automated>
  </verify>
  <done>
    - deploy-static.yml has deploy-gen job with correct env vars and wrangler deploy
    - edge-router.js routes /gen* to env.GEN binding
    - Pulumi index.ts has GEN Service Binding uncommented
    - Path filters include apps/gen/** and packages/rialto-catalog/**
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify playground app end-to-end</name>
  <files>apps/gen/src/main.tsx</files>
  <action>
CHECKPOINT: Human verification of the complete Phase 14 playground app.

What was built: Vite SPA at /gen with three-column layout (history, preview, JSON inspector), streaming generation via useGenStream with Auth0 token injection, prompt history, theme toggle, and CI/CD + edge router + Pulumi infrastructure.

Verification steps:

1. Local dev test:
   - Run `pnpm dev --filter=@mbe/gen` — should start on port 3005
   - Open http://localhost:3005/gen/ — should show login prompt
   - After login, should show three-column layout with prompt bar at bottom
   - Type a prompt and click Generate — should see streaming preview (requires agent service running on :3003)
   - Toggle theme — app should switch between light and dark

2. Build test:
   - Run `pnpm build --filter=@mbe/gen` — should succeed
   - Run `pnpm typecheck --filter=@mbe/gen` — should succeed

3. Pre-deploy checklist (user actions required before production works):
   - Create Auth0 SPA application "mattbutlerengineering-gen" with callback URL https://mattbutlerengineering.com/gen/callback
   - Add AUTH0_GEN_CLIENT_ID GitHub secret with the new Auth0 client ID
   - Run `wrangler deploy --config apps/gen/wrangler.toml` to create the Worker (or let CI do it on first push)
   - Run `pulumi up` from infrastructure/pulumi/ to activate the GEN Service Binding
   - Verify https://mattbutlerengineering.com/gen loads the playground
  </action>
  <verify>User confirms: type "approved" or describe issues</verify>
  <done>User has verified the playground app works locally and reviewed the pre-deploy checklist</done>
</task>

</tasks>

<verification>
1. `grep "deploy-gen" .github/workflows/deploy-static.yml` finds the job
2. `grep "env.GEN" infrastructure/worker/edge-router.js` finds the route
3. `grep '"GEN"' infrastructure/pulumi/index.ts` finds the uncommented binding (no leading //)
4. Full build: `pnpm build --filter=@mbe/gen` succeeds
</verification>

<success_criteria>
- CI will deploy gen app to Workers Static Assets on pushes to main that touch apps/gen/** or packages/rialto-catalog/**
- Edge router correctly routes /gen* requests to the GEN Worker via Service Binding
- Pulumi GEN binding is active (uncommented)
- User has clear checklist for Auth0 setup and first deploy
</success_criteria>

<output>
After completion, create `.planning/phases/14-playground-app/14-03-SUMMARY.md`
</output>
