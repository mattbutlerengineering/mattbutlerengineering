---
phase: 03-marketing-portfolio
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/marketing/package.json
  - apps/marketing/postcss.config.js
  - apps/marketing/tailwind.config.js
  - apps/marketing/src/index.css
autonomous: false
requirements:
  - PORT-07
  - PORT-08

must_haves:
  truths:
    - "No Tailwind CSS classes remain in apps/marketing/src — grep returns zero matches"
    - "No @mbe/ui imports remain in apps/marketing/src — grep returns zero matches"
    - "pnpm build succeeds for the marketing app with zero errors"
    - "pnpm typecheck succeeds for the marketing app with zero errors"
    - "pnpm lint succeeds for the marketing app"
    - "Visiting http://localhost:3000 in dev mode loads the portfolio page with correct Rialto styling"
  artifacts:
    - path: "apps/marketing/package.json"
      provides: "Clean dependency list — @mbe/rialto added, tailwindcss/postcss/autoprefixer/@mbe/ui/@mbe/auth removed"
      contains: "@mbe/rialto"
  key_links:
    - from: "apps/marketing/package.json"
      to: "@mbe/rialto"
      via: "workspace dependency"
      pattern: "\"@mbe/rialto\": \"workspace:\\*\""
---

<objective>
Remove all Tailwind CSS, PostCSS, and @mbe/ui dependencies from the marketing app and verify the build is clean.

Purpose: Complete the Rialto-only migration by eliminating deprecated dependencies and config files. This ensures the marketing app has no residual Tailwind artifacts and builds/typechecks/lints cleanly.
Output: Clean marketing app with only Rialto for styling, passing all quality checks.
</objective>

<execution_context>
@/Users/mbutler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbutler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-marketing-portfolio/03-01-SUMMARY.md
@.planning/phases/03-marketing-portfolio/03-RESEARCH.md

@apps/marketing/package.json
@apps/marketing/postcss.config.js
@apps/marketing/tailwind.config.js
@apps/marketing/vite.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Tailwind/PostCSS/@mbe/ui dependencies and config files</name>
  <files>
    apps/marketing/package.json,
    apps/marketing/postcss.config.js,
    apps/marketing/tailwind.config.js,
    apps/marketing/src/index.css
  </files>
  <action>
**1. Remove npm dependencies** from `apps/marketing/`:

```bash
cd apps/marketing
pnpm remove tailwindcss postcss autoprefixer
pnpm remove @mbe/ui @mbe/auth
pnpm add @mbe/rialto@workspace:*
```

- Remove `tailwindcss`, `postcss`, `autoprefixer` from devDependencies
- Remove `@mbe/ui` from dependencies (replaced by @mbe/rialto in Plan 01)
- Remove `@mbe/auth` from dependencies (marketing has no auth — this is a leftover)
- Add `@mbe/rialto` to dependencies as workspace link

**2. Delete config files:**
- Delete `apps/marketing/postcss.config.js`
- Delete `apps/marketing/tailwind.config.js`

**3. Delete old CSS file:**
- Delete `apps/marketing/src/index.css` (replaced by `global.css` in Plan 01, which is imported in main.tsx)
- If `index.css` was already replaced/deleted in Plan 01, confirm it no longer exists

**4. Verify package.json is clean:**
- `@mbe/rialto` should be in dependencies
- No `tailwindcss`, `postcss`, `autoprefixer` in devDependencies
- No `@mbe/ui` or `@mbe/auth` in dependencies

**5. Full codebase grep sweep** to catch any remaining Tailwind/UI artifacts:

```bash
# Tailwind utility classes (spot-check patterns)
grep -rE "className=\"[^\"]*\b(text-|bg-|flex |py-|px-|gap-|grid |container |mx-auto|font-|border-|min-h-|max-w-|items-|justify-|rounded-|shadow-)" apps/marketing/src/

# @tailwind directives
grep -r "@tailwind" apps/marketing/src/

# @mbe/ui imports
grep -r "@mbe/ui" apps/marketing/src/

# @mbe/auth imports (marketing should have none)
grep -r "@mbe/auth" apps/marketing/src/
```

All four greps must return 0 matches. If any match, fix the offending file by replacing Tailwind classes with Rialto CSS Module + token equivalents, or removing the import.

**6. Run `pnpm install`** from monorepo root to regenerate lockfile after dependency changes.
  </action>
  <verify>
    <automated>cd /Users/mbutler/github/mattbutlerengineering && pnpm install --frozen-lockfile 2>&1 | tail -5; cd apps/marketing && pnpm typecheck 2>&1 | head -20 && pnpm build 2>&1 | tail -10</automated>
  </verify>
  <done>package.json has @mbe/rialto, no tailwindcss/postcss/autoprefixer/@mbe/ui/@mbe/auth. postcss.config.js and tailwind.config.js deleted. index.css deleted. Grep returns zero Tailwind/UI matches in src/. pnpm install, typecheck, and build all succeed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification — portfolio renders correctly in dev mode</name>
  <files>apps/marketing/src</files>
  <action>
**What was built:** Complete engineering portfolio built with Rialto components. Dark theme default, Hero with fade-up animation, project cards with tilt effect, About section, contact/social footer. All Tailwind removed.

**How to verify:**
1. Start dev server: `cd apps/marketing && pnpm dev`
2. Open http://localhost:3000 in browser
3. Verify the following:
   - **Hero section**: Large "Engineering Leader" eyebrow, bold headline with gold accent word, subtitle text, two CTA buttons ("See my work" and "About me")
   - **Dark theme**: Page loads with dark background (warm charcoal, not pure black)
   - **Theme toggle**: Click the toggle button in the navbar — page switches between dark and light themes
   - **Hero CTAs**: Click "See my work" — page smooth-scrolls to Projects section. Click "About me" — scrolls to About section.
   - **Project cards**: 5 cards in a responsive grid. Cards have tilt effect on hover. 3 cards (Rialto, Hospitality, meta site) have "View live" buttons.
   - **Live links**: Click "View live" on Rialto card — navigates to /rialto. Click on Hospitality card — navigates to /hospitality. (These are cross-app links, not SPA navigation.)
   - **About section**: 3-5 sentences about builder mindset. No photo/avatar. Text only.
   - **Footer/Contact**: GitHub, LinkedIn, email links visible and clickable
   - **Responsive**: Resize browser to mobile width — cards stack to single column, hero text scales down
   - **No visual broken-ness**: No unstyled elements, no missing colors, no invisible text
4. Run `pnpm lint` from apps/marketing — should pass
5. Run `pnpm build` from apps/marketing — should succeed with dist/ output

**Resume signal:** Type "approved" if everything looks good, or describe any issues to fix.
  </action>
  <verify>
    <automated>cd apps/marketing && pnpm build 2>&1 | tail -5 && pnpm lint 2>&1 | tail -5</automated>
  </verify>
  <done>Human confirms portfolio renders correctly: Hero, Projects, About, Contact all visible with correct Rialto styling. Theme toggle works. CTAs scroll. Live links navigate. Build and lint pass.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **No Tailwind deps:** `grep -E "tailwindcss|postcss|autoprefixer" apps/marketing/package.json` returns 0 matches
2. **No @mbe/ui dep:** `grep "@mbe/ui" apps/marketing/package.json` returns 0 matches
3. **Has @mbe/rialto:** `grep "@mbe/rialto" apps/marketing/package.json` returns a match
4. **Config files gone:** `ls apps/marketing/postcss.config.js apps/marketing/tailwind.config.js 2>&1` returns "No such file"
5. **No Tailwind in source:** `grep -rE "@tailwind|className=\"(text-|bg-|flex |py-|px-|gap-)" apps/marketing/src/` returns 0 matches
6. **No @mbe/ui in source:** `grep -r "@mbe/ui" apps/marketing/src/` returns 0 matches
7. **Build passes:** `cd apps/marketing && pnpm build` exits 0
8. **Typecheck passes:** `cd apps/marketing && pnpm typecheck` exits 0
9. **Lint passes:** `cd apps/marketing && pnpm lint` exits 0
10. **Human approved:** Visual verification checkpoint passed
</verification>

<success_criteria>
- tailwindcss, postcss, autoprefixer removed from devDependencies
- @mbe/ui and @mbe/auth removed from dependencies
- @mbe/rialto added to dependencies
- postcss.config.js and tailwind.config.js deleted
- index.css deleted (replaced by global.css from Plan 01)
- Zero grep matches for Tailwind utilities, @tailwind directives, @mbe/ui imports in apps/marketing/src/
- pnpm build, typecheck, and lint all pass
- Human visually confirms portfolio renders correctly at http://localhost:3000
</success_criteria>

<output>
After completion, create `.planning/phases/03-marketing-portfolio/03-02-SUMMARY.md`
</output>
