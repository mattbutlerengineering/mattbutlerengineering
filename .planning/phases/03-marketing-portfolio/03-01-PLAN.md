---
phase: 03-marketing-portfolio
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/marketing/src/main.tsx
  - apps/marketing/src/global.css
  - apps/marketing/src/App.tsx
  - apps/marketing/src/pages/HomePage.tsx
  - apps/marketing/src/pages/HomePage.module.css
  - apps/marketing/src/components/HeroSection.tsx
  - apps/marketing/src/components/AboutSection.tsx
  - apps/marketing/src/components/ProjectsSection.tsx
  - apps/marketing/src/components/ProjectCard.tsx
  - apps/marketing/src/components/ProjectCard.module.css
  - apps/marketing/src/components/ContactSection.tsx
  - apps/marketing/src/components/Navbar.tsx
  - apps/marketing/src/components/Navbar.module.css
  - apps/marketing/src/data/projects.ts
autonomous: true
requirements:
  - PORT-01
  - PORT-02
  - PORT-03
  - PORT-04
  - PORT-05
  - PORT-06

must_haves:
  truths:
    - "Visitor sees a full-height hero with 'Engineering Leader' eyebrow, bold headline with accent span, subtitle, and two CTA buttons"
    - "Visitor sees a Projects section with 3+ project cards, each with title, tech stack tags, description, and live link buttons where applicable"
    - "Visitor sees an About section with builder mindset narrative (ownership, quality, automation, one-person dev team)"
    - "Visitor sees social/contact links (GitHub, LinkedIn, email) in a footer"
    - "The 'this site IS the project' narrative appears as one of the project cards, not a standalone section"
    - "Links to /rialto and /hospitality are plain <a> tags (cross-app), not React Router <Link>"
    - "Hero CTA buttons scroll to Projects and About sections via scrollIntoView"
  artifacts:
    - path: "apps/marketing/src/main.tsx"
      provides: "RialtoProvider wrapping BrowserRouter at root with theme state"
      contains: "RialtoProvider"
    - path: "apps/marketing/src/global.css"
      provides: "Rialto token body styles and Google Fonts import"
      contains: "var(--rialto-"
    - path: "apps/marketing/src/components/HeroSection.tsx"
      provides: "Hero composition with eyebrow, accent title, subtitle, CTA actions"
      contains: "Hero"
    - path: "apps/marketing/src/components/ProjectsSection.tsx"
      provides: "Responsive grid of ProjectCard components"
      contains: "ProjectCard"
    - path: "apps/marketing/src/components/AboutSection.tsx"
      provides: "Builder mindset narrative using Stack + Text"
      contains: "Stack"
    - path: "apps/marketing/src/components/ContactSection.tsx"
      provides: "Social links (GitHub, LinkedIn, email)"
      contains: "href"
    - path: "apps/marketing/src/data/projects.ts"
      provides: "Project data array with title, description, tags, href"
      exports: ["PROJECTS"]
  key_links:
    - from: "apps/marketing/src/main.tsx"
      to: "@mbe/rialto"
      via: "RialtoProvider import"
      pattern: "import.*RialtoProvider.*@mbe/rialto"
    - from: "apps/marketing/src/components/HeroSection.tsx"
      to: "@mbe/rialto"
      via: "Hero component import"
      pattern: "import.*Hero.*@mbe/rialto"
    - from: "apps/marketing/src/components/ProjectCard.tsx"
      to: "apps/marketing/src/data/projects.ts"
      via: "Project type import"
      pattern: "import.*Project.*projects"
    - from: "apps/marketing/src/components/ProjectsSection.tsx"
      to: "/rialto and /hospitality"
      via: "plain <a> tags for cross-app links"
      pattern: "href=\"/rialto\"|href=\"/hospitality\""
---

<objective>
Build the marketing portfolio as a single scrolling page using Rialto components exclusively.

Purpose: Transform the placeholder marketing site into a complete engineering portfolio with Hero, Projects, About, and Contact sections — all composed from Rialto primitives. This establishes the portfolio content and visual identity.
Output: Fully rendered portfolio page with all content sections, theme toggle, and responsive layout.
</objective>

<execution_context>
@/Users/mbutler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbutler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-marketing-portfolio/03-RESEARCH.md

@apps/marketing/src/main.tsx
@apps/marketing/src/App.tsx
@apps/marketing/src/index.css
@apps/marketing/src/pages/HomePage.tsx
@apps/marketing/src/components/Layout.tsx
@apps/marketing/package.json
@apps/marketing/vite.config.ts

@apps/rialto-web/src/main.tsx
@apps/rialto-web/src/global.css
@packages/rialto/src/components/Hero/Hero.tsx
@packages/rialto/src/components/Card/Card.tsx
@packages/rialto/src/components/Stack/Stack.tsx
@packages/rialto/src/components/Text/Text.tsx
@packages/rialto/src/components/Tag/Tag.tsx
@packages/rialto/src/components/Button/Button.tsx
@packages/rialto/src/components/Footer/Footer.tsx
@packages/rialto/src/components/Divider/Divider.tsx
@packages/rialto/src/providers/RialtoProvider.tsx
@packages/rialto/CLAUDE.md

<interfaces>
<!-- Key Rialto component APIs the executor needs. From RESEARCH.md verified sources. -->

Hero component (packages/rialto/src/components/Hero/Hero.tsx):
- Props: eyebrow?: string, title: ReactNode, subtitle?: string, actions?: ReactNode, minHeight?: string
- Built-in: fade-up stagger animation, gold divider, responsive typography (4xl → 3xl → 2xl)
- .title :global(.accent) applies color: var(--rialto-accent) + font-style: italic

Card component (packages/rialto/src/components/Card/Card.tsx):
- Props: variant?: "elevated" | "glass" | "flat", tilt?: boolean, className?: string, children
- Elevated variant has shadow + surface-elevated background

Stack component (packages/rialto/src/components/Stack/Stack.tsx):
- Props: direction?: "row" | "column", gap?: "xs" | "sm" | "md" | "lg" | "xl", wrap?: boolean, align?, justify?

Text component (packages/rialto/src/components/Text/Text.tsx):
- Props: variant?: "display" | "heading" | "subheading" | "body" | "caption" | "overline", color?: "primary" | "secondary" | "muted", as?: string

Tag component (packages/rialto/src/components/Tag/Tag.tsx):
- Props: children, variant?, selected?, dismissible?

Button component (packages/rialto/src/components/Button/Button.tsx):
- Props: variant?: "primary" | "secondary" | "ghost" | "danger", size?: "sm" | "md" | "lg", onClick?

Footer component (packages/rialto/src/components/Footer/Footer.tsx):
- Props: variant?: "minimal" | "rich", children
- Minimal: single row of content

RialtoProvider (packages/rialto/src/providers/RialtoProvider.tsx):
- Props: theme: "light" | "dark", children
- Sets data-theme attribute on wrapper, provides token cascade

getInitialTheme pattern (from apps/rialto-web/src/main.tsx):
- Reads localStorage "rialto-theme", falls back to prefers-color-scheme
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite app shell — main.tsx, global.css, App.tsx, Navbar</name>
  <files>
    apps/marketing/src/main.tsx,
    apps/marketing/src/global.css,
    apps/marketing/src/App.tsx,
    apps/marketing/src/components/Navbar.tsx,
    apps/marketing/src/components/Navbar.module.css
  </files>
  <action>
**1. Rewrite `apps/marketing/src/main.tsx`** — Copy the exact pattern from `apps/rialto-web/src/main.tsx`:
- Import `@mbe/rialto/styles` FIRST (before global.css)
- Import `./global.css` second
- Import `RialtoProvider` from `@mbe/rialto`
- Create `getInitialTheme()` helper (reads localStorage "rialto-theme", falls back to prefers-color-scheme, default "dark")
- Create `Root` component with `useState<"light" | "dark">(getInitialTheme)` and `useEffect` to persist to localStorage
- Wrap in `RialtoProvider theme={theme}` → `BrowserRouter` (NO basename — marketing owns root `/`)
- Pass `theme` and `onThemeToggle` props to `App`
- Render in `StrictMode`

**Default to dark mode** (per research recommendation — matches neon.com reference and Rialto's warm charcoal aesthetic).

**2. Create `apps/marketing/src/global.css`** (replaces index.css):
- Import Google Fonts DM Sans (same as rialto-web)
- `body` styles using Rialto tokens: `font-family: var(--rialto-font-sans)`, `font-size: var(--rialto-text-base)`, `background-color: var(--rialto-surface)`, `color: var(--rialto-text-primary)`
- `::selection` with `var(--rialto-accent-muted)` background
- `html { scroll-behavior: smooth; }` for CTA scroll-to-section
- Delete the old `index.css` file

**3. Rewrite `apps/marketing/src/App.tsx`**:
- Accept `theme` and `onThemeToggle` props
- Single route: `"/"` → `HomePage`
- Wrap content in a flex column min-height layout
- Include `Navbar` at top (receives theme + onThemeToggle) and Rialto `Footer` at bottom
- No `Layout` component needed — the App itself IS the layout for a single-page portfolio
- Delete old `apps/marketing/src/components/Layout.tsx`

**4. Create `apps/marketing/src/components/Navbar.tsx`** with CSS Module:
- Minimal top bar: "Matt Butler" text on left, theme toggle button on right
- Use Rialto `Button` (ghost variant, sm size) for theme toggle with sun/moon icon (simple unicode or SVG)
- CSS Module (`Navbar.module.css`): fixed/sticky position, backdrop-filter blur, token-only values (`var(--rialto-surface)` with opacity, `var(--rialto-space-md)` padding, `var(--rialto-border)` bottom border)
- Keep it lightweight — the hero is the primary visual impression

**CRITICAL rules:**
- `@mbe/rialto/styles` import MUST be first import in main.tsx
- `RialtoProvider` MUST be outside `BrowserRouter`
- NO `base` path in vite.config.ts (already correct — do not change)
- NO `basename` on `BrowserRouter` (marketing is at root)
- All Tailwind classes in these files must be replaced with Rialto token CSS Module styles or Rialto components — NO Tailwind utility strings may remain
  </action>
  <verify>
    <automated>cd apps/marketing && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>main.tsx renders RialtoProvider → BrowserRouter → App with dark theme default. global.css uses only Rialto tokens. App.tsx has single "/" route with Navbar and Footer. No Tailwind classes in any modified file. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Build all content sections — Hero, Projects, About, Contact</name>
  <files>
    apps/marketing/src/components/HeroSection.tsx,
    apps/marketing/src/components/AboutSection.tsx,
    apps/marketing/src/components/ProjectsSection.tsx,
    apps/marketing/src/components/ProjectCard.tsx,
    apps/marketing/src/components/ProjectCard.module.css,
    apps/marketing/src/components/ContactSection.tsx,
    apps/marketing/src/data/projects.ts,
    apps/marketing/src/pages/HomePage.tsx,
    apps/marketing/src/pages/HomePage.module.css
  </files>
  <action>
**1. Create `apps/marketing/src/data/projects.ts`** — Project data as a typed constant array:

```typescript
export interface Project {
  title: string;
  description: string;
  tags: string[];
  href?: string; // undefined = no live link
}
```

Define `PROJECTS` array with these cards (from CONTEXT.md decisions — "Live app links only"):
- **Rialto Design System** — Tags: React, TypeScript, Vite, Framer Motion — href: "/rialto" — Description: 2 sentences about the component library, token system, 55+ components
- **Hospitality App** — Tags: React, Auth0, Fastify, PostgreSQL — href: "/hospitality" — Description: 2 sentences about the reservation management system
- **mattbutlerengineering.com** (the meta "this IS the project" card) — Tags: Turborepo, Pulumi, Rialto, DigitalOcean — href: "/rialto" (self-referential, links to showcase) — Description: 2 sentences explaining this site IS the engineering proof — the monorepo, design system, and IaC running this page

Include Agent System and MBE CLI as cards WITHOUT href (no live link). This gives 5 cards total, exceeding the 3+ minimum from PORT-03.

**2. Create `apps/marketing/src/components/HeroSection.tsx`**:
- Use Rialto `Hero` component
- `eyebrow="Engineering Leader"`
- `title` as ReactNode with `<span className="accent">` for the accent word (MUST use literal "accent" className — Hero CSS Module uses `:global(.accent)`)
- Title text: bold, neon-style — something like "One-person team. <accent>Full ownership.</accent>" (Claude's discretion on exact wording, but must convey builder mindset)
- `subtitle`: one-liner about designing, building, shipping, and operating
- `minHeight="90vh"` for impact
- `actions`: Two buttons in a `Stack direction="row"`:
  - Primary "See my work" — `onClick` calls `document.getElementById("projects")?.scrollIntoView({ behavior: "smooth" })`
  - Ghost "About me" — `onClick` calls `document.getElementById("about")?.scrollIntoView({ behavior: "smooth" })`

**3. Create `apps/marketing/src/components/ProjectCard.tsx`** with CSS Module:
- Accept `project: Project` prop
- Use Rialto `Card variant="elevated" tilt`
- Inside: `Stack gap="md"` containing:
  - `Text variant="display" as="h3"` for title
  - `Stack direction="row" gap="xs" wrap` with `Tag` for each tech stack item
  - `Text variant="body" color="secondary"` for description
  - If `project.href` exists: an `<a href={project.href}>` wrapping a `Button variant="secondary" size="sm"` saying "View live" (use plain `<a>` tag for cross-app links — NOT React Router `<Link>`)
- CSS Module for card fixed height or min-height for visual consistency in the grid

**4. Create `apps/marketing/src/components/ProjectsSection.tsx`**:
- Section wrapper with `id="projects"` (scroll target for hero CTA)
- Section heading: `Text variant="heading" as="h2"` — "Projects"
- Optional brief intro: `Text variant="body" color="secondary"` — one-liner about what these projects demonstrate
- Responsive grid of `ProjectCard` components (CSS in HomePage.module.css)
- Import and map over `PROJECTS` from `../data/projects.ts`

**5. Create `apps/marketing/src/components/AboutSection.tsx`**:
- Section wrapper with `id="about"` (scroll target for hero CTA)
- Use `Stack gap="lg"` with `Text` components
- Heading: `Text variant="heading" as="h2"` — "About"
- 3-5 sentences focusing on builder mindset & philosophy: ownership, quality, automation, the "one-person dev team" approach (per CONTEXT.md — this is NOT a resume, it's a philosophy statement)
- Text only — NO photo or avatar (per CONTEXT.md)
- Use `Divider` between heading and body if it improves visual rhythm

**6. Create `apps/marketing/src/components/ContactSection.tsx`**:
- Section wrapper with `id="contact"`
- Social/contact links: GitHub, LinkedIn, email (mailto:)
- Use `Stack direction="row" gap="md"` with `Button variant="ghost"` or styled `<a>` tags
- Keep it minimal — this is a contact strip, not a full section

**7. Rewrite `apps/marketing/src/pages/HomePage.tsx`**:
- Import and compose: `HeroSection`, `ProjectsSection`, `AboutSection`, `ContactSection`
- Section order: Hero → Projects → About → Contact (per research recommendation — leads with work, then person)
- Each section wrapped in a container div for consistent max-width and padding
- CSS Module (`HomePage.module.css`) for section container styles and project grid layout

**8. Create `apps/marketing/src/pages/HomePage.module.css`**:
- `.section` — padding with `var(--rialto-space-4xl)` vertical, `var(--rialto-space-lg)` horizontal
- `.sectionInner` — `max-width: 1080px; margin-inline: auto;`
- `.projectGrid` — CSS Grid: `repeat(auto-fill, minmax(300px, 1fr))`, gap with `var(--rialto-space-lg)`
- Mobile breakpoint: single column at 640px
- ALL values from `--rialto-*` tokens — no hardcoded colors, spacing, or radii

**CRITICAL:**
- Cross-app links (`/rialto`, `/hospitality`) MUST use plain `<a href>` — NOT React Router `<Link>`
- `className="accent"` inside Hero title MUST be the literal string "accent" — NOT a CSS Module reference
- Every `id` attribute for scroll targets MUST match the `getElementById` calls in HeroSection
- No Tailwind utility classes anywhere (no `text-gray-600`, `py-20`, `flex`, `gap-4` etc.)
  </action>
  <verify>
    <automated>cd apps/marketing && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>HomePage renders Hero → Projects → About → Contact. 5 project cards visible (3 with live links, 2 without). Hero CTAs scroll to sections. All content uses Rialto components (Hero, Card, Stack, Text, Tag, Button, Divider). No Tailwind classes. TypeScript compiles.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **TypeScript:** `cd apps/marketing && pnpm typecheck` exits 0
2. **Rialto imports:** `grep -r "@mbe/rialto" apps/marketing/src/` returns imports in main.tsx and component files
3. **No @mbe/ui in new code:** `grep -r "@mbe/ui" apps/marketing/src/` returns 0 matches (old imports fully replaced)
4. **No Tailwind utilities in new code:** `grep -rE "className=\"(text-|bg-|flex|py-|px-|gap-|grid|container|mx-auto|font-|border-|min-h)" apps/marketing/src/` returns 0 matches
5. **Scroll targets exist:** `grep -r "id=\"projects\"\|id=\"about\"\|id=\"contact\"" apps/marketing/src/` returns matches
6. **Cross-app links correct:** `grep -r "href=\"/rialto\"\|href=\"/hospitality\"" apps/marketing/src/` returns matches
7. **No React Router Link for cross-app:** `grep -r "Link.*to=\"/rialto\"\|Link.*to=\"/hospitality\"" apps/marketing/src/` returns 0 matches
</verification>

<success_criteria>
- The marketing app renders a single scrolling portfolio page with Hero, Projects, About, and Contact sections
- RialtoProvider wraps the app at root with dark theme default and localStorage persistence
- Hero shows "Engineering Leader" eyebrow with bold accent title and scroll-to-section CTAs
- 5 project cards display in a responsive grid (Rialto, Hospitality, meta site, Agent System, MBE CLI)
- Cards with live links (Rialto, Hospitality, meta site) have "View live" buttons using plain `<a>` tags
- About section conveys builder mindset philosophy (not a resume)
- Footer/contact area shows GitHub, LinkedIn, email links
- All styling uses Rialto components and `--rialto-*` CSS tokens exclusively
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-marketing-portfolio/03-01-SUMMARY.md`
</output>
