---
task: CF Pages cutover - replace legacy DO App
status: blocked
last_updated: 2026-03-10T17:09:39.373Z
---

<current_state>
Cutting over from legacy DigitalOcean App Platform (serves all traffic) to CF Worker + CF Pages (new architecture). deploy-static.yml workflow is committed and pushed. All 3 app builds succeed in CI but wrangler pages deploy fails with Authentication error [code: 10000] on /memberships endpoint.
</current_state>

<completed_work>

- deploy-static.yml committed with workflow_dispatch + path-based change detection
- Workflow triggers correctly, all 3 apps build successfully (marketing, hospitality, rialto-web)
- Diagnosed wrangler-action incompatibility with pnpm workspaces, switched to npx wrangler@3
- Identified root cause: CLOUDFLARE_API_TOKEN GitHub secret lacks permissions for wrangler pages deploy
</completed_work>

<remaining_work>

- BLOCKED: Update CF API token permissions at https://dash.cloudflare.com/profile/api-tokens
  - Add: Account > Cloudflare Pages > Edit
  - Add: Account > Account Settings > Read
- Re-trigger workflow: `gh workflow run "Deploy Static Sites" --ref main`
- Verify CF Pages builds at *.pages.dev URLs (title should say "Hospitality" not "Dashboard", auth redirect should be production URL)
- Update Pulumi index.ts: remove _legacyApp block (lines 22-219), switch root DNS from legacy CNAME to AAAA 100::, remove legacy exports
- Run pulumi up to apply DNS cutover + legacy app deletion
- Verify mattbutlerengineering.com serves via CF Worker (all 3 apps at correct paths, auth redirects correctly)
</remaining_work>

<decisions_made>

- Remove legacy DO App entirely (no rollback option) — saves ~$7-12/mo
- Approach A: CI-first — deploy to CF Pages, verify, then switch DNS via Pulumi
- npx wrangler@3 instead of cloudflare/wrangler-action — action can't install in pnpm workspace
- workflow_dispatch added so all 3 apps can be force-deployed regardless of change detection
</decisions_made>

<blockers>
- CLOUDFLARE_API_TOKEN needs Cloudflare Pages:Edit and Account Settings:Read permissions for wrangler pages deploy to work. User must update token at CF dashboard.
</blockers>

<context>
The auth bug that started this: hospitality app on production redirects to localhost:3002 after Auth0 login. Root cause: legacy DO App serves a stale build from before the dashboard→hospitality rename. The proper fix is completing the cutover to CF Worker + CF Pages, which was already architected in Pulumi but never activated. DNS still points at legacy DO App. Once CF Pages has fresh builds and DNS switches, auth will use the correct production redirect URI.

Key files:
- .github/workflows/deploy-static.yml — CF Pages deploy workflow (committed, on main)
- infrastructure/pulumi/index.ts — needs _legacyApp removal + DNS change
- infrastructure/worker/edge-router.js — already deployed, routes traffic correctly
</context>

<next_action>
1. Update CLOUDFLARE_API_TOKEN at https://dash.cloudflare.com/profile/api-tokens (add Pages:Edit + Account Settings:Read)
2. Run: gh workflow run "Deploy Static Sites" --ref main
3. Watch: gh run watch <run-id> --exit-status
4. If green, verify *.pages.dev URLs, then proceed with Pulumi changes
</next_action>
