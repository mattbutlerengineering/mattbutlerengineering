---
task: CF Pages cutover + Rialto font/dark mode fixes
status: in-progress
last_updated: 2026-03-15T21:22:00.000Z
---

<current_state>
CF Pages cutover is COMPLETE. Now working on two Rialto improvements: font swap and dark mode fix. Both code changes are done and tests pass. Need to build, deploy, and verify.
</current_state>

<completed_work>

## CF Pages Cutover (DONE)
- deploy-static.yml workflow working — all 3 apps deploy to CF Pages
- Fixed CLOUDFLARE_ACCOUNT_ID + CLOUDFLARE_API_TOKEN GitHub secrets
- Pulumi: deleted legacy DO App, switched root DNS to AAAA 100::
- mattbutlerengineering.com now routes through CF Worker edge-router

## Rialto Font Swap (code done, not deployed)
- Replaced "Instrument Serif" with "Bricolage Grotesque" (clean warm grotesque with optical sizing)
- Files changed:
  - packages/rialto/src/tokens/typography.css — token definition
  - packages/rialto/src/styles/global.css — Google Fonts import
  - apps/rialto-web/src/global.css — Google Fonts import
  - apps/marketing/src/global.css — Google Fonts import
  - packages/rialto/CLAUDE.md — docs updated

## Dark Mode Fix (code done, not deployed)
- Root cause: RialtoProvider set data-theme on a wrapper <div>, but <body> styles in global.css use :root tokens which didn't inherit dark values
- Fix: Added useEffect in RialtoProvider.tsx to sync data-theme to document.documentElement
- Files changed:
  - packages/rialto/src/providers/RialtoProvider.tsx — added useEffect to sync theme to <html>
  - packages/rialto/src/providers/__tests__/RialtoProvider.test.tsx — added test for documentElement sync
- All 148 rialto tests pass
</completed_work>

<remaining_work>
- Run pnpm lint && pnpm typecheck across affected packages
- Commit all changes (Pulumi + Rialto font + dark mode fix)
- Push to main — deploy-static.yml will auto-deploy the 3 apps
- Verify marketing site dark mode works at mattbutlerengineering.com
- Verify font change looks good on rialto showcase and marketing site
- Check if hospitality app also has the Instrument Serif import to update
</remaining_work>

<decisions_made>
- Bricolage Grotesque chosen as display font: clean, warm, has optical sizing axis (matches DM Sans variable font usage)
- Dark mode fix at provider level (document.documentElement sync) rather than restructuring CSS selectors
</decisions_made>
