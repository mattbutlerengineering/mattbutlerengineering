---
phase: 06-accessibility-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rialto/src/components/accessibility.test.tsx
autonomous: true
requirements: [A11Y-04, A11Y-03, A11Y-07]

must_haves:
  truths:
    - "Every one of the 58 component directories has at least one axe-core assertion in accessibility.test.tsx"
    - "Portal-based components are tested in open state with document.body scan"
    - "Non-portal components are tested with container scan"
    - "All form inputs have programmatically associated labels (axe catches missing ones)"
  artifacts:
    - path: "packages/rialto/src/components/accessibility.test.tsx"
      provides: "Axe-core assertions for all 58 component directories"
      min_lines: 400
  key_links:
    - from: "packages/rialto/src/components/accessibility.test.tsx"
      to: "packages/rialto/src/components/*/index.ts"
      via: "import from each component directory"
      pattern: "import.*from.*\\./"
---

<objective>
Add axe-core test cases for the 18 missing component directories so that every Rialto component has a WCAG 2.1 AA assertion in CI. Portal-based components (9) are rendered in open state and scanned via `document.body`. Non-portal components (9) use the existing `container` scan pattern.

Purpose: A11Y-04 requires every component to have an axe assertion. Currently 39 components are tested; 18 are missing. This plan closes that gap.

Output: Updated `accessibility.test.tsx` with 18 new `it()` blocks, all passing.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/rialto/src/components/accessibility.test.tsx
@.planning/phases/06-accessibility-foundation/06-RESEARCH.md

<interfaces>
<!-- Existing test pattern from accessibility.test.tsx -->

Non-portal pattern:
```typescript
it("ComponentName", async () => {
  const { container } = render(<ComponentName prop="value" />);
  expect(await axe(container)).toHaveNoViolations();
});
```

Portal pattern (for open-state overlays):
```typescript
it("Drawer (open)", async () => {
  render(
    <Drawer open onClose={noop} title="Test Drawer">
      <p>Content</p>
    </Drawer>
  );
  expect(await axe(document.body)).toHaveNoViolations();
});
```

Disable unresolvable axe rules pattern:
```typescript
expect(
  await axe(container, {
    rules: {
      // jsdom cannot resolve CSS custom properties — covered by token-contrast.test.ts
      "color-contrast": { enabled: false },
    },
  })
).toHaveNoViolations();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Read all 18 missing component APIs and add non-portal axe tests</name>
  <files>packages/rialto/src/components/accessibility.test.tsx</files>
  <action>
First, read each of the 18 missing component files to understand their props API and whether they use portals. The missing components are:

**Non-portal (scan container):**
- AppBar, AspectRatio, DisabledTooltip, Footer, Hero, InputGroup, PageHeader, ScrollArea, Skeleton

**Portal-based (scan document.body in open state):**
- CommandPalette, Drawer, ConfirmDialog, DropdownMenu, Popover, Tooltip, ContextMenu, HoverCard, Autocomplete

For each component, read the `.tsx` file to determine:
1. The export name (default export vs named export)
2. Required props (what minimal props are needed for a valid render)
3. Whether it uses a portal (renders via createPortal or uses AnimatePresence with overlay)
4. Whether the component needs an `open` prop or equivalent to show content

Then add the 9 non-portal component tests to `accessibility.test.tsx`. Add imports at the top alongside existing imports. Add `it()` blocks inside the existing `describe("Accessibility — axe-core WCAG 2.1 AA")` block.

For each non-portal component, follow the existing pattern:
- `AppBar`: Render with minimal props. Check if it has `role="banner"` or needs `aria-label`.
- `AspectRatio`: Render with a child element (e.g., `<div>Content</div>`). Layout-only, minimal ARIA concerns.
- `DisabledTooltip`: Render with a disabled child element and reason text.
- `Footer`: Render with minimal props. Check for `role="contentinfo"`.
- `Hero`: Render with a title prop. Check heading hierarchy.
- `InputGroup`: Render with Input children. Ensure label association works for grouped inputs.
- `PageHeader`: Render with a title prop.
- `ScrollArea`: Render with scrollable child content.
- `Skeleton`: Render as-is. Should be presentation-only (`aria-hidden="true"` or no interactive content).

Disable `color-contrast` axe rule on ALL new tests (both portal and non-portal) with the comment: `// jsdom cannot resolve CSS custom properties — covered by token-contrast.test.ts`. This prevents false positives per CONTEXT.md decision.

Run the test suite to confirm the 9 non-portal tests pass.
  </action>
  <verify>
    <automated>cd packages/rialto && npx vitest run src/components/accessibility.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>9 new non-portal component axe tests added and passing. Each test renders the component with minimal valid props and asserts toHaveNoViolations with color-contrast disabled.</done>
</task>

<task type="auto">
  <name>Task 2: Add portal-based component axe tests (open state, document.body scan)</name>
  <files>packages/rialto/src/components/accessibility.test.tsx</files>
  <action>
Add the 9 portal-based component tests. For each:

1. **CommandPalette**: Render with `open={true}` and `items` array (at least 2 items with id, label, onSelect). Ensure the search input has a label or placeholder that satisfies axe. Scan `document.body`.

2. **Drawer**: Render with `open={true}`, `onClose={noop}`, `title="Test Drawer"`. Children required. Scan `document.body`.

3. **ConfirmDialog**: Read the component to determine props (likely wraps Dialog with confirm/cancel buttons). Render in open state with required props. Scan `document.body`.

4. **DropdownMenu**: Read the component API. Likely needs a trigger element and items array. Render in open state (check if there's an `open` prop or if it needs to be triggered). If no `open` prop, you may need to simulate a click on the trigger. Scan `document.body`.

5. **Popover**: Similar to DropdownMenu — check for `open` prop. Render open with content. Scan `document.body`.

6. **Tooltip**: Check if it can be rendered in a visible state. May need `open` or `defaultOpen` prop, or wrapping trigger with hover state. Scan `document.body`.

7. **ContextMenu**: Read API. May need trigger area and items. Render open. Scan `document.body`.

8. **HoverCard**: Read API. Render with content visible. Scan `document.body`.

9. **Autocomplete**: Read API. Render with options visible (open dropdown state). Scan `document.body`.

For ALL portal tests:
- Use `document.body` not `container` for axe scan
- Disable `color-contrast` rule with comment
- If a component fires warnings about missing `act()` or animation timing, wrap render in appropriate testing-library utilities
- If the component doesn't have an `open` prop, use `userEvent` or `fireEvent` to open it, then scan

Run the full accessibility test suite to confirm all 18 new tests (plus existing 39+) pass.
  </action>
  <verify>
    <automated>cd packages/rialto && npx vitest run src/components/accessibility.test.tsx 2>&1 | tail -30</automated>
  </verify>
  <done>All 18 new component axe tests added and passing. Total test count is now 57+ (39 existing + 18 new). Portal components scanned via document.body in open state. Non-portal components scanned via container. All assertions use toHaveNoViolations with color-contrast disabled.</done>
</task>

</tasks>

<verification>
- `cd packages/rialto && npx vitest run src/components/accessibility.test.tsx` — all tests pass (57+ assertions)
- `cd packages/rialto && pnpm test` — full suite passes
- Count `it(` calls in accessibility.test.tsx to confirm >= 57 test cases
</verification>

<success_criteria>
- All 58 component directories have at least one axe-core assertion
- Portal components tested in open state with document.body scan
- Non-portal components tested with container scan
- color-contrast rule disabled on all new tests with explanatory comment
- Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-accessibility-foundation/06-02-SUMMARY.md`
</output>
