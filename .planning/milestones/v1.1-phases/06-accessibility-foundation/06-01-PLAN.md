---
phase: 06-accessibility-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rialto/src/test/token-contrast.test.ts
  - packages/rialto/src/tokens/colors.css
  - packages/rialto/src/tokens/colors.json
autonomous: true
requirements: [A11Y-01, A11Y-10]

must_haves:
  truths:
    - "Programmatic token-contrast test asserts 4.5:1 for text pairs and 3:1 for UI control pairs in both light and dark themes"
    - "All token pairs pass WCAG AA contrast ratios after fixes"
    - "Visual warmth of the palette is preserved — no cold or desaturated shifts"
  artifacts:
    - path: "packages/rialto/src/test/token-contrast.test.ts"
      provides: "Programmatic WCAG AA contrast ratio assertions for all token pairs"
      min_lines: 80
    - path: "packages/rialto/src/tokens/colors.css"
      provides: "Fixed token hex values for failing light theme pairs"
      contains: "--rialto-text-tertiary"
    - path: "packages/rialto/src/tokens/colors.json"
      provides: "Updated JSON export matching colors.css fixes"
      contains: "text-tertiary"
  key_links:
    - from: "packages/rialto/src/test/token-contrast.test.ts"
      to: "packages/rialto/src/tokens/colors.json"
      via: "direct import of colors.json"
      pattern: "import colors from"
---

<objective>
Create a programmatic token-contrast Vitest test that reads colors.json and asserts WCAG AA contrast ratios for all text-on-surface and UI-control-on-surface token pairs. Then fix the 5 failing light theme token pairs by adjusting hex values in colors.css and colors.json.

Purpose: axe-core cannot resolve CSS custom property contrast values in jsdom, so this dedicated test is the ONLY reliable way to verify contrast compliance in CI. This must be done first because other plans depend on clean token values.

Output: `token-contrast.test.ts` with all assertions passing, updated `colors.css` and `colors.json` with fixed values.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@packages/rialto/src/tokens/colors.json
@packages/rialto/src/tokens/colors.css
@.planning/phases/06-accessibility-foundation/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/rialto/src/tokens/colors.json (structure):
```json
{
  "color": {
    "surface": { "default": { "$value": "#f8f6f3" }, "elevated": { "$value": "#fdfcfa" } },
    "text": { "primary": { "$value": "#1a1918" }, "secondary": { "$value": "#6b6660" }, "tertiary": { "$value": "#9e9890" }, "on-accent": { "$value": "#fdfcfa" } },
    "accent": { "default": { "$value": "#c4922a" } },
    "semantic": { "error": { "default": { "$value": "#b84a3c" } }, "success": { "default": { "$value": "#7a8a3c" } } }
  }
}
```

Dark theme tokens in colors.css use rgba opacity values:
- text-primary: rgb(253 252 250 / 0.92)
- text-secondary: rgb(253 252 250 / 0.6)
- text-tertiary: rgb(253 252 250 / 0.38)
- Dark surface: #1e1c1a, surface-elevated: #2a2725
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create token-contrast.test.ts with WCAG AA assertions</name>
  <files>packages/rialto/src/test/token-contrast.test.ts</files>
  <action>
Create `packages/rialto/src/test/token-contrast.test.ts` implementing:

1. Pure utility functions (NOT exported, internal to test file):
   - `sRGB(c: number): number` — linearize sRGB channel per WCAG 2.1 spec
   - `luminance(hex: string): number` — relative luminance from hex
   - `contrastRatio(hex1: string, hex2: string): number` — WCAG contrast ratio
   - `blendAlpha(fgHex: string, bgHex: string, alpha: number): string` — composite rgba over background to absolute hex

2. Import `colors` from `"../tokens/colors.json"` (use the exact import path — Vitest resolves JSON imports).

3. Light theme `describe` block asserting these pairs at minimum:
   - text-primary (#1a1918) on surface (#f8f6f3) — must meet 4.5:1
   - text-secondary (#6b6660) on surface (#f8f6f3) — must meet 4.5:1
   - text-tertiary on surface — must meet 4.5:1 (CURRENTLY FAILS at 2.65:1)
   - warning on surface — must meet 4.5:1 (CURRENTLY FAILS at 3.00:1)
   - success on surface — must meet 4.5:1 (CURRENTLY FAILS at 3.52:1)
   - error on surface — must meet 4.5:1
   - text-on-accent on accent — must meet 4.5:1 (CURRENTLY FAILS at 2.73:1)
   - accent on surface as UI control — must meet 3:1 (CURRENTLY FAILS at 2.60:1)
   - text-primary on surface-elevated — must meet 4.5:1
   - text-secondary on surface-elevated — must meet 4.5:1

4. Dark theme `describe` block:
   - For opacity-based tokens (text-primary, text-secondary, text-tertiary), use `blendAlpha` to compute absolute hex against BOTH `surface` (#1e1c1a) AND `surface-elevated` (#2a2725) backgrounds
   - Dark theme hex values are NOT in colors.json (they're CSS-only). Hardcode the dark theme hex values in the test file as constants at the top, with a comment: `// Dark theme values from colors.css — keep in sync manually`
   - Dark accent (#d4a23a) on dark surface — must meet 3:1
   - text-on-accent (#1a1918) on dark accent (#d4a23a) — must meet 4.5:1

5. Use `toBeGreaterThanOrEqual(4.5)` for text and `toBeGreaterThanOrEqual(3)` for UI controls.

6. Write the test so it CURRENTLY FAILS for the 5 known failing light theme pairs. Run it once to confirm RED state before Task 2 fixes them.
  </action>
  <verify>
    <automated>cd packages/rialto && npx vitest run src/test/token-contrast.test.ts 2>&1 | tail -30</automated>
  </verify>
  <done>token-contrast.test.ts exists, imports colors.json, has assertions for all light and dark theme pairs. Test runs but has expected failures for the 5 known failing pairs (RED state confirms test is detecting real issues).</done>
</task>

<task type="auto">
  <name>Task 2: Fix failing light theme token values and make tests green</name>
  <files>packages/rialto/src/tokens/colors.css, packages/rialto/src/tokens/colors.json</files>
  <action>
Fix the 5 failing light theme token pairs in BOTH `colors.css` and `colors.json` (they must stay in sync). Apply these fixes per research guidance:

1. **text-tertiary** (#9e9890 -> darken toward #7a7470 range): Darken within the warm gray family. Target ratio >= 4.5:1 on surface (#f8f6f3). Stay in the warm hue family — no blue/cool shifts. Try values like #787270 or #7a7470 and compute contrast. Pick the lightest value that passes 4.5:1 (minimal visual drift per user decision).

2. **warning** (#b8862a -> darken toward #7a5a1a range): Darken the warm amber. Must pass 4.5:1 on surface. Stay warm. Try values like #8a6820 or #7a5a1a.

3. **success** (#7a8a3c -> darken toward #5a6830 range): Darken the olive green. Must pass 4.5:1 on surface. Try values like #5e6a2e or #5a6830.

4. **text-on-accent** (#fdfcfa -> switch to dark text #1a1918): Per research, dark text on accent (#c4922a) gives 6.26:1. Change `--rialto-text-on-accent` in the light theme `:root` block to `#1a1918`. Update colors.json `color.text.on-accent.$value` to `"#1a1918"`. The dark theme already has `--rialto-text-on-accent: #1a1918` so no dark theme change needed.

5. **accent as UI control** (#c4922a -> darken slightly): The accent on surface must meet 3:1 for UI controls (focus rings, active states). Darken `--rialto-accent` slightly — try #b0841e or similar. Also update `--rialto-accent-hover` proportionally. Update the `accent-muted` and `accent-glow` rgba base values if the accent hex changes. Update `colors.json` to match.

For each fix:
- Compute the contrast ratio with the utility functions from Task 1 to verify the value passes BEFORE committing it
- Update BOTH colors.css (the `:root` block) AND colors.json (the corresponding `$value` field)
- Keep dark theme values UNCHANGED (research confirmed dark theme passes)
- Keep the warning-muted, success-muted, error-muted rgba values using the new base color if the base changes

After all fixes, run the token-contrast test to confirm all assertions pass (GREEN state).

Also run the full Rialto test suite to check for regressions — the existing 44 axe tests should still pass since they mostly disable color-contrast or don't depend on specific hex values.
  </action>
  <verify>
    <automated>cd packages/rialto && npx vitest run src/test/token-contrast.test.ts && pnpm test</automated>
  </verify>
  <done>All token-contrast assertions pass. All 5 failing light theme pairs now meet WCAG AA. colors.css and colors.json are in sync. Full Rialto test suite passes with no regressions. Warm palette character is preserved.</done>
</task>

</tasks>

<verification>
- `cd packages/rialto && npx vitest run src/test/token-contrast.test.ts` — all contrast assertions pass
- `cd packages/rialto && pnpm test` — full suite passes (no regressions from token value changes)
- Manually confirm in colors.css that no blue/cool hex values were introduced (warmth preserved)
</verification>

<success_criteria>
- token-contrast.test.ts exists with assertions for all text-on-surface and UI-control-on-surface pairs in both themes
- All 5 previously failing light theme pairs now pass WCAG AA (4.5:1 text, 3:1 UI controls)
- Dark theme pairs continue to pass (unchanged)
- colors.css and colors.json are synchronized
- Full Rialto test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/06-accessibility-foundation/06-01-SUMMARY.md`
</output>
