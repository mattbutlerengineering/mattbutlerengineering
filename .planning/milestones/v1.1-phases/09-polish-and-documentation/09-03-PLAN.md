---
phase: 09-polish-and-documentation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rialto/specs/stat.spec.md
  - packages/rialto/specs/table.spec.md
  - packages/rialto/specs/skeleton.spec.md
  - packages/rialto/specs/empty-state.spec.md
  - packages/rialto/specs/toggle.spec.md
  - packages/rialto/specs/tag.spec.md
  - packages/rialto/specs/dialog.spec.md
  - packages/rialto/specs/tabs.spec.md
  - packages/rialto/specs/badge.spec.md
  - packages/rialto/specs/tooltip.spec.md
autonomous: true
requirements:
  - AIDX-05

must_haves:
  truths:
    - "All 20 top components have structured spec files in packages/rialto/specs/"
    - "Each spec file follows the same template as plans 01-02"
    - "Complex overlay specs (Dialog, Tooltip) include thorough keyboard and screen reader documentation"
  artifacts:
    - path: "packages/rialto/specs/dialog.spec.md"
      provides: "Dialog component spec"
      contains: "## Anatomy"
    - path: "packages/rialto/specs/table.spec.md"
      provides: "Table component spec"
      contains: "## Props"
    - path: "packages/rialto/specs/tabs.spec.md"
      provides: "Tabs component spec"
      contains: "## Accessibility"
  key_links:
    - from: "packages/rialto/specs/*.spec.md"
      to: "packages/rialto/registry.json"
      via: "Props tables derived from registry data"
      pattern: "registry.json"
---

<objective>
Create structured spec files for the remaining 10 of the top 20 most-used Rialto components.

Purpose: Completes AIDX-05 by providing the second half of component specs, covering data display, feedback, navigation, and overlay components. Combined with Plan 02, this delivers all 20 spec files.

Output: 10 `.spec.md` files in `packages/rialto/specs/`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-and-documentation/09-RESEARCH.md

@packages/rialto/registry.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spec files for Stat, Table, Skeleton, EmptyState, Toggle</name>
  <files>
    packages/rialto/specs/stat.spec.md
    packages/rialto/specs/table.spec.md
    packages/rialto/specs/skeleton.spec.md
    packages/rialto/specs/empty-state.spec.md
    packages/rialto/specs/toggle.spec.md
  </files>
  <action>
Write 5 `.spec.md` files using the same template and process established in Plan 02.

For each component:
1. Read component source in `packages/rialto/src/components/{ComponentName}/` for anatomy, tokens, states
2. Read `packages/rialto/registry.json` entry for authoritative props
3. Read showcase page for accessibility section and composition patterns
4. Write spec following the established template

**Component-specific notes:**

- **Stat:** Data display. Anatomy: root div, label, value, optional trend indicator. Simple component — 40-60 lines. Tokens: text sizes, spacing.
- **Table:** Data display. Complex component. Anatomy: root table, thead with sortable headers, tbody with rows, optional empty state. Document the Column type carefully: `{ header, accessor, render?: (row: T) => ReactNode }`. Note: requires `rowKey` prop (not optional). States: loading (skeleton rows), empty (EmptyState), populated.
- **Skeleton:** Feedback. Anatomy: SkeletonGroup wrapper (semantic container with role=status) + individual Skeleton bones (aria-hidden=true, visual-only). Document the distinction clearly. States: animating (default), reduced motion (respects prefers-reduced-motion).
- **EmptyState:** Feedback. Anatomy: root div, icon slot, heading (`heading` prop, NOT `title`), description, action slot. Simple — 40-60 lines.
- **Toggle:** Form component. Anatomy: root label, hidden input, visual track, thumb. States: on/off, disabled. Note: uses `onCheckedChange` callback (not `onChange`). Announced as "switch" by screen readers.

If `packages/rialto/specs/` directory was already created by Plan 02, write directly into it. If Plan 02 has not run yet (parallel execution), create the directory.
  </action>
  <verify>
    <automated>ls /Users/mbutler/github/mattbutlerengineering/packages/rialto/specs/{stat,table,skeleton,empty-state,toggle}.spec.md 2>/dev/null | wc -l | xargs -I{} test {} -eq 5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>5 spec files exist for Stat, Table, Skeleton, EmptyState, and Toggle with correct template structure and component-accurate documentation.</done>
</task>

<task type="auto">
  <name>Task 2: Create spec files for Tag, Dialog, Tabs, Badge, Tooltip</name>
  <files>
    packages/rialto/specs/tag.spec.md
    packages/rialto/specs/dialog.spec.md
    packages/rialto/specs/tabs.spec.md
    packages/rialto/specs/badge.spec.md
    packages/rialto/specs/tooltip.spec.md
  </files>
  <action>
Write the final 5 `.spec.md` files using the same template and process.

For each component:
1. Read component source for anatomy, tokens, states
2. Read registry.json for props
3. Read showcase page for accessibility and examples
4. Write spec following template

**Component-specific notes:**

- **Tag:** Data display. Anatomy: root span, label text, optional remove button. States: default, with remove action. Simple — 40-60 lines.
- **Dialog:** Overlay. Complex component — most thorough spec. Anatomy: overlay backdrop, dialog container (role=dialog, aria-modal=true), title (aria-labelledby), description (aria-describedby), content, close button. Document focus trap behavior, focus return on close (Phase 6 triggerRef pattern), Escape key. States: open, closed. Tokens: surface, shadow, radius, backdrop opacity. 80-100 lines.
- **Tabs:** Navigation. Anatomy: root div, tab list (role=tablist), individual tabs (role=tab, aria-selected), tab panels (role=tabpanel). Document keyboard: Arrow Left/Right to navigate tabs, Enter/Space to select (or auto-activate). States: selected, unselected, disabled.
- **Badge:** Data display. Anatomy: root span with label text. Variants: neutral, success, warning, error (note: NO "info" variant — use "neutral" for neutral-status items). States: default only. Optional `dot` prop for status indicator. Simple — 40-50 lines.
- **Tooltip:** Overlay. Anatomy: trigger element (wrapped), tooltip content (role=tooltip, aria-describedby link). Document that content is announced via aria-describedby on trigger focus, NOT on hover for screen readers. No focus movement into tooltip. States: visible, hidden. Tokens: surface, shadow, radius.

After writing all 5, verify the complete set of 20 spec files exists (combined with Plan 02 output).
  </action>
  <verify>
    <automated>ls /Users/mbutler/github/mattbutlerengineering/packages/rialto/specs/{tag,dialog,tabs,badge,tooltip}.spec.md 2>/dev/null | wc -l | xargs -I{} test {} -eq 5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>All 20 spec files exist in `packages/rialto/specs/`. Tag, Dialog, Tabs, Badge, and Tooltip specs complete the set with consistent template structure. Dialog spec is the most thorough, covering focus trap, focus return, and full keyboard/screen reader documentation.</done>
</task>

</tasks>

<verification>
1. `ls packages/rialto/specs/*.spec.md | wc -l` returns 20 (combined with Plan 02)
2. Each of the 10 files in this plan contains `## Anatomy`, `## Props`, `## Accessibility`
3. Dialog spec contains focus trap, focus return, Escape key documentation
4. Badge spec does NOT list an "info" variant
5. Table spec documents `rowKey` as required
6. Toggle spec uses `onCheckedChange` (not `onChange`)
</verification>

<success_criteria>
- 10 `.spec.md` files exist for Stat, Table, Skeleton, EmptyState, Toggle, Tag, Dialog, Tabs, Badge, Tooltip
- Combined with Plan 02, all 20 top components have spec files
- Each spec follows the consistent template
- Component-specific decisions from Phase 7 are accurately reflected (Badge no "info", Table rowKey required, Toggle onCheckedChange)
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-and-documentation/09-03-SUMMARY.md`
</output>
