---
phase: 09-polish-and-documentation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rialto/specs/stack.spec.md
  - packages/rialto/specs/text.spec.md
  - packages/rialto/specs/data-list.spec.md
  - packages/rialto/specs/select.spec.md
  - packages/rialto/specs/button.spec.md
  - packages/rialto/specs/card.spec.md
  - packages/rialto/specs/checkbox.spec.md
  - packages/rialto/specs/input.spec.md
  - packages/rialto/specs/toast.spec.md
  - packages/rialto/specs/divider.spec.md
autonomous: true
requirements:
  - AIDX-05

must_haves:
  truths:
    - "Structured spec files exist for the first 10 of the top 20 most-used components"
    - "Each spec file follows a consistent template with anatomy, tokens, props, states, accessibility, and composition examples"
    - "Props tables reference registry.json as authoritative source"
  artifacts:
    - path: "packages/rialto/specs/button.spec.md"
      provides: "Button component spec"
      contains: "## Anatomy"
    - path: "packages/rialto/specs/select.spec.md"
      provides: "Select component spec"
      contains: "## Props"
    - path: "packages/rialto/specs/toast.spec.md"
      provides: "Toast component spec"
      contains: "## Accessibility"
  key_links:
    - from: "packages/rialto/specs/*.spec.md"
      to: "packages/rialto/registry.json"
      via: "Props tables derived from registry data"
      pattern: "registry.json"
---

<objective>
Create structured spec files for the first 10 of the top 20 most-used Rialto components.

Purpose: Fulfills AIDX-05 (first half) by providing machine-readable and human-readable component documentation with anatomy, design tokens, props, states, and composition examples. These serve both AI consumers and developers as authoritative component references.

Output: 10 `.spec.md` files in `packages/rialto/specs/`
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-polish-and-documentation/09-RESEARCH.md

@packages/rialto/registry.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create specs directory and first 5 spec files (Stack, Text, DataList, Select, Button)</name>
  <files>
    packages/rialto/specs/stack.spec.md
    packages/rialto/specs/text.spec.md
    packages/rialto/specs/data-list.spec.md
    packages/rialto/specs/select.spec.md
    packages/rialto/specs/button.spec.md
  </files>
  <action>
Create `packages/rialto/specs/` directory. Write 5 `.spec.md` files using the template defined in the research document.

For each component:
1. Read the component source in `packages/rialto/src/components/{ComponentName}/` to extract anatomy (sub-elements, DOM structure), design tokens used (grep for `--rialto-` in the CSS module), and states
2. Read the component entry in `packages/rialto/registry.json` to extract props (use these as authoritative — do NOT manually copy from TypeScript source)
3. Read the showcase page `apps/rialto-web/src/pages/` for the accessibility section content and composition examples
4. Write the spec file following this exact template structure:

```markdown
# [ComponentName]

**Import:** `import { ComponentName } from "@mbe/rialto"`
**Category:** [Form / Data Display / Overlay / Layout / Navigation / Feedback]

## Anatomy

```
[ComponentName]
+-- [sub-element] -- [description]
+-- [sub-element] -- [description]
```

## When to Use

- [Use case 1]
- [Use case 2]

## States

| State | Description | Prop/Trigger |
|-------|-------------|-------------|
| ... | ... | ... |

## Design Tokens Used

| Token | Purpose |
|-------|---------|
| `--rialto-*` | [what it controls] |

## Props

> See `registry.json` for authoritative prop types.

| Prop | Type | Default | Required | Description |
|------|------|---------|----------|-------------|
| ... | ... | ... | ... | ... |

## Accessibility

| Attribute | Value | Notes |
|-----------|-------|-------|
| ... | ... | ... |

**Keyboard:** [key behaviors]
**Screen reader:** [runtime announcement behavior]

## Composition Examples

```tsx
// [Short label]
<ComponentName ... />
```
```

**Component-specific notes:**

- **Stack:** Layout primitive. Anatomy is simple (root div). States: N/A (no interactive states). Tokens: spacing tokens only. Keep spec short — this is presentational.
- **Text:** Layout primitive. Anatomy is simple (root element via `as` prop). Tokens: font size, weight, color tokens. Keep spec short.
- **DataList:** Data display. Anatomy: root dl, items with dt/dd pairs. Document the `items` array prop shape carefully.
- **Select:** Form component. Complex anatomy: trigger button, listbox popover, option items. Document keyboard navigation thoroughly. States: default, open, disabled, error (note: no `error` prop — state shown via surrounding context).
- **Button:** Form component. Anatomy: motion.button with children. Document all 3 variants and 3 sizes. States: default, hover, focus, pressed, disabled.

Each file should be 60-100 lines. Do NOT over-document layout primitives (Stack, Text) — 40-60 lines is fine for those.
  </action>
  <verify>
    <automated>ls /Users/mbutler/github/mattbutlerengineering/packages/rialto/specs/{stack,text,data-list,select,button}.spec.md 2>/dev/null | wc -l | xargs -I{} test {} -eq 5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>5 spec files exist in `packages/rialto/specs/` for Stack, Text, DataList, Select, and Button. Each follows the template with anatomy, tokens, props, states, accessibility, and composition examples.</done>
</task>

<task type="auto">
  <name>Task 2: Create next 5 spec files (Card, Checkbox, Input, Toast, Divider)</name>
  <files>
    packages/rialto/specs/card.spec.md
    packages/rialto/specs/checkbox.spec.md
    packages/rialto/specs/input.spec.md
    packages/rialto/specs/toast.spec.md
    packages/rialto/specs/divider.spec.md
  </files>
  <action>
Write 5 more `.spec.md` files using the same template and process as Task 1.

For each component:
1. Read component source for anatomy, tokens, states
2. Read registry.json entry for props
3. Read showcase page for accessibility and examples
4. Write spec following the template

**Component-specific notes:**

- **Card:** Data display. Anatomy: root div with optional title, subtitle, children, footer slots. Tokens: surface, shadow, radius, padding. States: default only (no interactive states unless wrapped in a link).
- **Checkbox:** Form component. Anatomy: root label, hidden input, visual box, checkmark icon, label text. Document checked/unchecked/indeterminate states. Include radio variant notes if CheckboxRadio is a combined component.
- **Input:** Form component. Anatomy: wrapper div, label, input element, hint text, error text. States: default, focused, disabled, error, readOnly. Tokens: border, surface, text colors.
- **Toast:** Feedback component. Complex anatomy: ToastProvider (portal), toast container, individual toast with title/description/action/dismiss. Document useToast() hook usage. States: appearing, visible, dismissing. Note: requires ToastProvider ancestor.
- **Divider:** Layout primitive. Very simple anatomy (root hr or div). Tokens: border color only. Keep spec minimal — 30-40 lines.

Maintain consistent quality and depth matching Task 1 output. Presentational components (Card, Divider) should be shorter; interactive components (Checkbox, Input, Toast) should be thorough.
  </action>
  <verify>
    <automated>ls /Users/mbutler/github/mattbutlerengineering/packages/rialto/specs/{card,checkbox,input,toast,divider}.spec.md 2>/dev/null | wc -l | xargs -I{} test {} -eq 5 && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>10 total spec files exist in `packages/rialto/specs/`. Card, Checkbox, Input, Toast, and Divider specs follow the template consistently with appropriate depth per component complexity.</done>
</task>

</tasks>

<verification>
1. `ls packages/rialto/specs/*.spec.md | wc -l` returns 10
2. Each file contains `## Anatomy`, `## Props`, `## Accessibility` sections
3. Each file references `registry.json` in the Props section
4. Layout primitives (Stack, Text, Divider) are 30-60 lines; interactive components are 60-100 lines
</verification>

<success_criteria>
- 10 `.spec.md` files exist in `packages/rialto/specs/` for Stack, Text, DataList, Select, Button, Card, Checkbox, Input, Toast, Divider
- Each file follows the consistent template from the research document
- Props tables note registry.json as authoritative source
</success_criteria>

<output>
After completion, create `.planning/phases/09-polish-and-documentation/09-02-SUMMARY.md`
</output>
