---
phase: 11-registry-props-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/rialto/src/components/Alert/Alert.tsx
  - packages/rialto/src/components/Breadcrumb/Breadcrumb.tsx
  - packages/rialto/src/components/Checkbox/Checkbox.tsx
  - packages/rialto/src/components/CommandPalette/CommandPalette.tsx
  - packages/rialto/src/components/DisabledTooltip/DisabledTooltip.tsx
  - packages/rialto/src/components/Divider/Divider.tsx
  - packages/rialto/src/components/Drawer/Drawer.tsx
  - packages/rialto/src/components/DropdownMenu/DropdownMenu.tsx
  - packages/rialto/src/components/Kbd/Kbd.tsx
  - packages/rialto/src/components/NumberInput/NumberInput.tsx
  - packages/rialto/src/components/Pagination/Pagination.tsx
  - packages/rialto/src/components/Popover/Popover.tsx
  - packages/rialto/src/components/Skeleton/Skeleton.tsx
  - packages/rialto/src/components/Steps/Steps.tsx
  - packages/rialto/src/components/Tag/Tag.tsx
  - packages/rialto/src/components/TextArea/TextArea.tsx
  - packages/rialto/src/components/Timeline/Timeline.tsx
  - packages/rialto/src/components/Table/Table.tsx
  - packages/rialto/registry.json
autonomous: true
requirements: [AIDX-01]

must_haves:
  truths:
    - "High-use components (Drawer, Checkbox, Table, TextArea, CommandPalette, DropdownMenu, Popover, Tag, Steps, Pagination, NumberInput, Alert, Breadcrumb, Skeleton, Timeline, Divider, Kbd, DisabledTooltip) have non-empty props arrays in registry.json"
    - "Table has non-empty props array despite being generic (via non-generic export alias)"
    - "CI drift check passes after regeneration (pnpm build:registry + git diff --exit-code)"
    - "Existing tests still pass (no regressions from adding export keyword)"
  artifacts:
    - path: "packages/rialto/registry.json"
      provides: "Updated registry with props populated for 23+ previously-empty components"
      contains: "props"
  key_links:
    - from: "packages/rialto/src/components/*/ComponentName.tsx"
      to: "packages/rialto/registry.json"
      via: "generate-registry.ts uses checker.getExportsOfModule to find exported Props interfaces"
      pattern: "export interface \\w+Props"
---

<objective>
Export Props interfaces from 17 component source files (23 components) and add a non-generic TableProps export alias, then regenerate registry.json so high-use components have populated props arrays.

Purpose: Fix the root cause of 49/90 empty props in registry.json -- unexported Props interfaces are invisible to the TypeScript Compiler API's getExportsOfModule lookup in generate-registry.ts. This makes AI tools receive structured prop metadata for all primary components.

Output: Updated registry.json with ~23 fewer empty-props components. Table included via non-generic alias.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-registry-props-verification/11-RESEARCH.md
@packages/rialto/scripts/generate-registry.ts
@packages/rialto/src/components/Button/Button.tsx (reference: correctly exported Props interface pattern)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Export Props interfaces in 17 component files + Table generic fix</name>
  <files>
    packages/rialto/src/components/Alert/Alert.tsx
    packages/rialto/src/components/Breadcrumb/Breadcrumb.tsx
    packages/rialto/src/components/Checkbox/Checkbox.tsx
    packages/rialto/src/components/CommandPalette/CommandPalette.tsx
    packages/rialto/src/components/DisabledTooltip/DisabledTooltip.tsx
    packages/rialto/src/components/Divider/Divider.tsx
    packages/rialto/src/components/Drawer/Drawer.tsx
    packages/rialto/src/components/DropdownMenu/DropdownMenu.tsx
    packages/rialto/src/components/Kbd/Kbd.tsx
    packages/rialto/src/components/NumberInput/NumberInput.tsx
    packages/rialto/src/components/Pagination/Pagination.tsx
    packages/rialto/src/components/Popover/Popover.tsx
    packages/rialto/src/components/Skeleton/Skeleton.tsx
    packages/rialto/src/components/Steps/Steps.tsx
    packages/rialto/src/components/Tag/Tag.tsx
    packages/rialto/src/components/TextArea/TextArea.tsx
    packages/rialto/src/components/Timeline/Timeline.tsx
    packages/rialto/src/components/Table/Table.tsx
  </files>
  <action>
    For each of the 17 component files listed, add the `export` keyword to their Props interface declarations. The change is minimal: `interface FooProps` becomes `export interface FooProps`.

    Files with multiple interfaces to export:
    - Checkbox.tsx: export CheckboxProps, RadioProps, RadioGroupProps
    - Kbd.tsx: export KbdProps, ShortcutProps
    - Skeleton.tsx: export SkeletonProps, SkeletonGroupProps
    - Tag.tsx: export TagProps, TagGroupProps, AnimatedTagProps (TagProps must be exported first since AnimatedTagProps extends it -- ordering matters for the compiler)

    Files with single interface to export:
    - Alert.tsx: AlertProps
    - Breadcrumb.tsx: BreadcrumbProps
    - CommandPalette.tsx: CommandPaletteProps
    - DisabledTooltip.tsx: DisabledTooltipProps
    - Divider.tsx: DividerProps
    - Drawer.tsx: DrawerProps
    - DropdownMenu.tsx: DropdownMenuProps
    - NumberInput.tsx: NumberInputProps
    - Pagination.tsx: PaginationProps
    - Popover.tsx: PopoverProps
    - Steps.tsx: StepsProps
    - TextArea.tsx: TextAreaProps
    - Timeline.tsx: TimelineProps

    For Table.tsx specifically (generic type parameter prevents auto-extraction):
    1. Rename the existing `interface TableProps<T>` to `interface TablePropsGeneric<T>`
    2. Update the component function signature to use `TablePropsGeneric<T>` instead
    3. Add a new non-generic exported alias AFTER the generic interface:
       ```typescript
       /** Registry documentation type -- concrete version of TableProps for AI tooling */
       export interface TableProps {
         columns: Column<unknown>[];
         data: unknown[];
         rowKey: (row: unknown) => string | number;
         density?: "compact" | "default" | "spacious";
         striped?: boolean;
         emptyMessage?: string;
         className?: string;
       }
       ```
    4. The component itself continues to use `TablePropsGeneric<T>` for full type safety -- the exported `TableProps` is only for registry/AI consumption.

    Reference pattern: packages/rialto/src/components/Button/Button.tsx (`export interface ButtonProps extends Pick<...>`)

    This is a purely additive change -- adding `export` to an existing interface does not change runtime behavior, only TypeScript module exports.
  </action>
  <verify>
    <automated>cd packages/rialto && pnpm typecheck && pnpm test</automated>
  </verify>
  <done>All 17 files have exported Props interfaces. Table has non-generic TableProps export. Typecheck passes. Tests pass with no regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Regenerate registry.json and verify props populated</name>
  <files>packages/rialto/registry.json</files>
  <action>
    Run `pnpm build:registry` from packages/rialto to regenerate registry.json using the now-exported Props interfaces.

    After regeneration, verify:
    1. High-use components now have non-empty props arrays: Drawer, Checkbox, Table, TextArea, CommandPalette, DropdownMenu, Popover, Tag, Steps, Pagination, NumberInput, Alert, Breadcrumb, Skeleton, Timeline, Divider, Kbd, DisabledTooltip
    2. TextArea has approximately 10-18 props (NOT 200+ -- Pick<> should resolve to only picked fields)
    3. AnimatedTag inherits TagProps fields correctly
    4. Table has props from the non-generic TableProps alias (7 props: columns, data, rowKey, density, striped, emptyMessage, className)
    5. Total empty-props count drops from 49 to approximately 26 (only Category 3 sub-component types remain empty)

    If any high-use component still shows empty props, debug by checking if the barrel re-export chain is intact: ComponentName.tsx -> index.ts -> components/index.ts -> src/index.ts.

    Stage and commit the updated registry.json.
  </action>
  <verify>
    <automated>cd packages/rialto && pnpm build:registry && node -e 'const r=JSON.parse(require("fs").readFileSync("./registry.json","utf8")); const targets=["Drawer","Checkbox","Table","TextArea","CommandPalette","DropdownMenu","Popover","Tag","Steps","Pagination","NumberInput","Alert","Breadcrumb","Skeleton","Timeline","Divider","Kbd","DisabledTooltip"]; const missing=targets.filter(t=>{const c=r.components.find(x=>x.name===t);return !c||c.props.length===0;}); if(missing.length){console.error("Still empty:",missing.join(", "));process.exit(1);}console.log("All target components have props");'</automated>
  </verify>
  <done>registry.json committed with non-empty props arrays for all 23 previously-empty Category 1 components plus Table. Empty-props count is approximately 26 (only sub-component types). CI drift check will pass since committed file matches generated output.</done>
</task>

</tasks>

<verification>
1. `cd packages/rialto && pnpm typecheck` passes
2. `cd packages/rialto && pnpm test` passes with no regressions
3. `cd packages/rialto && pnpm build:registry && git diff --exit-code registry.json` shows no drift (committed matches generated)
4. Empty-props count in registry.json is ~26 (down from 49)
5. High-use components (Drawer, Checkbox, Table, TextArea, etc.) all have non-empty props arrays
</verification>

<success_criteria>
- All 17 component files export their Props interfaces
- Table has a non-generic exported TableProps alias
- registry.json regenerated and committed with ~23 fewer empty-props components
- All tests and typecheck pass
- CI drift check passes
</success_criteria>

<output>
After completion, create `.planning/phases/11-registry-props-verification/11-01-SUMMARY.md`
</output>
