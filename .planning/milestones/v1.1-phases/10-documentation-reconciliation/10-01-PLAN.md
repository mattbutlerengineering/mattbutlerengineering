---
phase: 10-documentation-reconciliation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/06-accessibility-foundation/06-02-SUMMARY.md
  - .planning/phases/06-accessibility-foundation/06-03-SUMMARY.md
  - .planning/phases/08-ai-developer-experience/08-02-SUMMARY.md
  - llms-full.txt
  - llms.txt
  - CLAUDE.md
autonomous: true
requirements: [A11Y-03, A11Y-04, A11Y-05, A11Y-07, A11Y-08, AIDX-02, AIDX-03]

must_haves:
  truths:
    - "Phase 06 SUMMARY files (06-02, 06-03) list all contributed requirements in requirements-completed frontmatter"
    - "08-02-SUMMARY lists AIDX-02 and AIDX-03 in requirements-completed frontmatter"
    - "llms-full.txt documents correct prop names for Drawer (onClose), ConfirmDialog (onConfirm/onCancel), Popover (trigger/children), and Dialog (onClose)"
    - "llms.txt overlay table is consistent with corrected llms-full.txt"
    - "CLAUDE.md Rialto section is verified accurate against current component source APIs"
  artifacts:
    - path: ".planning/phases/06-accessibility-foundation/06-02-SUMMARY.md"
      provides: "requirements-completed frontmatter for A11Y-03, A11Y-04, A11Y-07"
      contains: "requirements-completed"
    - path: ".planning/phases/06-accessibility-foundation/06-03-SUMMARY.md"
      provides: "requirements-completed frontmatter for A11Y-05, A11Y-08"
      contains: "requirements-completed"
    - path: ".planning/phases/08-ai-developer-experience/08-02-SUMMARY.md"
      provides: "requirements-completed frontmatter for AIDX-02, AIDX-03"
      contains: "requirements-completed"
    - path: "llms-full.txt"
      provides: "Correct overlay component prop names"
      contains: "onClose"
    - path: "llms.txt"
      provides: "Correct overlay component prop names (lean version)"
      contains: "onClose"
  key_links:
    - from: "llms-full.txt"
      to: "llms.txt"
      via: "Overlay table prop names must be identical"
      pattern: "Drawer.*onClose"
    - from: "CLAUDE.md"
      to: "packages/rialto/src/components/*/index.ts"
      via: "Top 10 component API table must match source props"
      pattern: "Dialog.*onClose"
---

<objective>
Close all documentation-only gaps for Phase 10: add missing requirements-completed frontmatter to three SUMMARY files, correct stale overlay prop names in both llms.txt files, and verify CLAUDE.md Rialto section accuracy.

Purpose: All seven Phase 10 requirements (A11Y-03, A11Y-04, A11Y-05, A11Y-07, A11Y-08, AIDX-02, AIDX-03) were implemented in earlier phases but not properly registered in documentation. This plan closes the documentation trail.
Output: Updated SUMMARY frontmatter, corrected llms-full.txt and llms.txt, verified CLAUDE.md
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-documentation-reconciliation/10-RESEARCH.md
@.planning/phases/06-accessibility-foundation/06-02-SUMMARY.md
@.planning/phases/06-accessibility-foundation/06-03-SUMMARY.md
@.planning/phases/08-ai-developer-experience/08-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add requirements-completed frontmatter to SUMMARY files</name>
  <files>
    .planning/phases/06-accessibility-foundation/06-02-SUMMARY.md
    .planning/phases/06-accessibility-foundation/06-03-SUMMARY.md
    .planning/phases/08-ai-developer-experience/08-02-SUMMARY.md
  </files>
  <action>
Add `requirements-completed` YAML frontmatter field to three SUMMARY files. Insert the field inside the existing `---` frontmatter block, after the last existing key-value pair (before the closing `---`).

**06-02-SUMMARY.md:** Add `requirements-completed: [A11Y-03, A11Y-04, A11Y-07]`
- A11Y-03: ARIA roles/labels — CommandPalette aria-labels fixed, cloneElement injection for DropdownMenu/Popover
- A11Y-04: 63 toHaveNoViolations assertions covering all 58 component directories
- A11Y-07: axe-core catches label violations; CommandPalette search input got aria-label="Search commands"

**06-03-SUMMARY.md:** Add `requirements-completed: [A11Y-05, A11Y-08]`
- A11Y-05: Drawer + CommandPalette Tab-wrap focus traps prevent keyboard traps
- A11Y-08: triggerRef pattern in all 7 overlay components returns focus on close

**08-02-SUMMARY.md:** Add `requirements-completed: [AIDX-02, AIDX-03]`
- AIDX-02: Two-tier llms.txt created (llms.txt 11KB + llms-full.txt 26KB)
- AIDX-03: CLAUDE.md Rialto usage section added

Follow exact pattern from 06-01-SUMMARY.md: `requirements-completed: [ID1, ID2]` (no quotes around IDs).
  </action>
  <verify>
    <automated>grep -l "requirements-completed" .planning/phases/06-accessibility-foundation/06-02-SUMMARY.md .planning/phases/06-accessibility-foundation/06-03-SUMMARY.md .planning/phases/08-ai-developer-experience/08-02-SUMMARY.md | wc -l | grep -q 3 && echo "PASS: All 3 SUMMARY files have requirements-completed" || echo "FAIL"</automated>
  </verify>
  <done>All three SUMMARY files contain requirements-completed frontmatter with the correct requirement IDs: 06-02 has [A11Y-03, A11Y-04, A11Y-07], 06-03 has [A11Y-05, A11Y-08], 08-02 has [AIDX-02, AIDX-03]</done>
</task>

<task type="auto">
  <name>Task 2: Correct stale overlay prop names in llms-full.txt and llms.txt</name>
  <files>
    llms-full.txt
    llms.txt
  </files>
  <action>
Fix stale prop names in both llms files. The research has confirmed exact corrections needed from reading component source files directly.

**llms-full.txt corrections:**

1. **Overlays table** (around lines 86-98): Update these rows:
   - Dialog: change `onOpenChange` to `onClose` (Dialog row may show stale `onOpenChange`)
   - Drawer: change `open`, `onOpenChange`, `placement` to `open`, `onClose`, `title`, `side`, `size`
   - ConfirmDialog: change `open`, `onOpenChange`, `title`, `variant` to `open`, `onConfirm`, `onCancel`, `title`, `variant`
   - Popover: change `open`, `onOpenChange`, `content`, `placement` to `trigger`, `children`, `placement`, `title`

2. **"Settings panel" composition example** (around lines 386-401): Fix Drawer usage:
   - Change `onOpenChange={setOpen}` to `onClose={() => setOpen(false)}`
   - Change `placement="right"` to `side="right"`

3. **"Confirmation flow" composition example** (around lines 407-414): Fix ConfirmDialog usage:
   - Change `onOpenChange={setShowConfirm}` to `onConfirm={handleConfirm}` and add `onCancel={() => setShowConfirm(false)}`
   - Change `variant="danger"` to `variant="destructive"`

**llms.txt corrections** (overlay table, around lines 104-111):
   - Drawer: change props to `open`, `onClose`, `title`, `side`, `size`
   - ConfirmDialog: change props to `open`, `onConfirm`, `onCancel`, `title`, `variant`
   - Popover: change props to `trigger`, `children`, `placement`, `title`

IMPORTANT: Both files must be consistent — fix the same components in both files. Read each file first to locate exact line numbers before editing.
  </action>
  <verify>
    <automated>grep -c "onOpenChange" llms-full.txt llms.txt | grep -v ":0" | wc -l | grep -q 0 && echo "PASS: No stale onOpenChange in either file" || echo "FAIL: onOpenChange still present"</automated>
  </verify>
  <done>Both llms-full.txt and llms.txt have correct prop names: Drawer uses onClose/side, ConfirmDialog uses onConfirm/onCancel/variant="destructive", Popover uses trigger/children/placement, Dialog uses onClose. Zero occurrences of "onOpenChange" remain in either file.</done>
</task>

<task type="auto">
  <name>Task 3: Verify CLAUDE.md Rialto section against component source</name>
  <files>
    CLAUDE.md
  </files>
  <action>
Formally verify the CLAUDE.md "Rialto Design System Usage" section against actual component source files. Read the following source files to confirm props:

1. Read `packages/rialto/src/components/Toggle/Toggle.tsx` — verify Toggle props match CLAUDE.md table (`label`, `checked`, `onChange`, `disabled`)
2. Read `packages/rialto/src/components/Select/Select.tsx` — verify Select props match CLAUDE.md table (`label`, `options`, `placeholder`, `value`, `onChange`)
3. Read `packages/rialto/src/components/Dialog/Dialog.tsx` — verify Dialog props match CLAUDE.md table (`open`, `onClose`, `title`, `children`)
4. Read `packages/rialto/src/components/Toast/Toast.tsx` or `useToast` hook — verify Toast usage matches CLAUDE.md (`useToast()` hook pattern)

For each component, compare the CLAUDE.md "Top 10 Component APIs" table row against the actual TypeScript interface in the source file. If any prop name or description is wrong, correct it.

Also verify:
- Import path `from "@mbe/rialto"` is correct (barrel export)
- `import "@mbe/rialto/styles"` is correct
- RialtoProvider `theme` prop values are correct
- Stack `justify="between"` is documented (per STATE.md decision)

If CLAUDE.md is already accurate (likely per audit), no changes needed — just confirm in the SUMMARY that verification was performed. If any discrepancies found, fix them.
  </action>
  <verify>
    <automated>grep -q 'onClose' CLAUDE.md && grep -q '"between"' CLAUDE.md && grep -q '@mbe/rialto' CLAUDE.md && echo "PASS: CLAUDE.md contains key verified patterns" || echo "FAIL: Missing expected patterns"</automated>
  </verify>
  <done>CLAUDE.md Rialto section verified accurate against component source: all 10 component API entries match their TypeScript interfaces, import paths are correct, RialtoProvider setup is correct, Stack justify="between" is documented</done>
</task>

</tasks>

<verification>
1. All three SUMMARY files have `requirements-completed` frontmatter with correct IDs
2. `grep "onOpenChange" llms-full.txt llms.txt` returns zero matches
3. `grep "variant=\"danger\"" llms-full.txt` returns zero matches
4. CLAUDE.md top-10 table prop names match component TypeScript interfaces
5. llms.txt and llms-full.txt overlay tables are consistent with each other
</verification>

<success_criteria>
- Phase 06 SUMMARY files (06-02, 06-03) list all contributed requirements in requirements-completed frontmatter
- 08-02-SUMMARY lists AIDX-02 and AIDX-03 in requirements-completed frontmatter
- llms-full.txt and llms.txt have zero occurrences of "onOpenChange" and correct overlay prop names
- CLAUDE.md Rialto section formally verified against source (corrections applied if needed)
- All 7 requirement IDs (A11Y-03, A11Y-04, A11Y-05, A11Y-07, A11Y-08, AIDX-02, AIDX-03) have documentation trail
</success_criteria>

<output>
After completion, create `.planning/phases/10-documentation-reconciliation/10-01-SUMMARY.md`
</output>
