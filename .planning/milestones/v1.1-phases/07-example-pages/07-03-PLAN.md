---
phase: 07-example-pages
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/rialto-web/src/pages/examples/SettingsExamplePage.tsx
  - apps/rialto-web/src/pages/examples/SettingsExamplePage.module.css
  - apps/rialto-web/src/pages/examples/FormStatesExamplePage.tsx
  - apps/rialto-web/src/pages/examples/FormStatesExamplePage.module.css
autonomous: true
requirements:
  - EXMP-02
  - EXMP-03
  - EXMP-04
  - EXMP-05

must_haves:
  truths:
    - "Navigating to /rialto/examples/settings shows a full settings layout with sectioned groups for profile, notifications, and preferences"
    - "Settings page uses Input, Select, Toggle, and Button in realistic form sections separated by Dividers"
    - "Navigating to /rialto/examples/form shows all validation states (default, error, disabled, loading) visible simultaneously"
    - "Form states page renders four state variants as static content — no useState toggle or tab switching required"
    - "All form data uses hospitality domain content (hotel operations, not Lorem ipsum)"
    - "Both pages have working Copy JSX buttons and composition notes"
  artifacts:
    - path: "apps/rialto-web/src/pages/examples/SettingsExamplePage.tsx"
      provides: "Settings page with Form, Input, Select, Toggle, Button in sections"
      exports: ["SettingsExamplePage"]
      min_lines: 80
    - path: "apps/rialto-web/src/pages/examples/SettingsExamplePage.module.css"
      provides: "Settings page layout styles"
    - path: "apps/rialto-web/src/pages/examples/FormStatesExamplePage.tsx"
      provides: "Form states with all validation states visible simultaneously"
      exports: ["FormStatesExamplePage"]
      min_lines: 80
    - path: "apps/rialto-web/src/pages/examples/FormStatesExamplePage.module.css"
      provides: "Form states page layout styles"
  key_links:
    - from: "apps/rialto-web/src/pages/examples/SettingsExamplePage.tsx"
      to: "apps/rialto-web/src/pages/examples/ExamplePageLayout.tsx"
      via: "import { ExamplePageLayout, CompositionNote }"
      pattern: "ExamplePageLayout"
    - from: "apps/rialto-web/src/pages/examples/FormStatesExamplePage.tsx"
      to: "apps/rialto-web/src/pages/examples/ExamplePageLayout.tsx"
      via: "import { ExamplePageLayout, StatePanel, CompositionNote }"
      pattern: "ExamplePageLayout|StatePanel"
    - from: "apps/rialto-web/src/pages/examples/SettingsExamplePage.tsx"
      to: "@mbe/rialto"
      via: "import { Input, Select, Toggle, Button, Card, Stack, Text, Divider }"
      pattern: "from \"@mbe/rialto\""
---

<objective>
Build the Settings example page (sectioned form layout) and Form States example page (all validation states visible simultaneously).

Purpose: EXMP-02 requires a settings page showing Form/Input/Select/Toggle/Button in realistic sections. EXMP-03 requires all validation states (default, error, disabled, loading) shown simultaneously without interaction. Both pages use hospitality domain data.
Output: SettingsExamplePage.tsx and FormStatesExamplePage.tsx replacing their stubs from Plan 01
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-example-pages/07-RESEARCH.md
@.planning/phases/07-example-pages/07-01-SUMMARY.md

@apps/rialto-web/src/pages/examples/ExamplePageLayout.tsx

<interfaces>
<!-- From ExamplePageLayout (created in Plan 01) -->
```typescript
interface ExamplePageLayoutProps {
  name: string;
  description: string;
  sourceJsx: string;
  compositionNotes?: ReactNode;
  children: ReactNode;
}
export function ExamplePageLayout(props: ExamplePageLayoutProps): JSX.Element;
export function StatePanel(props: { label: string; children: ReactNode }): JSX.Element;
export function CompositionNote(props: { children: ReactNode }): JSX.Element;
```

<!-- From @mbe/rialto — key component APIs to verify -->
```typescript
// Input: label, placeholder, value, error (boolean), hint, disabled, readOnly
// Select: label, options, value, onChange, disabled
// Toggle: label, checked, onChange, disabled
// Button: variant, size, onClick, disabled, children (NO loading prop)
// Card: variant ("default" | "flat"), children
// Divider: (no required props)
// Spinner: size, label
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build SettingsExamplePage with sectioned form layout</name>
  <files>
    apps/rialto-web/src/pages/examples/SettingsExamplePage.tsx
    apps/rialto-web/src/pages/examples/SettingsExamplePage.module.css
  </files>
  <action>
Replace the stub `SettingsExamplePage.tsx` with the full implementation.

**Structure:**
1. Define `SETTINGS_EXAMPLE_JSX` constant at top with full importable JSX. Comment: `// Keep in sync with component below`.

2. Export named `SettingsExamplePage` component:
   - Wraps in `<ExamplePageLayout name="Settings" description="Account settings page with profile, notifications, and display preferences" sourceJsx={SETTINGS_EXAMPLE_JSX} compositionNotes={...}>`
   - `compositionNotes`: 2-3 `<CompositionNote>` explaining: (a) Card + Divider creates visual sections without nested forms, (b) Toggle is used for binary preferences — immediate effect, no "save" needed for these, (c) Stack gap="lg" provides consistent vertical rhythm between fields

   - Body: Three `<Card>` sections separated by `<Stack gap="xl">`:

   **Section 1 — Profile** (`<Text variant="label" as="h2">Profile</Text>`):
   - `<Stack gap="md">` containing:
     - `<Input label="Full Name" value="Marcus Winters" readOnly={false} placeholder="Your name" />`
     - `<Input label="Email" value="m.winters@grandlakehotel.com" readOnly={false} placeholder="Email address" />`
     - `<Select label="Role">` with options: Operations Manager, Front Desk, Housekeeping Lead, General Manager — pre-selected "Operations Manager"
     - `<Select label="Timezone">` with options: America/Chicago, America/New_York, America/Los_Angeles, Europe/London — pre-selected "America/Chicago"
   - `<Divider />`
   - `<Stack direction="row" justify="flex-end"><Button variant="primary" size="md">Save Profile</Button></Stack>`

   **Section 2 — Notifications** (`<Text variant="label" as="h2">Notifications</Text>`):
   - `<Stack gap="md">` with three Toggle rows:
     - `<Toggle label="Booking alerts" checked />` (default on)
     - `<Toggle label="Maintenance requests" />` (default off)
     - `<Toggle label="Revenue reports" checked />` (default on)

   **Section 3 — Display Preferences** (`<Text variant="label" as="h2">Display</Text>`):
   - `<Stack gap="md">` with:
     - `<Select label="Date format">` options: MM/DD/YYYY, DD/MM/YYYY, YYYY-MM-DD — pre-selected "MM/DD/YYYY"
     - `<Select label="Currency">` options: USD ($), EUR (euro), GBP (pound) — pre-selected "USD ($)"
     - `<Toggle label="Compact table view" />`

   All form fields use hospitality domain labels/values. No Lorem ipsum. No "Test User" or "test@example.com".

   IMPORTANT: Check the actual Select, Toggle, Input APIs before using them. Read the component source files in `packages/rialto/src/components/` to confirm:
   - How Select accepts options (array of objects? children? render prop?)
   - How Toggle accepts label (prop? children?)
   - Whether Input accepts `readOnly` or `isReadOnly`
   Adapt the JSX to match the real API.

**CSS Module (SettingsExamplePage.module.css):**
- `.sections` — display flex, flex-direction column, gap `var(--rialto-space-xl)`
- `.sectionCard` — padding `var(--rialto-space-lg)` (if Card doesn't provide internal padding)
- All values use `var(--rialto-*)` tokens. Logical CSS properties only.
  </action>
  <verify>
    <automated>cd /Users/mbutler/github/mattbutlerengineering && npx tsc --noEmit -p apps/rialto-web/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <done>Settings page renders three sectioned form groups (Profile, Notifications, Display) with Input, Select, Toggle, Button, Card, and Divider. All content uses hospitality domain data. Composition notes explain section grouping choices. Copy JSX button works. TypeScript compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Build FormStatesExamplePage with all validation states visible simultaneously</name>
  <files>
    apps/rialto-web/src/pages/examples/FormStatesExamplePage.tsx
    apps/rialto-web/src/pages/examples/FormStatesExamplePage.module.css
  </files>
  <action>
Replace the stub `FormStatesExamplePage.tsx` with the full implementation.

**Structure:**
1. Define `FORM_STATES_EXAMPLE_JSX` constant at top. Comment: `// Keep in sync with component below`.

2. Export named `FormStatesExamplePage` component:
   - Wraps in `<ExamplePageLayout name="Form States" description="Every form input state rendered simultaneously — default, error, disabled, and loading" sourceJsx={FORM_STATES_EXAMPLE_JSX} compositionNotes={...}>`
   - `compositionNotes`: 2-3 `<CompositionNote>` explaining: (a) All states rendered as static siblings — no useState or interaction needed to see every state, (b) Error state uses the `error` prop + `hint` for validation messages, (c) Loading state shown via Spinner next to Button since Button has no loading prop

   - Body: Four `<StatePanel>` siblings, each showing the SAME form layout in a different state:

   **StatePanel label="Default":**
   - `<Card><Stack gap="md">`
     - `<Input label="Guest Name" placeholder="Full name" />`
     - `<Input label="Email Address" placeholder="guest@hotel.com" />`
     - `<Select label="Room Type">` with options: Standard, Deluxe, Suite, Penthouse
     - `<Stack direction="row" justify="flex-end"><Button variant="primary">Submit Reservation</Button></Stack>`
   - `</Stack></Card>`

   **StatePanel label="Error":**
   - Same layout but:
     - `<Input label="Guest Name" value="" error hint="Guest name is required" />`
     - `<Input label="Email Address" value="invalid-email" error hint="Enter a valid email address" />`
     - `<Select label="Room Type" error>` (if Select supports error prop — check the API; if not, wrap with error text)
     - Button still enabled (user can re-submit)

   **StatePanel label="Disabled":**
   - Same layout but all fields have `disabled` prop:
     - `<Input label="Guest Name" value="Elena Marchetti" disabled />`
     - `<Input label="Email Address" value="e.marchetti@resort.com" disabled />`
     - `<Select label="Room Type" disabled>` pre-selected "Suite"
     - `<Button variant="primary" disabled>Submit Reservation</Button>`

   **StatePanel label="Loading":**
   - Same layout but:
     - Fields enabled with values filled in: `<Input label="Guest Name" value="Sophie Laurent" readOnly />`
     - `<Input label="Email Address" value="s.laurent@grandlake.com" readOnly />`
     - `<Select label="Room Type" disabled>` pre-selected "Deluxe"
     - Button area: `<Stack direction="row" align="center" gap="sm" justify="flex-end"><Spinner size="sm" label="Submitting..." /><Button variant="primary" disabled>Submitting...</Button></Stack>`

   All four panels are rendered as siblings in a `<Stack gap="xl">` — NO tabs, NO state switching, NO useState for panel visibility. All visible on load.

   CRITICAL: Check actual Input, Select APIs before implementing. Read `packages/rialto/src/components/Input/Input.tsx` and `packages/rialto/src/components/Select/Select.tsx` to verify prop names (`error` vs `isInvalid`, `hint` vs `helperText`, `disabled` vs `isDisabled`, etc.). Adapt accordingly.

   Button has NO `loading` prop. Use Spinner adjacent to disabled Button for the loading state.

**CSS Module (FormStatesExamplePage.module.css):**
- `.statePanels` — display flex, flex-direction column, gap `var(--rialto-space-xl)`
- All values use `var(--rialto-*)` tokens. Logical CSS properties only.
  </action>
  <verify>
    <automated>cd /Users/mbutler/github/mattbutlerengineering && npx tsc --noEmit -p apps/rialto-web/tsconfig.json 2>&1 | head -20 && cd apps/rialto-web && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <done>Form States page renders four StatePanels (Default, Error, Disabled, Loading) all visible simultaneously without interaction. Each panel shows the same reservation form in its respective state. Error panel shows validation messages. Loading panel uses Spinner next to disabled Button. All content uses hospitality domain data. Build passes.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit -p apps/rialto-web/tsconfig.json` passes
2. `cd apps/rialto-web && npx vite build` succeeds
3. SettingsExamplePage exports named component with 3 form sections
4. FormStatesExamplePage exports named component with 4 StatePanels
5. Both pages have EXAMPLE_JSX constants
6. Both pages have CompositionNote elements
7. All data uses hospitality domain content
8. No useState for panel visibility in FormStatesExamplePage
</verification>

<success_criteria>
- Settings page at /rialto/examples/settings shows Profile, Notifications, and Display sections with realistic form controls
- Form States page at /rialto/examples/form shows all four states (default, error, disabled, loading) simultaneously without any user interaction
- Both pages use hospitality domain data throughout
- Both pages have working Copy JSX and visible composition notes
- Full build passes
</success_criteria>

<output>
After completion, create `.planning/phases/07-example-pages/07-03-SUMMARY.md`
</output>
