# Phase 41, Plan 1: Configuration Consolidation (Lighthouse)

**Goal**: Remove duplicate Lighthouse configuration files and ensure the primary configuration is correctly referenced across the monorepo.

**Dependencies**: None.

## Requirements
- **REQ-41-01:** `lighthouserc.json` must be deleted.
- **REQ-41-02:** `lighthouserc.js` must be confirmed as the source of truth.
- **REQ-41-03:** Root `package.json` and `.github/workflows/lighthouse-ci.yml` must use the `.js` config.

## Execution Steps
1. **[TDD] Verify Workflow:** Read `.github/workflows/lighthouse-ci.yml` to see how it references the config.
2. **[TDD] Verify Script:** Read root `package.json` `lighthouse` script.
3. **Deletion:** Delete `lighthouserc.json`.
4. **Validation:** Run `pnpm lighthouse` (if possible in this environment) or verify config path manually.

## Verification
- `ls lighthouserc.json` fails.
- `lighthouserc.js` remains.
