---
phase: 02-dashboard-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/dashboard/ -> apps/hospitality/ (git mv)
  - apps/hospitality/package.json
  - apps/hospitality/vite.config.ts
  - apps/hospitality/src/main.tsx
  - apps/hospitality/src/pages/SettingsPage.tsx
  - apps/hospitality/.env
  - apps/hospitality/.env.example
  - infrastructure/pulumi/auth0.ts
  - infrastructure/pulumi/index.ts
  - infrastructure/pulumi/README.md
  - apps/marketing/src/components/Layout.tsx
  - apps/rialto-web/src/layouts/ShowcaseLayout.tsx
  - .claude/settings.local.json
  - pnpm-lock.yaml
autonomous: true
requirements: [HOSP-01, HOSP-02, HOSP-03, HOSP-04, INFRA-01, INFRA-02, INFRA-03]

must_haves:
  truths:
    - "The app directory is apps/hospitality and the package name is @mbe/hospitality"
    - "Vite base, BrowserRouter basename, and Pulumi ingress all use /hospitality/"
    - "Auth0 callback URLs reference /hospitality/callback (both localhost and production)"
    - "A 301 redirect from /dashboard to /hospitality exists in Pulumi ingress rules"
    - "No references to /dashboard or @mbe/dashboard remain in code, config, or IaC"
    - "pnpm typecheck passes with zero errors"
    - "pnpm build --filter=@mbe/hospitality succeeds"
  artifacts:
    - path: "apps/hospitality/package.json"
      provides: "Package identity as @mbe/hospitality"
      contains: "@mbe/hospitality"
    - path: "apps/hospitality/vite.config.ts"
      provides: "Vite base /hospitality/ and PWA manifest with hospitality naming"
      contains: "base: \"/hospitality/\""
    - path: "apps/hospitality/src/main.tsx"
      provides: "BrowserRouter basename and Auth0 redirect URI using /hospitality"
      contains: "basename=\"/hospitality\""
    - path: "infrastructure/pulumi/auth0.ts"
      provides: "Auth0 client renamed to mattbutlerengineering-hospitality with /hospitality callbacks"
      contains: "mattbutlerengineering-hospitality"
    - path: "infrastructure/pulumi/index.ts"
      provides: "Ingress rules with /dashboard redirect and /hospitality component"
      contains: "redirectCode: 301"
  key_links:
    - from: "apps/hospitality/vite.config.ts"
      to: "infrastructure/pulumi/index.ts"
      via: "base path alignment: Vite base /hospitality/ matches ingress prefix /hospitality"
      pattern: "base.*hospitality"
    - from: "apps/hospitality/src/main.tsx"
      to: "infrastructure/pulumi/auth0.ts"
      via: "Auth0 redirect URI alignment: /hospitality/callback in both app and IaC"
      pattern: "hospitality/callback"
    - from: "infrastructure/pulumi/index.ts"
      to: "infrastructure/pulumi/auth0.ts"
      via: "Client ID reference: hospitalityClientId from auth0Outputs"
      pattern: "auth0Outputs\\.hospitalityClientId"
---

<objective>
Rename dashboard to hospitality: directory, package name, Vite config, React Router, Auth0 IaC, Pulumi ingress, cross-app links, and .env files — all atomically in one commit so auth and routing never break.

Purpose: Isolate the app rename as a clean, atomic operation before any Rialto migration work begins in Phase 4.
Output: Fully renamed app at apps/hospitality with matching IaC, plus a 301 redirect from /dashboard.
</objective>

<execution_context>
@/Users/mbutler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbutler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-rename/02-RESEARCH.md
@.planning/phases/02-dashboard-rename/02-CONTEXT.md
@infrastructure/pulumi/index.ts
@infrastructure/pulumi/auth0.ts
@apps/dashboard/vite.config.ts
@apps/dashboard/src/main.tsx
@apps/dashboard/package.json
@apps/dashboard/.env.example
@apps/dashboard/src/pages/SettingsPage.tsx
@apps/marketing/src/components/Layout.tsx
@apps/rialto-web/src/layouts/ShowcaseLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename directory and update all app-level files</name>
  <files>
    apps/dashboard/ -> apps/hospitality/ (git mv)
    apps/hospitality/package.json
    apps/hospitality/vite.config.ts
    apps/hospitality/src/main.tsx
    apps/hospitality/src/pages/SettingsPage.tsx
    apps/hospitality/.env
    apps/hospitality/.env.example
    apps/marketing/src/components/Layout.tsx
    apps/rialto-web/src/layouts/ShowcaseLayout.tsx
    .claude/settings.local.json
    pnpm-lock.yaml
  </files>
  <action>
    **Step 1: Directory rename via git mv** (preserves history better than delete+create):
    ```bash
    git mv apps/dashboard apps/hospitality
    ```

    **Step 2: Update apps/hospitality/package.json:**
    - Change `"name": "@mbe/dashboard"` to `"name": "@mbe/hospitality"`

    **Step 3: Update apps/hospitality/vite.config.ts:**
    - Change `base: "/dashboard/"` to `base: "/hospitality/"`
    - Change PWA manifest:
      - `name: "MBE Dashboard"` → `name: "MBE Hospitality"`
      - `short_name: "Dashboard"` → `short_name: "Hospitality"`
      - `description:` → `"Hospitality management — reservations, guests, and floor plans"` (per CONTEXT.md decision)
      - `scope: "/dashboard/"` → `scope: "/hospitality/"`
      - `start_url: "/dashboard/"` → `start_url: "/hospitality/"`

    **Step 4: Update apps/hospitality/src/main.tsx:**
    - Change BrowserRouter basename: `basename="/dashboard"` → `basename="/hospitality"`
    - Change redirectUri fallback: `"/dashboard/callback"` → `"/hospitality/callback"`

    **Step 5: Update apps/hospitality/src/pages/SettingsPage.tsx:**
    - Change line ~128: `"Choose how the dashboard looks to you"` → `"Choose how the app looks to you"`

    **Step 6: Update apps/hospitality/.env and .env.example:**
    - In `.env`: Change `VITE_AUTH_REDIRECT_URI=http://localhost:3002/dashboard/callback` to `VITE_AUTH_REDIRECT_URI=http://localhost:3002/hospitality/callback`
    - In `.env.example`: Same change

    **Step 7: Update cross-app links:**
    - `apps/marketing/src/components/Layout.tsx` line 13: Change `href="/dashboard"` → `href="/hospitality"`
    - `apps/rialto-web/src/layouts/ShowcaseLayout.tsx` line 92: Change `{ label: "Dashboard", href: "https://mattbutlerengineering.com/dashboard" }` → `{ label: "Hospitality", href: "https://mattbutlerengineering.com/hospitality" }`

    **Step 8: Update .claude/settings.local.json:**
    - Change all `@mbe/dashboard` filter references to `@mbe/hospitality` (there are two: typecheck and lint)

    **Step 9: Regenerate lockfile:**
    ```bash
    pnpm install
    ```
    This regenerates pnpm-lock.yaml with the new package name. Do NOT skip this — the lockfile will be inconsistent otherwise.

    **IMPORTANT — Do NOT touch infrastructure/pulumi/ files in this task.** Those are handled in Task 2.
  </action>
  <verify>
    <automated>
      cd /Users/mbutler/github/mattbutlerengineering && \
      test -d apps/hospitality && \
      ! test -d apps/dashboard && \
      grep -q '"@mbe/hospitality"' apps/hospitality/package.json && \
      grep -q 'base: "/hospitality/"' apps/hospitality/vite.config.ts && \
      grep -q 'basename="/hospitality"' apps/hospitality/src/main.tsx && \
      grep -q '/hospitality/callback' apps/hospitality/.env.example && \
      grep -q '/hospitality' apps/marketing/src/components/Layout.tsx && \
      grep -q 'Hospitality' apps/rialto-web/src/layouts/ShowcaseLayout.tsx && \
      echo "PASS: All app-level renames verified"
    </automated>
  </verify>
  <done>
    - apps/dashboard directory no longer exists
    - apps/hospitality exists with package name @mbe/hospitality
    - Vite base, BrowserRouter basename, PWA manifest, and .env all reference /hospitality
    - SettingsPage says "app" instead of "dashboard"
    - Marketing and rialto-web link to /hospitality
    - pnpm-lock.yaml regenerated
  </done>
</task>

<task type="auto">
  <name>Task 2: Update Pulumi IaC — Auth0 client, ingress rules, static site, exports</name>
  <files>
    infrastructure/pulumi/auth0.ts
    infrastructure/pulumi/index.ts
    infrastructure/pulumi/README.md
  </files>
  <action>
    **Step 1: Update infrastructure/pulumi/auth0.ts** — full rewrite of dashboard references:

    **Callback URLs:**
    - `localCallbacks`: Replace `"http://localhost:3002/dashboard/callback"` with `"http://localhost:3002/hospitality/callback"`
    - `prodCallbacks`: Replace `\`https://${domain}/dashboard/callback\`` with `\`https://${domain}/hospitality/callback\``

    **Logout URLs:**
    - `localLogoutUrls`: Replace `"http://localhost:3002/dashboard"` with `"http://localhost:3002/hospitality"`
    - `prodLogoutUrls`: Replace `\`https://${domain}/dashboard\`` with `\`https://${domain}/hospitality\``

    **Web Origins:**
    - `prodWebOrigins`: Replace `\`https://${domain}/dashboard\`` with `\`https://${domain}/hospitality\``
    (Note: localWebOrigins use port-only origins — no path change needed for `"http://localhost:3002"`)

    **Auth0 Client resource:**
    - Change resource logical name: `"mattbutlerengineering-app"` → `"mattbutlerengineering-hospitality"`
    - Change `name:` property: `"mattbutlerengineering-app"` → `"mattbutlerengineering-hospitality"`
    - Change `description:` to `"Matt Butler Engineering Hospitality"`
    - Rename export variable: `export const dashboardApp` → `export const hospitalityApp`

    **Client Grant:**
    - Change resource logical name: `"mattbutlerengineering-app-api-grant"` → `"mattbutlerengineering-hospitality-api-grant"`
    - Change `clientId:` reference: `dashboardApp.clientId` → `hospitalityApp.clientId`
    - Rename export variable: `export const dashboardApiGrant` → `export const hospitalityApiGrant`

    **auth0Outputs:**
    - Change `dashboardClientId: dashboardApp.clientId` → `hospitalityClientId: hospitalityApp.clientId`

    **Step 2: Update infrastructure/pulumi/index.ts:**

    **Import reference:**
    - The import `{ auth0Outputs }` stays the same (the object is imported, not destructured properties)

    **Export:**
    - Change `auth0Outputs.dashboardClientId` → `auth0Outputs.hospitalityClientId` (appears in export line AND in marketing static site envs AND in hospitality static site envs)

    **Ingress rules — replace the /dashboard component rule and add redirect:**
    Replace the existing `/dashboard` ingress rule with TWO rules:
    ```typescript
    // 301 redirect from old /dashboard path
    {
      match: {
        path: {
          prefix: "/dashboard",
        },
      },
      redirect: {
        uri: "/hospitality",
        redirectCode: 301,
      },
    },
    // Hospitality app (renamed from dashboard)
    {
      match: {
        path: {
          prefix: "/hospitality",
        },
      },
      component: {
        name: "hospitality",
        preservePathPrefix: false,
      },
    },
    ```
    Place both rules AFTER `/api` and BEFORE `/rialto`. Order: /api, /dashboard (redirect), /hospitality (component), /rialto, / (catch-all).

    **Static site — rename the dashboard entry:**
    - Change `name: "dashboard"` → `name: "hospitality"`
    - Change `buildCommand:` to `"pnpm build --filter=@mbe/hospitality"`
    - Change `outputDir:` to `"apps/hospitality/dist"`
    - Change `VITE_AUTH_CLIENT_ID` value: `auth0Outputs.dashboardClientId` → `auth0Outputs.hospitalityClientId`
    - Change `VITE_AUTH_REDIRECT_URI` value: `` `https://${domain}/dashboard/callback` `` → `` `https://${domain}/hospitality/callback` ``

    **Also update marketing static site env:**
    - Change `VITE_AUTH_CLIENT_ID` value: `auth0Outputs.dashboardClientId` → `auth0Outputs.hospitalityClientId`

    **Bottom exports:**
    - Change `export const dashboardUrl` → `export const hospitalityUrl`
    - Change the interpolation: `` `https://${domain}/dashboard` `` → `` `https://${domain}/hospitality` ``

    **Step 3: Update infrastructure/pulumi/README.md:**
    - In the "What Gets Created" table: Change `└─ \`dashboard\`` row to `└─ \`hospitality\`` with description "Hospitality static site"
    - Add mention of rialto-web static site if not already present

    **CRITICAL NOTE about Auth0 client ID change:**
    Renaming the Pulumi resource from `"mattbutlerengineering-app"` to `"mattbutlerengineering-hospitality"` will cause Pulumi to delete and recreate the Auth0 client, generating a **new client_id**. Since both static sites (marketing and hospitality) reference `auth0Outputs.hospitalityClientId`, they will automatically pick up the new client ID in the same `pulumi up`. However, the local `.env` file will need manual update after `pulumi up` — document this in the summary.
  </action>
  <verify>
    <automated>
      cd /Users/mbutler/github/mattbutlerengineering && \
      grep -q 'mattbutlerengineering-hospitality' infrastructure/pulumi/auth0.ts && \
      grep -q 'hospitalityClientId' infrastructure/pulumi/auth0.ts && \
      grep -q 'hospitalityApp' infrastructure/pulumi/auth0.ts && \
      grep -q 'hospitality/callback' infrastructure/pulumi/auth0.ts && \
      grep -q 'redirectCode: 301' infrastructure/pulumi/index.ts && \
      grep -q 'name: "hospitality"' infrastructure/pulumi/index.ts && \
      grep -q 'hospitalityClientId' infrastructure/pulumi/index.ts && \
      grep -q '@mbe/hospitality' infrastructure/pulumi/index.ts && \
      grep -q 'hospitalityUrl' infrastructure/pulumi/index.ts && \
      ! grep -q 'dashboardClientId' infrastructure/pulumi/auth0.ts && \
      ! grep -q 'dashboardClientId' infrastructure/pulumi/index.ts && \
      ! grep -q '"dashboard"' infrastructure/pulumi/index.ts && \
      echo "PASS: All IaC renames verified"
    </automated>
  </verify>
  <done>
    - Auth0 client renamed to mattbutlerengineering-hospitality with /hospitality callbacks
    - Client grant references updated to hospitalityApp
    - auth0Outputs exports hospitalityClientId instead of dashboardClientId
    - Pulumi ingress has 301 redirect from /dashboard and component rule for /hospitality
    - Static site renamed to "hospitality" with correct buildCommand, outputDir, and envs
    - Export variables renamed (hospitalityUrl, hospitalityClientId)
    - Pulumi README updated
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full verification — typecheck, build, and grep for stale references</name>
  <files>
    (no files created — verification only)
  </files>
  <action>
    **Step 1: TypeScript typecheck** across the monorepo:
    ```bash
    pnpm typecheck
    ```
    Must pass with zero errors. The Pulumi TypeScript will validate that all renamed exports and references are consistent.

    **Step 2: Build the hospitality app:**
    ```bash
    pnpm build --filter=@mbe/hospitality
    ```
    Must succeed. This validates the Vite config, base path, and all imports resolve.

    **Step 3: Grep for stale dashboard references in code and config** (exclude docs, planning, and .claude/):
    ```bash
    grep -r --include="*.ts" --include="*.tsx" --include="*.json" --include="*.yaml" --include="*.yml" \
      -l "dashboard" \
      apps/ infrastructure/ services/ packages/ tools/ \
      | grep -v node_modules | grep -v dist | grep -v pnpm-lock.yaml
    ```
    This MUST return zero results. If any files still reference "dashboard", fix them before proceeding.

    **Step 4: Verify directory state:**
    ```bash
    test -d apps/hospitality && ! test -d apps/dashboard && echo "Directory rename confirmed"
    ```

    **Step 5: Verify three-way alignment:**
    ```bash
    grep 'base:.*hospitality' apps/hospitality/vite.config.ts && \
    grep 'basename.*hospitality' apps/hospitality/src/main.tsx && \
    grep 'prefix:.*hospitality' infrastructure/pulumi/index.ts && \
    echo "Three-way alignment confirmed"
    ```

    If any check fails, fix the issue and re-run. Do NOT proceed to the commit if verification fails.
  </action>
  <verify>
    <automated>
      cd /Users/mbutler/github/mattbutlerengineering && \
      pnpm typecheck 2>&1 | tail -5 && \
      pnpm build --filter=@mbe/hospitality 2>&1 | tail -5
    </automated>
  </verify>
  <done>
    - pnpm typecheck passes with zero errors
    - pnpm build --filter=@mbe/hospitality succeeds
    - Zero stale "dashboard" references in code/config files (excluding docs and planning)
    - Three-way alignment confirmed: Vite base, BrowserRouter basename, Pulumi ingress all say /hospitality
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `pnpm typecheck` — zero errors across monorepo
2. `pnpm build --filter=@mbe/hospitality` — clean build
3. `grep -r "dashboard" apps/ infrastructure/ --include="*.ts" --include="*.tsx" --include="*.json" | grep -v node_modules | grep -v dist | grep -v pnpm-lock.yaml` — zero results
4. Three-way path alignment: Vite base, React Router basename, Pulumi ingress all reference `/hospitality`
5. Auth0 callback URLs in auth0.ts all reference `/hospitality/callback`
6. 301 redirect rule exists in index.ts for `/dashboard` → `/hospitality`
</verification>

<success_criteria>
- apps/hospitality exists, apps/dashboard does not
- @mbe/hospitality is the package name
- All code, config, and IaC reference "hospitality" not "dashboard"
- Auth0 client named mattbutlerengineering-hospitality
- 301 redirect from /dashboard in Pulumi ingress
- typecheck and build both pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-rename/02-01-SUMMARY.md`
</output>
