---
phase: 02-dashboard-rename
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - CLAUDE.md
  - docs/ARCHITECTURE.md
  - docs/NEXT_STEPS.md
  - docs/evaluations/2026-02-26-frontend-meta-frameworks.md
  - docs/evaluations/2026-02-26-auth-providers.md
  - docs/evaluations/2026-02-26-monorepo-tooling.md
  - docs/evaluations/2026-02-26-analytics-feature-flags.md
  - docs/evaluations/2026-02-26-e2e-testing.md
  - docs/plans/2026-01-22-platform-design.md
  - docs/plans/2026-02-25-rialto-monorepo-integration.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/STRUCTURE.md
  - .planning/codebase/STACK.md
  - .planning/codebase/CONCERNS.md
  - .claude/skills/auth-package/SKILL.md
autonomous: true
requirements: [HOSP-01, HOSP-03, INFRA-01, INFRA-03]

must_haves:
  truths:
    - "No documentation file references /dashboard or apps/dashboard as the current app path"
    - "CLAUDE.md URL table, directory layout, and dev access point all say hospitality"
    - "All evaluations and plans reference apps/hospitality and /hospitality where appropriate"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Primary project docs with hospitality naming"
      contains: "apps/hospitality"
    - path: ".planning/codebase/STRUCTURE.md"
      provides: "Codebase structure reflecting hospitality directory"
      contains: "apps/hospitality"
    - path: ".claude/skills/auth-package/SKILL.md"
      provides: "Auth skill with /hospitality/callback example"
      contains: "/hospitality/callback"
  key_links:
    - from: "CLAUDE.md"
      to: "apps/hospitality/"
      via: "URL convention table and directory layout reference the actual app directory"
      pattern: "apps/hospitality"
---

<objective>
Update all documentation across the monorepo to replace dashboard references with hospitality — CLAUDE.md, evaluations, plans, codebase docs, and skill files.

Purpose: Keep documentation in sync with the functional rename completed in Plan 02-01. Follows the same two-commit pattern used in the web→marketing rename.
Output: All docs reference "hospitality" instead of "dashboard" where the term refers to the app name, path, or directory.
</objective>

<execution_context>
@/Users/mbutler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbutler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dashboard-rename/02-CONTEXT.md
@.planning/phases/02-dashboard-rename/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update primary project docs — CLAUDE.md, ARCHITECTURE.md, NEXT_STEPS.md</name>
  <files>
    CLAUDE.md
    docs/ARCHITECTURE.md
    docs/NEXT_STEPS.md
  </files>
  <action>
    **CLAUDE.md updates** — this is the most critical doc file:

    1. **URL Convention table**: Change the `/dashboard` row:
       - Path: `/dashboard` → `/hospitality`
       - App: `Authenticated dashboard` → `Hospitality app`
       - Directory: `apps/dashboard` → `apps/hospitality`

    2. **Dev port comment**: Update references from "dashboard" to "hospitality" (port 3002 stays the same)

    3. **Directory Layout**: Change `├── dashboard/` to `├── hospitality/` and update its description

    4. **Access points section**: Change `Dashboard: http://localhost:3002/dashboard` to `Hospitality: http://localhost:3002/hospitality`

    5. **Any other occurrences**: grep for "dashboard" in CLAUDE.md and replace with "hospitality" where it refers to the app (NOT where it refers to Auth0 dashboard or other generic usage of the word "dashboard")

    **docs/ARCHITECTURE.md**: Replace all references to `apps/dashboard`, `@mbe/dashboard`, `/dashboard` path with hospitality equivalents. Be careful to only replace references to the APP named dashboard, not generic uses of the word.

    **docs/NEXT_STEPS.md**: Replace app list entries, URL table rows, and any path references from dashboard to hospitality.

    **IMPORTANT context-sensitivity rule**: The word "dashboard" in phrases like "Auth0 dashboard", "Stripe dashboard", or "admin dashboard" (generic UI concept) must NOT be changed. Only change references to the specific `apps/dashboard` app, the `@mbe/dashboard` package, or the `/dashboard` URL path.
  </action>
  <verify>
    <automated>
      cd /Users/mbutler/github/mattbutlerengineering && \
      grep -c 'apps/dashboard' CLAUDE.md docs/ARCHITECTURE.md docs/NEXT_STEPS.md 2>/dev/null | grep -v ':0$' | wc -l | xargs test 0 -eq && \
      grep -q 'apps/hospitality' CLAUDE.md && \
      echo "PASS: Primary docs updated"
    </automated>
  </verify>
  <done>
    - CLAUDE.md URL table, directory layout, and access points all reference hospitality
    - docs/ARCHITECTURE.md references apps/hospitality
    - docs/NEXT_STEPS.md references hospitality
    - Zero occurrences of "apps/dashboard" or "@mbe/dashboard" in these three files
  </done>
</task>

<task type="auto">
  <name>Task 2: Update evaluations, plans, codebase docs, and skill files</name>
  <files>
    docs/evaluations/2026-02-26-frontend-meta-frameworks.md
    docs/evaluations/2026-02-26-auth-providers.md
    docs/evaluations/2026-02-26-monorepo-tooling.md
    docs/evaluations/2026-02-26-analytics-feature-flags.md
    docs/evaluations/2026-02-26-e2e-testing.md
    docs/plans/2026-01-22-platform-design.md
    docs/plans/2026-02-25-rialto-monorepo-integration.md
    .planning/codebase/ARCHITECTURE.md
    .planning/codebase/STRUCTURE.md
    .planning/codebase/STACK.md
    .planning/codebase/CONCERNS.md
    .claude/skills/auth-package/SKILL.md
  </files>
  <action>
    For each file listed, perform targeted find-and-replace for app-specific dashboard references:

    **Replacements to apply** (in order of specificity, most specific first):
    1. `apps/dashboard/src/main.tsx` → `apps/hospitality/src/main.tsx`
    2. `apps/dashboard/` → `apps/hospitality/`
    3. `apps/dashboard` → `apps/hospitality`
    4. `@mbe/dashboard` → `@mbe/hospitality`
    5. `/dashboard/callback` → `/hospitality/callback`
    6. `/dashboard` → `/hospitality` (only when referring to the URL path, NOT generic "dashboard" word)
    7. `"dashboard app"` or `"Dashboard app"` → `"hospitality app"` or `"Hospitality app"` (when referring to the specific app)

    **Context-sensitivity rule (CRITICAL):**
    - Do NOT change "Auth0 dashboard", "Stripe dashboard", or any generic use of "dashboard" meaning a UI screen
    - Do NOT change text inside `02-CONTEXT.md`, `02-RESEARCH.md`, or other phase 2 planning docs (they document the rename itself)
    - Do NOT change references in `.planning/phases/01-*` files (they are historical records)

    **File-specific notes:**
    - `docs/evaluations/2026-02-26-auth-providers.md`: Has `apps/dashboard/src/main.tsx` path reference — update to apps/hospitality
    - `docs/evaluations/2026-02-26-monorepo-tooling.md`: Has `@mbe/dashboard` package reference — update
    - `docs/evaluations/2026-02-26-analytics-feature-flags.md`: Has "dashboard app" references — update to "hospitality app"
    - `docs/evaluations/2026-02-26-e2e-testing.md`: Has dashboard app reference — update
    - `docs/evaluations/2026-02-26-frontend-meta-frameworks.md`: Has `apps/dashboard/` path references — update
    - `docs/plans/2026-01-22-platform-design.md`: Has `/dashboard` path references — update
    - `docs/plans/2026-02-25-rialto-monorepo-integration.md`: Has `apps/dashboard` references — update
    - `.planning/codebase/*.md`: Has `apps/dashboard/` file paths and references — update all four files
    - `.claude/skills/auth-package/SKILL.md`: Has `/dashboard/callback` example in env vars section (line ~253) — update to `/hospitality/callback`

    **Approach:** For each file:
    1. Read the file
    2. Identify all dashboard references (use grep to find line numbers)
    3. Apply replacements using Edit tool for each unique match
    4. Verify no stale references remain in that file
  </action>
  <verify>
    <automated>
      cd /Users/mbutler/github/mattbutlerengineering && \
      grep -r --include="*.md" -l "apps/dashboard\|@mbe/dashboard\|/dashboard/callback" \
        docs/ .planning/codebase/ .claude/skills/ 2>/dev/null \
        | grep -v "02-CONTEXT\|02-RESEARCH\|01-.*PLAN\|01-.*SUMMARY\|01-RESEARCH\|.continue-here" \
        | wc -l | xargs test 0 -eq && \
      echo "PASS: All doc references updated"
    </automated>
  </verify>
  <done>
    - Zero occurrences of apps/dashboard, @mbe/dashboard, or /dashboard/callback in docs/, .planning/codebase/, or .claude/skills/ (excluding phase 2 context/research and phase 1 historical records)
    - All evaluations reference apps/hospitality and @mbe/hospitality
    - All plan docs reference /hospitality path
    - Codebase docs reflect the renamed directory structure
    - Auth skill SKILL.md example uses /hospitality/callback
  </done>
</task>

</tasks>

<verification>
After all tasks complete:
1. `grep -r --include="*.md" "apps/dashboard\|@mbe/dashboard" CLAUDE.md docs/ .planning/codebase/ .claude/skills/ | grep -v "02-CONTEXT\|02-RESEARCH\|01-.*PLAN\|01-.*SUMMARY\|01-RESEARCH"` — zero results
2. CLAUDE.md URL convention table shows `/hospitality` for the authenticated app
3. CLAUDE.md directory layout shows `apps/hospitality/`
4. Auth skill example uses `/hospitality/callback`
</verification>

<success_criteria>
- All documentation references updated from dashboard to hospitality
- No stale apps/dashboard or @mbe/dashboard references in docs (excluding historical phase records)
- CLAUDE.md is the authoritative source and fully reflects the rename
</success_criteria>

<output>
After completion, create `.planning/phases/02-dashboard-rename/02-02-SUMMARY.md`
</output>
