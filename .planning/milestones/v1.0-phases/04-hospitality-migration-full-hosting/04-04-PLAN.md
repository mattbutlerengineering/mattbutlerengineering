---
phase: 04-hospitality-migration-full-hosting
plan: 04
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - apps/hospitality/package.json
  - apps/hospitality/tailwind.config.js
  - apps/hospitality/postcss.config.js
  - apps/hospitality/src/index.css
  - packages/config/package.json
  - CLAUDE.md
  - AGENTS.md
autonomous: true
requirements:
  - CLEAN-01
  - CLEAN-02
  - CLEAN-03

must_haves:
  truths:
    - "tailwindcss, postcss, and autoprefixer are not in apps/hospitality/package.json"
    - "tailwind.config.js and postcss.config.js do not exist in apps/hospitality/"
    - "No @tailwind directives remain in apps/hospitality/src/index.css"
    - "@mbe/rialto is in apps/hospitality/package.json dependencies"
    - "@mbe/ui and @mbe/shared-layout are not in apps/hospitality/package.json"
    - "packages/ui/ directory does not exist"
    - "packages/shared-layout/ directory does not exist"
    - "pnpm install succeeds and lockfile is updated"
    - "ESLint downgraded to v9 stable — pnpm lint no longer errors on ajv"
    - "Stale agent worktrees deleted"
    - "CLAUDE.md and AGENTS.md updated to reflect Rialto-only architecture"
  artifacts:
    - path: "apps/hospitality/package.json"
      provides: "Clean dependency list with @mbe/rialto, no @mbe/ui, no Tailwind"
    - path: "packages/config/package.json"
      provides: "ESLint v9 stable instead of v10 pre-release"
  key_links:
    - from: "apps/hospitality/package.json"
      to: "packages/rialto"
      via: "workspace:* dependency"
      pattern: "@mbe/rialto.*workspace"
---

<objective>
Remove all Tailwind/PostCSS/autoprefixer from hospitality, fix the ESLint ajv error by downgrading to v9, delete @mbe/ui and @mbe/shared-layout packages from the monorepo, clean up stale worktrees, and update documentation.

Purpose: After this plan, the monorepo has no @mbe/ui or @mbe/shared-layout packages, no Tailwind in hospitality, ESLint works correctly, and documentation reflects the Rialto-only architecture.

Output: Clean package.json, deleted config files, deleted packages, fixed ESLint, updated docs.
</objective>

<execution_context>
@/Users/mbutler/.claude/get-shit-done/workflows/execute-plan.md
@/Users/mbutler/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-hospitality-migration-full-hosting/04-RESEARCH.md

@apps/hospitality/package.json
@apps/hospitality/src/index.css
@packages/config/package.json
@CLAUDE.md
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove Tailwind from hospitality + fix ESLint ajv error</name>
  <files>
    apps/hospitality/package.json,
    apps/hospitality/tailwind.config.js,
    apps/hospitality/postcss.config.js,
    apps/hospitality/src/index.css,
    packages/config/package.json
  </files>
  <action>
    **apps/hospitality/package.json:**
    - Remove from dependencies: `@mbe/ui`, `@mbe/shared-layout`
    - Add to dependencies: `"@mbe/rialto": "workspace:*"`
    - Remove from devDependencies: `tailwindcss`, `postcss`, `autoprefixer`
    - Keep all other dependencies unchanged (react, react-dom, react-router-dom, konva, react-konva, @mbe/auth, @mbe/api-client, @mbe/types, @mbe/config, @types/react, @types/react-dom, @vitejs/plugin-react, typescript, vite, vite-plugin-pwa)

    **Delete files:**
    - `rm apps/hospitality/tailwind.config.js`
    - `rm apps/hospitality/postcss.config.js`

    **apps/hospitality/src/index.css:**
    - Remove ALL `@tailwind base;`, `@tailwind components;`, `@tailwind utilities;` directives
    - Keep any non-Tailwind CSS resets or global styles (e.g., `:root` vars, `*` box-sizing, html/body height)
    - If the file becomes empty or only has Tailwind directives, keep it with a minimal reset:
      ```css
      *,
      *::before,
      *::after {
        box-sizing: border-box;
        margin: 0;
      }
      ```
    - Note: Rialto's styles (imported via `@mbe/rialto/styles`) provide the design token cascade. index.css only needs app-specific resets.

    **packages/config/package.json — ESLint fix:**
    - Change `"eslint": "^10.0.1"` to `"eslint": "^9.0.0"` (latest stable v9)
    - ESLint 9 uses flat config (same as v10 pre-release), so eslint.config.js files should work without changes
    - Run `pnpm install` to resolve the dependency change
    - Verify: `pnpm lint` should no longer error on `ajv/lib/refs/json-schema-draft-04.json`
    - If any eslint.config.js API changes are needed between v9 and v10, fix them

    **Run after edits:**
    - `cd /Users/mbutler/github/mattbutlerengineering && pnpm install` — updates lockfile, removes deleted packages from node_modules
    - Verify: `pnpm typecheck` passes (no @mbe/ui references should remain after Plans 01-03)
  </action>
  <verify>
    <automated>cd /Users/mbutler/github/mattbutlerengineering && pnpm install && pnpm typecheck 2>&1 | tail -20</automated>
  </verify>
  <done>Tailwind/PostCSS/autoprefixer removed from hospitality. @mbe/ui and @mbe/shared-layout removed from hospitality deps. @mbe/rialto added. index.css has no @tailwind directives. ESLint downgraded to v9 stable. pnpm install and typecheck both pass.</done>
</task>

<task type="auto">
  <name>Task 2: Delete @mbe/ui and @mbe/shared-layout packages + clean up worktrees + update docs</name>
  <files>
    CLAUDE.md,
    AGENTS.md
  </files>
  <action>
    **Delete packages:**
    - `rm -rf packages/ui/`
    - `rm -rf packages/shared-layout/`
    - Run `pnpm install` to update lockfile after deletion
    - Run `pnpm typecheck` to verify zero errors remain

    **Delete stale agent worktrees:**
    - `rm -rf tools/cli/.agent-worktrees/`
    - `rm -rf services/agent/.agent-worktrees/`
    - `rm -rf .worktrees/`
    - These are git worktrees that snapshot old repo state. They reference deleted packages and are dead weight.
    - Note: If git complains about worktree references, also run `git worktree prune` to clean up stale entries

    **Update CLAUDE.md:**
    - In the Directory Layout section, remove the `ui/` line from packages and update the comment: `ui/` → remove entirely
    - Update the `shared-layout/` line → remove entirely (or note "deleted")
    - If there are references to @mbe/ui as a dependency or "being replaced by rialto", update to state that Rialto IS the design system (no longer "being replaced")
    - Ensure the rialto line says: `rialto/            # Rialto design system (React component library)` (no "being replaced by" language)

    **Update AGENTS.md:**
    - Apply the same changes as CLAUDE.md (AGENTS.md mirrors CLAUDE.md)
    - Remove @mbe/ui references
    - Remove @mbe/shared-layout references

    **Verify no remaining references:**
    - `grep -r "@mbe/ui" --include="*.json" --include="*.ts" --include="*.tsx" --include="*.md" . | grep -v node_modules | grep -v .git | grep -v PLAN | grep -v SUMMARY | grep -v RESEARCH | grep -v CONTEXT | grep -v ROADMAP | grep -v REQUIREMENTS` — should return zero results (or only references in historical planning docs)
  </action>
  <verify>
    <automated>cd /Users/mbutler/github/mattbutlerengineering && pnpm install && pnpm typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>packages/ui/ and packages/shared-layout/ directories deleted. Stale worktrees removed. pnpm lockfile updated. pnpm typecheck passes clean. CLAUDE.md and AGENTS.md updated to reflect Rialto-only architecture with no @mbe/ui references.</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. `ls packages/ui/ packages/shared-layout/ 2>&1` — "No such file or directory" for both
2. `pnpm install` — succeeds without errors
3. `pnpm typecheck` — zero errors
4. `pnpm lint 2>&1 | head -5` — no ajv error (ESLint v9 fix)
5. `grep -r "@mbe/ui\|@mbe/shared-layout" apps/ services/ packages/ tools/ --include="*.json" --include="*.ts" --include="*.tsx" | grep -v node_modules` — zero results
6. `ls apps/hospitality/tailwind.config.js apps/hospitality/postcss.config.js 2>&1` — "No such file or directory" for both
7. `grep "@tailwind" apps/hospitality/src/index.css` — zero results
</verification>

<success_criteria>
- tailwindcss, postcss, autoprefixer removed from hospitality devDependencies
- @mbe/ui, @mbe/shared-layout removed from hospitality dependencies
- @mbe/rialto added to hospitality dependencies
- tailwind.config.js and postcss.config.js deleted from hospitality
- index.css has no @tailwind directives
- packages/ui/ and packages/shared-layout/ directories deleted
- Stale agent worktrees deleted
- ESLint downgraded to v9 stable (ajv error resolved)
- pnpm install succeeds
- pnpm typecheck passes with zero errors
- CLAUDE.md and AGENTS.md reflect Rialto-only architecture
</success_criteria>

<output>
After completion, create `.planning/phases/04-hospitality-migration-full-hosting/04-04-SUMMARY.md`
</output>
