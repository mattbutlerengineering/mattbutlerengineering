---
phase: 12-catalog-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - packages/agent-core/src/pr-creator.ts
  - packages/agent-core/package.json
  - services/users/package.json
  - services/agent/package.json
  - services/reservations/package.json
  - packages/rialto-catalog/package.json
  - packages/rialto-catalog/tsconfig.json
  - packages/rialto-catalog/vitest.config.ts
  - packages/rialto-catalog/src/index.ts
  - packages/rialto-catalog/src/__tests__/catalog.test.ts
  - packages/rialto-catalog/src/__tests__/drift-check.test.ts
autonomous: true
requirements:
  - CAT-01

must_haves:
  truths:
    - "pnpm build succeeds across all workspace packages after Zod v4 upgrade"
    - "pnpm typecheck passes with no type errors in existing Zod schemas"
    - "packages/rialto-catalog exists as a private workspace package with test infrastructure"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Zod version pinned in workspace catalog"
      contains: "zod"
    - path: "packages/rialto-catalog/package.json"
      provides: "New package with json-render + Zod dependencies"
      contains: "@mbe/rialto-catalog"
    - path: "packages/rialto-catalog/vitest.config.ts"
      provides: "Test configuration for catalog package"
    - path: "packages/rialto-catalog/src/__tests__/catalog.test.ts"
      provides: "Test stubs for catalog verification"
  key_links:
    - from: "packages/rialto-catalog/package.json"
      to: "pnpm-workspace.yaml"
      via: "workspace protocol"
      pattern: "catalog:"
---

<objective>
Upgrade Zod from v3 to v4 across the entire monorepo and scaffold the new `packages/rialto-catalog` package with test infrastructure.

Purpose: Zod v4 is a hard dependency of `@json-render/core`. Upgrading first in isolation ensures existing schemas still pass before any new code is written. The package scaffold establishes the workspace package, dependencies, and test stubs that Plans 02 and 03 build on.

Output: All existing services build with Zod v4, `packages/rialto-catalog` exists with vitest config and test stubs.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-catalog-foundation/12-RESEARCH.md

Source files for Zod migration:
@packages/agent-core/src/pr-creator.ts
@packages/agent-core/src/orchestrator.ts

<interfaces>
<!-- Key Zod usage in existing codebase that must survive the upgrade -->

From packages/agent-core/src/pr-creator.ts (line 9):
```typescript
// BEFORE (v3): z.string().url() — deprecated in v4, must update to z.url()
const GhPrResultSchema = z.object({
  url: z.string().url(),
  number: z.number().int(),
});
```

From packages/agent-core/src/orchestrator.ts:
```typescript
// Uses z.string(), z.object(), z.array(), z.union(), z.enum(), z.optional() — all stable in v4
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade Zod v4 monorepo-wide and fix deprecations</name>
  <files>
    pnpm-workspace.yaml
    packages/agent-core/src/pr-creator.ts
    packages/agent-core/package.json
    services/users/package.json
    services/agent/package.json
    services/reservations/package.json
  </files>
  <action>
1. Add a `catalog:` section to `pnpm-workspace.yaml` pinning `zod: "^4.3.6"`. Then update each of the 4 packages that depend on Zod to use `"zod": "catalog:"` in their `package.json` dependencies:
   - `services/users/package.json`
   - `services/agent/package.json`
   - `services/reservations/package.json`
   - `packages/agent-core/package.json`

2. Run `pnpm install` from the repo root to resolve the new version.

3. Fix the one known deprecation: in `packages/agent-core/src/pr-creator.ts`, change `z.string().url()` (line 9) to `z.url()`. This is the only Zod v4 deprecation in the codebase.

4. Run `pnpm build && pnpm typecheck && pnpm test` to confirm no breakage.

Do NOT use `z.strictObject()` anywhere — the existing `z.object()` usage is correct for v4.
  </action>
  <verify>
    <automated>cd /Users/mbutler/github/mattbutlerengineering && pnpm build && pnpm typecheck && pnpm test</automated>
  </verify>
  <done>All 4 packages use Zod v4 via workspace catalog. `pnpm build`, `pnpm typecheck`, and `pnpm test` pass with zero errors. The `z.string().url()` deprecation in pr-creator.ts is updated to `z.url()`.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold packages/rialto-catalog with test infrastructure</name>
  <files>
    packages/rialto-catalog/package.json
    packages/rialto-catalog/tsconfig.json
    packages/rialto-catalog/vitest.config.ts
    packages/rialto-catalog/src/index.ts
    packages/rialto-catalog/src/__tests__/catalog.test.ts
    packages/rialto-catalog/src/__tests__/drift-check.test.ts
  </files>
  <action>
1. Create `packages/rialto-catalog/` directory structure:
   ```
   packages/rialto-catalog/
   ├── package.json
   ├── tsconfig.json
   ├── vitest.config.ts
   ├── scripts/          (empty, Plan 02 populates)
   ├── src/
   │   ├── index.ts      (placeholder re-exports)
   │   └── __tests__/
   │       ├── catalog.test.ts
   │       └── drift-check.test.ts
   ```

2. `package.json`:
   - name: `@mbe/rialto-catalog`
   - version: `0.1.0`
   - type: `module`
   - private: `true`
   - main/types: `./src/index.ts`
   - exports: `{ ".": { "types": "./src/index.ts", "import": "./src/index.ts" } }`
   - scripts: `generate`, `build`, `test`, `test:drift`, `typecheck`
   - dependencies: `@json-render/core@^0.15.0`, `@json-render/react@^0.15.0`, `@mbe/rialto: workspace:*`, `zod: catalog:`
   - devDependencies: `@mbe/config: workspace:*`, `@types/node@^25.3.0`, `@types/react@^19.2.14`, `tsx@^4.19.0`, `typescript@^5.9.3`, `vitest@^4.0.18`, `react@^19.2.4`

3. Before running `pnpm install`, check React version resolution: run `pnpm why react` and verify the resolved version is 19.2.3+. If it resolves below 19.2.3, add `react: "^19.2.4"` to the package.json dependencies (not just devDependencies).

4. `tsconfig.json`: extend from `@mbe/config/typescript` if that exists, else create a standard strict config targeting ES2022 with ESNext modules, `noEmit: true`.

5. `vitest.config.ts`: standard vitest config with `test: { globals: true }`.

6. `src/index.ts`: placeholder with a comment `// Exports added in Plan 02 and Plan 03`.

7. Test stubs — write as `describe` blocks with `it.todo()` for each expected behavior:
   - `catalog.test.ts`: stubs for `catalog.prompt()` returns non-empty string, contains Button/Card, contains validateForm/navigate, contains setState (built-in)
   - `drift-check.test.ts`: stub for deterministic generation (run twice, diff is empty)

8. Run `pnpm install` from repo root, then `cd packages/rialto-catalog && pnpm test` to confirm vitest runs (all tests should be pending/todo).
  </action>
  <verify>
    <automated>cd /Users/mbutler/github/mattbutlerengineering && pnpm install && cd packages/rialto-catalog && pnpm test</automated>
  </verify>
  <done>`packages/rialto-catalog` exists as a workspace package. `pnpm install` resolves all dependencies including `@json-render/core`, `@json-render/react`, and `zod@^4.3.6`. Vitest runs and reports pending test stubs. `pnpm build` from root still succeeds.</done>
</task>

</tasks>

<verification>
1. `pnpm build` succeeds monorepo-wide (Zod v4 + new package)
2. `pnpm typecheck` passes across all packages
3. `pnpm test` passes (existing tests + new pending stubs)
4. `pnpm why zod` shows a single v4.x version across all packages
5. `packages/rialto-catalog` appears in `pnpm list --depth 0`
</verification>

<success_criteria>
- Zod v4 is the sole version across the monorepo enforced by workspace catalog
- The one deprecation (`z.string().url()`) is fixed
- `packages/rialto-catalog` exists with correct dependencies and test stubs
- All existing tests pass — zero regressions from the Zod upgrade
</success_criteria>

<output>
After completion, create `.planning/phases/12-catalog-foundation/12-01-SUMMARY.md`
</output>
