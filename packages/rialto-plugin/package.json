{
  "name": "@mbe/rialto-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Claude Code plugin for the Rialto design system — skills, agent, and validation hook",
  "scripts": {
    "generate": "tsx scripts/generate-reference.ts",
    "build": "pnpm generate",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mbe/rialto": "workspace:*"
  },
  "devDependencies": {
    "@mbe/config": "workspace:*",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
