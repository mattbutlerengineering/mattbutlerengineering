import { describe, it } from "vitest";

describe("drift-check: catalog generation is deterministic", () => {
  it.todo("running catalog generation twice produces identical output (diff is empty)");
});
