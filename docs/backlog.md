# Seed backlog

Advisory inbox for ideas, not run state. Ordering is the prioritization —
top of file is proposed first. One line per seed.

- Add a contract test that pins every `@mbe/api-client` URL to a route the owning service actually registers (build the Fastify app, `printRoutes()` or `fastify.routes`, diff against the client's `/api/v1/...` literals) — `FloorPlansClient.setActive` posted to `/floor-plans/:id/active` and `bulkUpdatePositions` to `/floor-plans/:id/bulk-update-positions` with a bare array, while the reservations service registers `/:id/activate` and `/tables/positions` with `{ floorPlanId, positions }`; each side's own tests pinned its own path, the E2E mocks mirrored the client, and the deployed API answered 404 to both (probed 2026-08-30 via 1.1.1.1), so "Set as Active" and every position save in the floor-plan editor failed in production with every gate green. Fixed client-side in the same session; the class (two independently-pinned halves of one contract) is what the test would close (from: session:2026-08-30)
- Dispatch `rialto-visual-noise-floor.yml` (one `workflow_dispatch` on `main`, ~12 paid runner-minutes: three parallel ~3-minute capture legs plus analyze) and give `packages/rialto/playwright.visual.config.ts` the measured `threshold`/budget pair `scripts/visual-tolerance-rule.mjs` recommends, with `// noise-floor:` provenance lines and a drift guard mirroring `apps/rialto-web/playwright.config.ts` — the Storybook suite still runs at Playwright's inherited `threshold: 0.2`, blind to a +36/255 whole-image shift on all 53 baselines. PR #4654 shipped the harness only: its provisional `threshold: 0.01` was measured red by the suite's own CI (run 33218366634 — `specialty-commandpalette--open` 31,167 noise pixels = 0.04 ratio, `overlay-dialog--default` 631 = 0.02, both over `maxDiffPixelRatio: 0.01`), so the number must come from the measurement, never a guess; the reproduction binding test to resurrect alongside the re-tune is in commit e55ef7d3a. The measured threshold alone CANNOT close 4 sparse-on-white baselines (data-display-timeline, data-display-tree, layout-stack, specialty-scrollarea) that a RATIO budget is blind to at any threshold — that needs the budget-form change the rule's clause 4 reviews (from: maintenance:visual-tolerance-threshold)
- Fix the CSP-blocked font preload in `apps/rialto-web/index.html` — the inline `onload="this.rel='stylesheet'"` violates `script-src`, so the Google Fonts stylesheet is never promoted, `document.fonts.size` is 0 in production, and the design system's own showcase renders in `system-ui` instead of DM Sans / Bricolage Grotesque (from: feature:rialto-game-ui) (claimed: maintenance:rialto-web-fonts)
- Decide whether production gets a Redis instance at all — `REDIS_URL` is unset in prod, so booking reminders, waitlist expiry, and post-visit notifications have never been scheduled there; the service boots degraded by design and nothing tracks the decision (from: #3763)
- Improve `human_touch_reason` classifier precision — 10 of 11 in-window classifications land on `other`, so the taxonomy is live and correct but says almost nothing about _why_ a human intervened (from: #4241)
- Pass `VITE_SENTRY_DSN` to the rialto-web and marketing builds in `deploy-static.yml` — only the hospitality build receives it, so both other sites ship the Sentry SDK with `enabled: false` and report nothing (from: feature:rialto-game-ui) (claimed: maintenance:sentry-dsn-static-builds)
- Give rialto-web any usage instrumentation at all — the deployed document carries exactly one script tag (the app bundle), no beacon and no edge injection, so "did anyone use this route" is unanswerable for every feature run against this app, not just early (from: feature:rialto-game-ui) (claimed: maintenance:rialto-web-usage-instrumentation)
- Make the cookie banner's analytics toggle gate something, or drop it — every reference to the `analytics` preference lives inside `CookieConsent/`, so visitors are asked to consent to a category the site does not have (from: feature:rialto-game-ui)
- Assert the accessibility tree of a running page as a gate, not just axe rules — both majors in the game-UI run (duplicate status announcement, meters reporting a false 0) were invisible to axe, 2707 unit tests, typecheck, and lint (from: feature:rialto-game-ui)
- Extend the 100ms latency budget from one route to the whole catalog — `SegmentedControl`'s untokenized `0.15s` existed everywhere and was only ever caught because one route measured computed style (from: feature:rialto-game-ui)
- Give `Meter` an indeterminate state — `role="meter"` requires `aria-valuenow`, so a meter with no data currently reports a definite 0 where the truth is "unknown" (from: feature:rialto-game-ui) (shipped: #4347)
- Fix `SegmentedControl` dropping a caller's `aria-label` — it spreads `{...props}` onto its wrapper while `role="radiogroup"` sits on an inner element, so the group is unnamed for every consumer (from: feature:rialto-game-ui) (shipped: #4347)
- Make demo routes clear DemoLayout's fixed floating controls by default — the overlap is structural, and telemetry was simply the first route to put content in that band (from: feature:rialto-game-ui)
- Catch tests that pass against unfixed code because of module-state caching — the `matchMedia` regression test passed 8/8 before the fix and would have shipped asserting nothing (from: feature:rialto-game-ui)
- Offer the `game` vibe in DemoLayout's own vibe switcher — it hand-rolls three `<option>`s and `game` is not among them (from: feature:rialto-game-ui)
- Document that explicit `vibeOverrides` outrank the reduced-motion adapter, so a consumer can re-impose motion on a user who asked for less (from: feature:rialto-game-ui)
- Cover the `hold` feed state at route level — the hook branch is tested, the rendering is not (from: feature:rialto-game-ui)
- Close the tracker loop on every run — `idea.md` records `origin: github-issue #3978` in one direction only, so the run consumed the issue, shipped, and left it with zero comments and its human-verdict gate unanswered (from: feature:rialto-game-ui)
- Make deferred findings leave the run directory as issues — `review.md` deferred 2 majors and 3 minors and `release.md` lists 3 open items, none of which exist as GitHub issues, including two real defects in shipped components that affect every consumer (from: feature:rialto-game-ui)
- Resolved, not a defect — the Cloudflare Insights beacon is fine, and the `ERR_CONNECTION_REFUSED` was a LAN-level DNS sinkhole on the probing machine: measured 2026-08-21, `dig +short static.cloudflareinsights.com` returns `0.0.0.0` through the LAN resolver `192.168.4.40` but `104.16.80.73` / `104.16.79.73` through `@1.1.1.1`, and `curl --resolve static.cloudflareinsights.com:443:104.16.80.73` returns HTTP 200 with real JavaScript; the edge-injected tag is correctly nonced and CSP does not block it. Whether events reach the Cloudflare Web Analytics dashboard was not verified — that needs dashboard access. (from: feature:rialto-game-ui) (claimed: maintenance:e2e-behind-edge-csp)
- Extend the post-deploy smoke suite to the routes features actually ship — `tests/smoke/smoke.spec.ts` hits `/rialto` but no `/rialto/demos/*` route, so a green Post-Deploy Check says nothing about a newly shipped demo (from: feature:rialto-game-ui)
- Give a human-verdict acceptance criterion an owner, a notification, and a deadline — PRD-7 was the sole gate for this run's central claim and sat unanswered 28 hours after ship with nothing tracking it (from: feature:rialto-game-ui)
- Add a live-surface probe to Ship's post-release step — 15 minutes of probing the deployed page found a CSP violation and zero loaded web fonts, neither of which any local gate can see; a third apparent finding, a dead analytics beacon, proved on 2026-08-21 to be a LAN DNS artifact on the probing machine rather than a production fault (from: feature:rialto-game-ui)
- Remove or guard the duplicated font block in `apps/*/index.html` — marketing and rialto-web now carry byte-identical 20-line font setups, Vite HTML entries have no include mechanism, and nothing detects drift between them, which is the same maintained-in-one-place-not-the-other shape that caused the rialto-web outage (from: maintenance:rialto-web-fonts)
- Run at least one E2E pass behind the real edge CSP — `apps/rialto-web/playwright.config.ts` serves the suite with `vite dev` and no `Content-Security-Policy` header, so every CSP-caused defect (the zero-web-fonts outage, the blocked Sentry envelopes) is invisible to visual and functional E2E by construction, not by oversight (from: maintenance:rialto-web-fonts) (claimed: maintenance:e2e-behind-edge-csp)
- Sweep sibling apps when fixing an app-specific bug, and write the regression guard at the FIRST fix — #3149 fixed the inline-`onload`-under-nonce-CSP defect in `apps/marketing` on 2026-07-04 while `apps/rialto-web` had carried the identical bug since 2026-06-14, and the guard written six weeks later would have gone red immediately had it existed then (from: maintenance:rialto-web-fonts)
- Give CSP refusals a server-side trace — a violation is client-side-only (no 4xx, no log, and Sentry's own envelopes are CSP-blocked), so the zero-web-fonts outage survived nine weeks with every gate green; a `report-to`/`report-uri` endpoint on the edge worker would make refusals observable without a human opening devtools (from: maintenance:rialto-web-fonts)
- Alert when a Sentry project receives zero events for N days — the marketing/rialto-web outage ran 4.5 months and its only symptom was silence, which is indistinguishable from health; every other gate was green throughout (from: maintenance:sentry-dsn-static-builds)
- Make a change to a deploy workflow deploy itself — `deploy-static.yml`'s push trigger filters on `apps/**` and a few packages but not `.github/**`, so a fix to the deploy pipeline is exactly the class of change that never ships on merge, and "PR merged, CI green" reads identically to "live in production" (from: maintenance:sentry-dsn-static-builds)
- Prove the Sentry round trip end to end once per app — the deployed bundles now carry a well-formed, CSP-allowed DSN, but no error has ever been deliberately thrown to confirm an envelope is actually ingested, so "can report" is verified and "does report" is not (from: maintenance:sentry-dsn-static-builds)
- Confirm source maps upload and produce readable stack traces — `sentryVitePlugin` self-disables via `disable: !process.env.SENTRY_AUTH_TOKEN`, so the token is now supplied, but nothing has verified a symbolicated trace actually appears rather than a minified one (from: maintenance:sentry-dsn-static-builds)
- Audit `deploy-static.yml` for other per-app build-env drift — the Sentry set was one instance of a general shape, and `VITE_BUILD_ID` is currently passed to marketing alone with no recorded reason why the other two static apps don't need it (from: maintenance:sentry-dsn-static-builds)
- Route every ad-hoc CI-rollup poll through `classifyCiGateStatus()` rather than hand-rolling a filter — a hand-rolled one omitted `IN_PROGRESS` and briefly read "still running" as "never ran", two states with opposite correct responses (from: feature:tape-chart-overlaps)
- Never let `git push` reach a pipe — `git push … | tail` masks the exit code, and a failed push twice reported success in one session, surfacing only when a later `gh pr create` failed with "No commits between" (from: feature:tape-chart-overlaps)
- Give pipeline stage agents a liveness contract — three died mid-stage in one run (two stall-watchdog, one API quota), and a stalled reviewer fails open: it returns nothing and is indistinguishable from a clean pass (from: feature:tape-chart-overlaps)
- Make a human-judgement acceptance criterion block the merge it gates, or drop it — G2 was written into the tape-chart PRD, could not be checked pre-merge because preview URLs were broken, and was still unanswered at run close (from: feature:tape-chart-overlaps)
- Give the TapeChart overlaps work a changeset before the next rialto release — `e4c30808` shipped a public `classifyOverlap` prop, an `overlap` field on every positioned bar, and a row-height behavior change with no `.changeset/` entry, so the next publish carries six unrelated changelog entries and documents none of it (from: maintenance:e2e-behind-edge-csp)
- Fail the release when a `packages/rialto/src/**` change reaches `main` with no changeset — the TapeChart gap was found by hand while writing an unrelated ship record, which is not a detection mechanism (from: maintenance:e2e-behind-edge-csp)
- Audit every CI guard's `paths:` filter against the directory it guards — F1 was a CSP test that would not have run when the CSP changed, and that omission is invisible in a green run (from: maintenance:e2e-behind-edge-csp)
- Make "prove the guard fails" a required step for any new test that exists to catch a specific defect class — it took minutes here and is the only thing separating a guard from a decoration (from: maintenance:e2e-behind-edge-csp)
- Split the CSP suite into its own CI job, or emit an explicit skipped-coverage signal, so a flake in an earlier step cannot silently remove CSP coverage while the result still reads as a normal red (from: maintenance:e2e-behind-edge-csp)
- Stand up a database-backed test for the venue-bootstrap transaction — `Serializable` is the part of the design doing the actual concurrency work and the part with the weakest evidence: mocked Prisma proves the isolation level is _requested_, never that Postgres aborts a real racing transaction (from: feature:first-venue-self-serve)
- (claimed: maintenance:nonadmin-journey-identity) Provision the non-admin journey identity and set `E2E_NONADMIN_AUTH_EMAIL` / `E2E_NONADMIN_AUTH_PASSWORD` — until then the bootstrap journey case fails loudly on every scheduled run, which is deliberate but leaves the feature's only production-level check red by design (from: feature:first-venue-self-serve)
- (done 2026-08-23: self-signup is ENABLED — see ADR-020) Confirm whether the Auth0 tenant permits open self-signup, and record the answer in ADR-020 — it decides whether "any authenticated identity may create one venue" means "any invited operator" or "anyone on the internet", and the whole risk assessment for this feature hinges on it (from: feature:first-venue-self-serve)
- Teach the pack generator to keep a file's primary export when a smaller one is added — adding `VENUE_CREATE_RATE_LIMIT` to `venues.ts` displaced the entire inlined `venueRoutes` body from `llms-full.txt` (-663 lines), so a routine export silently degrades the context pack with no signal (from: feature:first-venue-self-serve)
- Add a guard that fails when a preHandler reads state the request then mutates — the bootstrap guard reads membership outside the transaction that creates it, a check-then-act shape that is invisible to lint, typecheck and every single-request test (from: feature:first-venue-self-serve)
- Assert that every publicly-reachable route still carries `x-ratelimit-*` on its unauthenticated responses — declaring a route-level `config.rateLimit` silently displaces the service-wide limiter, and the loss is invisible to lint, typecheck, and every single-request test (from: feature:first-venue-self-serve) (done 2026-08-23: `scripts/check-api-surface-invariants.mjs`, PR #4505)
- Probe the deployed surface, not just the merged diff, as a pipeline stage — the venue-create rate-limit regression passed CI, review, and a closed retro, and was only found by curling the live route and noticing an absent response header (from: feature:first-venue-self-serve) (done 2026-08-23: `API Surface Invariants` job in post-deploy-check.yml, PR #4505)
- Reconcile the DigitalOcean app spec with `infrastructure/pulumi/index.ts` and remove the residual `ignoreChanges` narrowing — every spec field still ignored (env vars, instance sizes, component config) is silently unmanaged, exactly as ingress was (from: maintenance:public-ingress-never-applied)
- Move `deploy-services.yml`'s CI precondition check to run _after_ `Wait for CI` fails rather than once at job start, and give `absent` its own recovery (dispatch `ci.yml` on the ref, per #3969) instead of only handling `cancelled` — a point-in-time probe taken before the wait cannot classify a cancellation that happens _during_ the wait, and on 2026-08-24 the probe returned `absent` for a SHA whose CI run had already existed for 5 minutes and was cancelled 45s later (from: session:2026-08-24)
- Guard `pulumi-up.yml`'s `Pin Pulumi CLI` step with `set -o pipefail` — `curl -fsSL https://get.pulumi.com | sh -s -- --version 3.253.0` runs under GitHub's default `bash -e` (no pipefail), so a failed curl leaves `sh` exiting 0: the step goes green having installed nothing, `$HOME/.pulumi/bin` is added to PATH empty, and the later `run:`-based `pulumi cancel` / `stack export | stack import` preamble falls through to the runner image's floating CLI — reintroducing the exact 3.256.0 R2 `InvalidDigest` outage the pin exists to prevent, on the production deploy path (from: maintenance:public-ingress-never-applied)
- Make a stranded visual-regression comment recoverable — GitHub evaluates a `pull_request` `paths:` filter against the PR's cumulative `base...head` diff, so a PR that backs out its whole `apps/rialto-web/**` / `packages/rialto/src/**` / `infrastructure/worker/**` surface after a failing visual run stops triggering `rialto-web-e2e.yml` at all, and the failure comment stands forever claiming a regression that no longer exists (observed live on #4569 at `d9ce41471`) (from: feature:visual-diffs-in-pr)
- Bring `scripts/**` inside a lint gate — it is unlinted for two independent reasons, and closing either alone does nothing: `scripts/package.json` (`@mbe/scripts`) declares no `lint` script so `turbo run lint` never visits it, and `lint-staged.config.js` routes `.mjs` through `PRETTIER_ONLY_GLOB` with no `eslint --fix`; the four modules this run added are checked by nothing but their own tests, so an orphaned import or unused variable in them goes unnoticed (from: feature:visual-diffs-in-pr)
- Author-filter the sticky-comment lookup in `.github/workflows/preview-deploy.yml` — it selects `.[] | select(.body | contains("Preview Deployed")) | .id` and takes the oldest match with no author check, so any user can pre-post a matching comment and have every later run PATCH theirs instead of posting its own; this is the same gap review finding F4 fixed in the visual-diff publisher (from: feature:visual-diffs-in-pr)
- Revisit the rialto-web visual tolerance with Playwright's per-pixel `threshold` in view, not just `maxDiffPixels` — `threshold` gates before the budget is ever consulted, so a perturbation changing 71,140 of 139,216 pixels in `light-button-variants` passed all 49 snapshots (largest per-channel delta 36/255 = 0.14, under the 0.2 default); #4496 tuned the budget from a ratio to an absolute 300 without examining the gate sitting in front of it (from: feature:visual-diffs-in-pr) (claimed: maintenance:visual-tolerance-threshold) (done 2026-08-28: `threshold: 0` + `maxDiffPixels: 674`, measured from run 33107801311; PR #4613)
- Apply the `testTimeout: 15000` the gotchas file already prescribes to the packages that tip over under full-parallel `pnpm test` locally — the failing member drifts run to run (rialto and marketing in Implement, service-bootstrap in Verify; all green in isolation and at `--concurrency=4`) and none of `packages/rialto`, `packages/service-bootstrap` or `apps/marketing` sets it, so a local test run reports on load rather than on the change under test (from: feature:visual-diffs-in-pr)
- Promote inline visual-diff comments to the `packages/rialto` Storybook visual suite (`src/test/visual/visual.spec.ts`, driven by `rialto-visual.yml`) — this run scoped itself to rialto-web and named the promotion out of scope rather than taking it; the report parser and comment renderer are already suite-agnostic (the budget's config path comes from the report's own `config.configFile`), so what is missing is the per-workflow wiring (from: feature:visual-diffs-in-pr)
- Declare the real inputs of `test` and `test:coverage` in `turbo.json` — `test:coverage` declares none at all, so `@mbe/scripts`'s task hash is byte-identical before and after a mutation that breaks either of the two visual-tolerance guards, both of which read `apps/rialto-web/playwright.config.ts` from outside their own package; their soundness in CI is incidental rather than designed, resting on `test` declaring `needs: typecheck` so typecheck saves the `github.sha`-keyed `actions/cache` entry first and `actions/cache` then declines to save from `test` (measured 16/16 build cache hits vs 0/29 `test:coverage` hits across five `main` runs), and either setting `TURBO_TOKEN` or dropping that `needs` silently reopens the hole with nothing going red to announce it (from: maintenance:visual-tolerance-threshold)
- Make the chaos loop falsifiable before trusting it again — the planter has failed 3 consecutive scheduled runs since 2026-08-17 (#4287, open 11 days), and its last successful plant (#4566, a `// FIXME:` in `WalkInDialog.tsx`, 2026-08-25) sat 3 days with zero detection and no issue filed; no `no-warning-comments` rule exists anywhere in the repo, so ESLint structurally cannot flag the marker the experiment plants, which makes a green chaos run indistinguishable from a detector that does not exist (from: session:2026-08-28)
- Teach `scheduled-workflow-health.yml` to see a workflow that is green but hollow — it classifies purely on consecutive `failure` conclusions, so the two worst cases found this session were invisible to it by construction: `nightly-compliance.yml` reported `success` for 100 days while its `✗ FAILED` branch was unreachable (GitHub's default `bash -e` aborts on the failing `pnpm` before `cmd_status=$?` runs, so the drift detector's `grep -q '✗'` never matched), and `changelog.yml` reported `success` while `git diff --quiet CHANGELOG.md` on an untracked file exited 0 and short-circuited its create-PR step, producing no changelog PR in its entire history; a green run that does nothing is the failure mode the fleet watchdog most needs to catch and the one it cannot (from: session:2026-08-28)
- Scale the scheduled-health threshold to each workflow's cadence — `DEFAULT_THRESHOLD = 3` consecutive failing runs means a monthly workflow (`changelog`, `cors-audit`, `dependency-freshness`, `audit-scout`, `reflection-review`) needs three months to trip, and #4502's `awaiting-rerun` suppression resets that clock on any touch to the file, so a monthly job can be broken indefinitely without ever being flagged; `changelog.yml` sat broken from 2026-07-01 with no issue filed, while `resource-audit.yml`'s identical 5-failure streak was correctly suppressed as `awaiting-rerun` after its 2026-08-24 fix — the mechanism works, the window does not fit the cadence (from: session:2026-08-28)
- Add `plugins/acmm/scripts/evals/**` to `instruction-regression.yml`'s trigger paths — the workflow's entire eval harness lives outside its `CLAUDE.md`/`AGENTS.md`/`GEMINI.md` filter, so a PR changing the harness runs no eval and a broken harness lands silently, going red only on the next unrelated instruction-file edit with the wrong PR blamed; found and allowlisted by `scripts/check-workflow-paths-coverage.mjs` (from: session:2026-08-28)
- Add `scripts/publish-visual-diffs.mjs` to `rialto-web-e2e.yml`'s trigger paths — the visual-diff publisher runs under `contents: write` in that workflow yet a PR changing it triggers no visual run, so a publisher regression ships unexercised; found and allowlisted by `scripts/check-workflow-paths-coverage.mjs` (from: session:2026-08-28)
- Add `scripts/preview-comment.mjs` to `preview-deploy.yml`'s trigger paths — a PR changing the sticky-comment lookup deploys no preview and exercises nothing, the same shipped-never-run shape as #4119/#3695; found and allowlisted by `scripts/check-workflow-paths-coverage.mjs` (from: session:2026-08-28)
- Add `scripts/deploy-ci-precondition.mjs` to `deploy-services.yml`'s and `scripts/collect-repo-stats.mjs` to `deploy-static.yml`'s trigger paths (or move both under a filter-covered directory) — each deploy workflow shells into a helper its own `paths:` can never fire on, so a helper change reaches `main` without ever running the deploy that depends on it; found and allowlisted by `scripts/check-workflow-paths-coverage.mjs` (from: session:2026-08-28)
- Re-derive `changelog.yml`'s `FROM_REF` from the run date rather than `date -d '1 month ago'` — the expression resolves to the _preceding calendar month_ only when the job runs near the 1st, so the `workflow_dispatch` escape hatch (this workflow's only recovery path, and the one used to verify #4658) is broken on most days: measured on `origin/main` at 2026-08-28, a dispatch resolves boundary 2026-07-01 → 993 commits → 1,181,579 log bytes, over the 900,000-byte `MAX_SAFE_BYTES` guard, so the job exits 1 by design; the scheduled 2026-09-01 path resolves 2026-08-01 → 647 commits → 736,580 bytes and passes with only 22% headroom, so at this repo's ~650 commits/month the scheduled path itself is one or two months from tripping the same guard (from: session:2026-08-28)
- Repair `regen-after-update-branch.sh`'s input the way the three Bash hooks were repaired (adapter via `hook-input.mjs command`) — it is the last hook still reading nonexistent env vars (`CLAUDE_TOOL_INPUT`/`CLAUDE_TOOL_OUTPUT`, both measured at zero occurrences in the installed binary), deliberately left inert because lines 36-58 run never-executed `git checkout origin/$branch --detach` + `git push --no-verify` against the live working tree; rewrite those lines first, then wire the adapter (from: session:2026-08-28)
- Close the docs-only prettier gap in `ci.yml` — `detect-changes`' `has_code == 'false'` path skips the entire Build chain including `pnpm check:prettier`, so a docs-only PR can land prettier-dirty files that redden every subsequent full Build and main push (measured 2026-08-31: #4787's six unformatted run docs simultaneously failed #4790/#4792/#4797 and main's `a4473b0f9`, fixed by `27fa27736`; recovery needed `gh pr update-branch` because `gh run rerun --failed` re-uses the pre-fix merge SHA); add a cheap docs-lane job that runs `prettier --check` on the changed files when `has_code` is false and wire it into `CI Gate`'s `needs` (from: session:2026-08-31)
- Reconcile `CORS_ORIGIN` (singular, what the DigitalOcean app spec sets on all three services) with `CORS_ORIGINS` (plural, what `packages/service-bootstrap/src/create-service-app.ts:141` reads) — the configured value is silently discarded and the hardcoded `defaultOrigins` list is used instead, so CORS is currently unconfigurable at deploy time and only appears to work because the defaults happen to include the production origin; the same class as the DSN gap it was found beside (configuration that ships and never takes effect), and equally invisible because the fallback is correct today (from: maintenance:backend-observability-blackout)
- Run `scripts/sentry-round-trip.mjs`'s `main()` from CI, where `SENTRY_AUTH_TOKEN` exists — the script was written this run as the observation that distinguishes "no errors yet" from "reporting is dead", and its decision helpers are unit-tested, but the entrypoint itself has still never executed once; a round-trip checker that has never round-tripped is the same shipped-never-run shape as #4119/#3695, and it is guarding the exact failure class (silence reading as health) that let this blackout run five months (from: maintenance:backend-observability-blackout)
- Teach `scripts/check-deploy-secret-provisioning.mjs` to see `SENTRY_DSN`, which it misses for two independent reasons — closing either alone changes nothing: (1) `findRequiredProductionSecrets` scans only `services/reservations/src/config` and returns `['MANAGE_TOKEN_SECRET','UNSUBSCRIBE_TOKEN_SECRET']`, while `SENTRY_DSN` is required by `packages/service-bootstrap/src/validate-startup-config.ts`, so it never enters `names`; and (2) `isSecretProvisioned` matches one exact name against all three markers, but the GitHub secret and the app-spec key deliberately differ (`secrets.SENTRY_DSN_USERS_API` upserted as `"key":"SENTRY_DSN"`), so `isSecretProvisioned("SENTRY_DSN")` fails on `hasPassthrough` and `isSecretProvisioned("SENTRY_DSN_USERS_API")` fails on `hasUpsert` — both measured false against the live workflow on 2026-09-03 while the secret is in fact correctly provisioned (from: maintenance:backend-observability-blackout)
- Give `packages/sentry/src/react.ts` the `beforeSend` redaction the Node path got this run — `redactSignal` is wired into the backend init only, so the browser SDK ships every event unredacted; the asymmetry is invisible because both paths look configured and neither errors (from: maintenance:backend-observability-blackout)
- Replace the vanish-when-unconfigured env shape at `infrastructure/pulumi/index.ts:62-64` with one that fails loudly — `...(otelEndpoint ? [extraEnv(...)] : [])` means an unset Pulumi config silently emits no `OTEL_EXPORTER_OTLP_ENDPOINT` at all, which is byte-for-byte how `SENTRY_DSN` went missing from the app spec for five months; the M2 traces work will land on exactly this line, so fix the shape before it has a second chance (from: maintenance:backend-observability-blackout)
- Give `/public/**` a production ingress rule — the app spec has no route for it, so the surface is unreachable in production while appearing correctly configured in the repo; found while dumping the live spec during Verify, unrelated to the DSN gap but the same "config that ships and never takes effect" class, and large enough to want its own maintenance run (from: maintenance:backend-observability-blackout)
- Convert all five `yq` secret upserts in `.github/workflows/deploy-services.yml` to `strenv()` — each interpolates a secret directly into the yq expression string (`\"value\":\"${SENTRY_DSN_USERS_API}\"`), so a DSN containing a quote or backslash breaks the expression rather than the value; deferred as a Review minor because the current values are URL-shaped and the same pattern predates this run at every site, but it is one rotated credential away from a confusing deploy failure (from: maintenance:backend-observability-blackout)
- Make `validateStartupConfig` collect every failure before throwing instead of throwing on the first — this run added the `SENTRY_DSN` check ahead of the existing `AUTH_AUTHORITY` one, so a service misconfigured in both ways now reports only the DSN and the operator fixes one, redeploys, and discovers the second; deferred as a Review minor because each individual message is correct (from: maintenance:backend-observability-blackout)
- Close the class `scripts/check-workflow-paths-coverage.mjs` structurally cannot see — a workflow whose `paths:` filter omits a package the deployed artifact _boots with_; the checker derives coverage from path tokens appearing in `run:` blocks, `pnpm --filter`, `pnpm --dir` and `working-directory:`, and its own header states transitive dependencies are not modeled, so `deploy-services.yml` never naming `packages/sentry` read as fully covered while #4927 merged green and deployed nothing; the fix landed by hand in #4931, but nothing prevents the next package from re-opening it (from: maintenance:backend-observability-blackout)
- Open the follow-up run for Milestones 2 and 3 (traces and logs to Grafana Cloud) once a Grafana Cloud account exists — `architecture.md` was deliberately left unedited as that run's input, and `verification.md`'s single FAIL (T2, "a successful request leaves a durable record") is its acceptance criterion; until then a successful production request still leaves no durable trace anywhere, because the DigitalOcean log window is destroyed by every deploy (from: maintenance:backend-observability-blackout)
- Stop the OTLP exporters defaulting to `localhost:4318` in production — `packages/observability/src/sdk.ts:76-83` constructs `new OTLPTraceExporter()` and `new OTLPMetricExporter()` with no arguments, so both fall back to the OTel default `http://localhost:4318` while `OTEL_EXPORTER_OTLP_ENDPOINT` is absent and `OTEL_SDK_DISABLED` is unset, and the `PeriodicExportingMetricReader` retries every 30s against nothing; observed live as `RESERVATIONS-API-7` (`connect ECONNREFUSED ::1:4318` -> `127.0.0.1:4318`, unhandled rejection ~45-60s after each boot) within three hours of error reporting being restored — the interim fix is `OTEL_SDK_DISABLED=true` until a real collector exists, the real one is to construct the exporters only when an endpoint is configured (from: maintenance:backend-observability-blackout) (claimed: maintenance:otlp-localhost-default)
- Explain the `/ready` 503s on long-running containers — `RESERVATIONS-API-8` fired at 01:47:55 on a container whose `app_start_time` is 00:32:42, so 75 minutes after boot and not a start-up race, and `USERS-API-1` is the same shape on users-api; both were invisible for the whole blackout and appeared within hours of reporting being restored, so the frequency before 2026-09-03 is unknown and the readiness probe may have been flapping in production for months (from: maintenance:backend-observability-blackout)
- Give Capture-stage factual claims the evidence bar Verify already enforces — this run produced two corrections to confident sentences that nobody was required to check, and both named the right mechanism while never checking whether the path was populated: `docs/fixes/sentry-dsn-static-builds/defect.md`'s "all backend services not affected", and this run's own "zero events since ~2026-04-02" (contradicted by `USERS-API-1`'s First Seen of 2026-05-19, mid-blackout, now past the 90-day event window and unrecoverable); a brief's headline number is load-bearing for scope and it currently ships unverified (from: maintenance:backend-observability-blackout)
- Give `packages/rialto/playwright.visual.config.ts` the measured `threshold`/`maxDiffPixels` pair `apps/rialto-web` got in #4613 — it carries the identical gap (`maxDiffPixelRatio: 0.01`, `threshold` unset, so Playwright's default 0.2 applies and a uniform per-channel shift up to 52/255 reaches the budget as a count of zero), it is demonstrably live (#4913 regenerated `data-display-table--default.png` on 2026-09-02), and it is the one suite `defect.md` named and deliberately did not take; re-run `scripts/visual-tolerance-rule.mjs` against a noise-floor artifact for that suite rather than copying rialto-web's 674, which was measured for a different snapshot set (from: maintenance:visual-tolerance-threshold)
- Make prepare-and-stop runs reconcilable — `docs/fixes/visual-tolerance-threshold/release.md` claimed "PREPARED, NOT RELEASED" for six days after #4613 merged on 2026-08-28, because Ship was authorized to stop at the PR and no stage owns writing a later human merge back into the artifact; `docs/backlog.md` was updated on the merge day and the run directory was not, so the two disagreed with nothing to notice, and the plausible reading of the artifact was the wrong one — either Operate re-derives release state from the PR instead of trusting `release.md`, or something reconciles the two (from: maintenance:visual-tolerance-threshold)
- Give the hospitality dashboard an authenticated production smoke check — `feature:hospitality-animations` shipped its headline deliverable (the NeonSign in the dashboard header) with **zero** production evidence, and this was accepted at PRD time ("success is provable only by tests and the showcase") rather than noticed as a gap; the rialto-web showcase was verifiable live because it is public, so a green showcase reads as confirmation of a consumer nobody has ever observed, and any future feature living behind Auth0 inherits the same blind spot (from: feature:hospitality-animations)
- Reconcile a `/decompose` tracking issue's checkboxes against the real state of the issues it references — #4746 still showed #4738 unchecked two days after that issue closed, and nothing detects the drift in either direction, so a tracking issue can under- or over-report completion indefinitely; the run's own artifacts are the state per ADR-0026, but the tracker is what a human reads (from: feature:hospitality-animations)
- Resolve what is actually published as `@mattbutlerengineering/rialto` and cut a release for the pending changesets — `.changeset/` holds 22 unconsumed entries against a `package.json` still at `0.2.0`, including two `major` ones (`DateRange` ISO migration, `AuthMascot` removal), so no version carrying any recent component has been cut; registry state is currently undeterminable rather than known-bad (`npm view` → 404 but `npm whoami` → 401, and npm answers 404 not 403 for private scoped packages to unauthenticated clients), so step one is restoring npm auth and re-measuring, not assuming (from: feature:hospitality-animations)
- Consult `hasAuthParams` in `apps/hospitality/src/App.tsx`'s `isLoading` branch so the post-logout restore beat stops flashing "Verifying your sign-in / Exchanging your code for a session" for a beat in which no exchange exists — the guard already exists one branch lower (`isCallback && !isAuthenticated`), so the fix is `hasAuthParams(window.location) ? <CallbackPage /> : <LoadingPage />`; deferred as review minor m1 and re-confirmed still open on `origin/main` at `5a302deed` (from: feature:auth-handshake-flows)
- Branch `AuthFailurePage`'s copy on the failing navigator so a failed sign-**out** stops telling the operator their sign-in failed — img label, `describeAuthError` default title/body and the "Try again" action are sign-in-specific on every path, while `App.tsx` routes any interactive error there including `signoutRedirect`; deferred as review minor m2, whose deferral rationale ("pair it with the tenant-verification step, the same path's likeliest trigger") is now measured false — the tenant's discovery document does expose `end_session_endpoint` (from: feature:auth-handshake-flows)
- Resolve the refresh-banner in-flight inconsistency (review minor m3) one way or the other — `DashboardLayout`'s "Sign back in" button has no `isLoading`/`loadingText`, so it is the one remaining feedback-less sign-in-bound click on a run whose whole theme is that no click feels dead, while ux.md's S5 table still assigns it `LAPSE_ACTION_BUSY` and sketches the busy beat; either wire `isLoading={activeNavigator === "signinRedirect"}` or give S5 the dated amendment D2 got (from: feature:auth-handshake-flows)
- Export `SIGN_IN_PHASES` from `apps/rialto-web/src/pages/auth/SignIn.tsx` and pin `ariaLabel !== status` over it with the `describe.each` table #4772 added for `SIGN_UP_PHASES` — the sibling table is still module-private and unpinned, so a copy edit collapsing a SignIn pair reproduces the exact criterion-11 defect Verify caught, with the suite green; notable that #4828 and #4834 both rewrote this file after the gap was documented and neither added the pin (from: feature:auth-handshake-flows)
- Collapse the three parallel hand-maintained station orderings in the auth-flow walkthrough and make its Handshake label phase-aware (review minors m5 + m6) — `STATIONS` still documents itself as "in display order (start → end)" while display order comes from `HANDSHAKE_STATIONS` and the legend is driven by a third ordering, `LEGEND_STATIONS`, with no test pinning any to another; and `AuthFlowPage.tsx:109` hard-codes a state-invariant label that describes a healthy exchange while `data-state="failed"` (from: feature:auth-handshake-flows)
- Give UI-state transition tables a second column — _and then what?_ — because a table of rendered instants grades green over an unusable flow: `feature:auth-handshake-flows` enumerated twelve transitions, passed 12/12, shipped with a "Ready to ship" verdict, and within eight hours of its post-release probe an accessibility-framed audit landed #4834 (rejected code strands keyboard focus on `document.body`) and #4828 (the demo's settled beat is the terminus — the reference sign-in signs you into nothing), both on rows the table named and the run deliberately changed (from: feature:auth-handshake-flows)
- Require a stage that restates a `SURFACED:` unknown to record what it _tried_, not only what the previous stage wrote — `feature:auth-handshake-flows` carried "whether the discovery document exposes `end_session_endpoint`" through four artifacts as unverifiable-from-the-repo, and it was one unauthenticated `curl .well-known/openid-configuration` away the entire time (measured 2026-09-03: `end_session_endpoint: https://dev-ytbgmz5ls3wh4xdx.us.auth0.com/oidc/logout`); the mechanism is bundling a checkable unknown with an uncheckable one under a single line, so the checkable half inherits the other's status and propagates by quotation (from: feature:auth-handshake-flows)
- Hold a run's own additions to that run's own accessibility criteria — `feature:auth-handshake-flows` graded criterion 11 ("accessible and motion-safe") green by checking the Handshake's travelling pulse under reduced motion, while the timed beats the run itself introduced on the sign-in demo ignored the preference entirely; #4828 had to add `REDUCED_VERIFIED_SETTLE_MS` and `REDUCED_HANDOFF_DELAY_MS` four days later (from: feature:auth-handshake-flows)
- Add the Auth0 Allowed Logout URLs entries for `https://mattbutlerengineering.com/hospitality/callback` and `http://localhost:3002/hospitality/callback` on tenant `dev-ytbgmz5ls3wh4xdx.us.auth0.com` — the genuinely-human half of the step every stage of `feature:auth-handshake-flows` surfaced; until it is done a real production sign-out stops on Auth0's own error page and never reaches the signed-out state the run built, which is confirmed working by direct navigation but has never been exercised through the actual logout return leg (from: feature:auth-handshake-flows)
- Verify whether the edge CSP refuses the Cloudflare Web Analytics beacon's events — the zone-level injection adds `beacon.min.js` for browser UAs (token `0c3656fe25764d6b891842c8ceb2e718`, `spa:2`), which POSTs to `https://cloudflareinsights.com/cdn-cgi/rum`, but `infrastructure/worker/csp.js:58` `connect-src` lists only `'self'`, the Auth0 origin, `https://api.mattbutlerengineering.com`, and the Sentry ingest origin, so the tag likely loads and every event is then refused; this is a hypothesis, not a finding, and it cannot be probed from the LAN because the resolver `192.168.4.40` sinkholes `cloudflareinsights.com` to `0.0.0.0` — verify only from CI/Linux or with `curl --resolve`, read the live `Content-Security-Policy` header rather than `csp.js` (the KV key `security/csp` can override it), then either add the origin to `connect-src` or drop the zone-level injection (from: maintenance:rialto-web-usage-instrumentation)
- Promote the git-excluded `apps/hospitality/e2e/.ux-audit/` walker (synthetic OIDC storageState + `mockApi` + per-route screenshots/aria/meta at 3 viewports × 2 schemes) into a committed `ux-walk` tool with per-route assertions — an `h1`, a `main` landmark, a route-unique `document.title`, zero `button button`, zero horizontal overflow, and ≥44 px targets at 768 — it found 30 unique findings in one evening that no existing gate could see (from: feature:hospitality-service-ux)
- Close the UTC-vs-local date class in one move: `packages/types/src/date.ts` `toDateString` is UTC while hospitality compares it to local `en-CA` strings (the timeline now-line, A1), `BriefingPage` buckets with `getUTCHours` (A2), the booking widget shows guest-TZ slots (D1, #4976), and `e2e/api-mocks.ts` `todayReservations()` re-dates fixtures with the UTC day so evening E2E runs render an empty timeline — add a lint/contract test that hospitality never compares a local date string to `toDateString(Date)` and decide one venue-timezone formatter (from: feature:hospitality-service-ux)
- `apps/hospitality/docs/IMPROVEMENT-BACKLOG.md` checkboxes are graded by memory, not grep: "[x] `useApiCall` hook + `ErrorRetryBanner` adopted" (line 14 — no such hook exists) and "[x] All hardcoded CSS colors replaced with Rialto tokens" (line 20 — 14 literals remain, incl. a light canvas grid in dark theme) — run `/md-audit` over it and require a grep-able claim per checked line (from: feature:hospitality-service-ux)
- Suspicions worth one probe each before filing: hand-rolled dialog Escape-path focus loss may be StrictMode-only (verify in a production build); the dashboard "Live" pill reads Live while the SSE stream is refused (trace the indicator's source); waitlist Notify may drop the guest from the host's list (verify the real `GET /api/v1/waitlist` returns `notified` rows) (from: feature:hospitality-service-ux)
- Resolve `RESERVATIONS-API-7`, `AGENT-API-8` and `USERS-API-7` in Sentry now that the fix has held for 4 d 19 h and ten container swaps — all three still sit `unresolved` at 5 / 4 / 2 events (re-measured 2026-09-10T00:38Z; newest event still `2026-09-05T05:14:40Z`, from an outgoing pre-fix container), so the noise the run existed to stop is gone from the stream but still on the board, and Sentry only arms regression detection on a resolved issue — resolving is what turns today's silence into an alert the day a fourth OTLP signal or an SDK bump reopens the fall-through; no stage of the run was authorized to change Sentry state, so this is a human's click or a run that is (from: maintenance:otlp-localhost-default)
- Pin `unconfigured` and `disabled` modes to "zero OTLP endpoints of any signal" against the real `@opentelemetry/sdk-node`, not the mocked constructor argument — Review found the logs signal still exporting to `http://localhost:4318/v1/logs` after traces and metrics were closed, because `NodeSDK.start()` builds an exporter from env for every signal the caller leaves unset and the `sdk.test.ts` seam mocks `NodeSDK` so it cannot see any of them; Review's per-process probe (`AS SHIPPED -> endpoints: http://localhost:4318/v1/logs`, `WITH logRecordProcessors: [] -> NONE`) is the shape of the test — run it as its own vitest project or a `node` script in CI and assert zero endpoints in both modes, so a fourth signal or a `0.221.0` → later bump that moves a default goes red instead of silently reopening the defect (from: maintenance:otlp-localhost-default)
- Make `scripts/check-env-sync.js` see env reads made through a helper — it matches only a literal `process.env.NAME` (`:57`, `:64`) and skips test files (`:44`, `:50`), so `resolveTelemetryPlan(env)` indexing named constants and `@mbe/sentry`'s `resolveConfig` are both invisible to it; measured on the branch, the check was green before and after the `PLATFORM_VARS` entries were added, and the change removed the last literal `process.env.OTEL_*` read from non-test source, so the `OTEL_SDK_DISABLED` lines in all three `services/*/.env.example` files are now unguarded — the gate is quietly narrower than its name, and centralised env access, the pattern two packages now use, is exactly what it cannot count (from: maintenance:otlp-localhost-default)
- Exercise `exporting` mode once against a throwaway collector before the Grafana Cloud run claims it — the path has never met a real endpoint: it is verified structurally only (`expect(OTLPTraceExporter).toHaveBeenCalledWith()`) and nothing since the merge changed that; a one-off `otel-collector` container with the debug exporter, `OTEL_EXPORTER_OTLP_ENDPOINT` pointed at it and one boot of a service would prove traces and metrics arrive and logs stay off as the mode table says, and would trip the dormant precedence bug `architecture.md` flagged (with `LANGFUSE_PUBLIC_KEY` set, `spanProcessors` wins over `traceExporter` at `sdk.js:202` vs `:208` and the OTLP trace exporter is silently ignored) while it is still cheap; needs no account, unlike the M2/M3 seed above (from: maintenance:otlp-localhost-default)
- Stop `deploy-services.yml` creating two DigitalOcean deployments for one merge — runs `33945783241` (`f9ee20a05`, 04:54:00Z) and `33945822458` (`dffa3945c`, 04:54:55Z) on 2026-09-05 each ran `doctl apps create-deployment`, producing `be2ca73a` and, nine minutes later, `c347e4e7` superseding it, both built from `dffa3945c` because DigitalOcean builds `main` HEAD at create time rather than the run's SHA; that is two container swaps for one change and a deployment list that misattributes the second unless the reader knows this; a `concurrency:` group on the deploy job (queue, not cancel-in-progress) or a "newer `main` commit already queued — skip" check before `create-deployment` closes it, and writing the SHA → deployment id into the run summary makes the attribution readable afterwards (from: maintenance:otlp-localhost-default)
- Find out what OTel export does at a container swap before a real collector depends on it — every one of the 11 pre-fix `AggregateError` events sits within 20 s of a DigitalOcean deployment going ACTIVE (6/6 pre-fix swaps fired, 0/10 post-fix), the last three came from the outgoing containers (`app_start_time` 2026-09-04T22:05Z, replaced at the 05:14:23Z swap), and thousands of steady-state export attempts per service surfaced nothing — so the exporter swallows a refused connection in steady state and something at the swap (the SIGTERM `NodeSDK.shutdown()` flush, the incoming container's first export after cut-over, or both) lets it escape as an unhandled rejection; the population that would answer this is gone from production, but it reproduces locally (pre-fix `sdk.ts`, no endpoint, boot, SIGTERM), and it matters for M2: if the outgoing container's final flush is racing its kill, the last batch of every deploy is lost to the collector too (from: maintenance:otlp-localhost-default)
- Give Ship a completion predicate stronger than "`release.md` exists" — the protocol's orientation table marks Ship complete on file existence, so a draft written with CI in flight (this run: 2026-09-03, steps 7–9 reading "see Outcome") made Operate the next stage while the PR sat unmerged for 24 h 32 min after `CI Gate` went green, was then merged by hand rather than by the `--auto` the draft planned, and the record said nothing for six days until a Ship stage was re-dispatched to complete it; the visual-tolerance-threshold seed above states the reconciliation problem — this is the predicate underneath it: treat a `release.md` whose release-log steps are unfilled as Ship-in-progress, and let autorun re-enter Ship to complete the record rather than advance past it (from: maintenance:otlp-localhost-default)
- When a fix is "stop the dependency doing X by default", have Architect enumerate every default the dependency applies to the inputs the caller leaves unset, read from the installed source, before criteria are derived — this run's two-exporter framing came from the backlog seed, was copied into `defect.md`, `architecture.md`, `breakdown.md` and `verification.md` unchallenged, and every downstream check faithfully verified the two things it was handed while `NodeSDK` was building a third; Review caught it only by asking the real SDK "which endpoints exist now", a question no artifact had posed; the criteria list verified itself, and the existing Capture-evidence-bar seed does not cover this because the claim was not false, it was incomplete (from: maintenance:otlp-localhost-default)
- Derive `deploy-static.yml`'s `on.push.paths` and its dorny `hospitality:` / `marketing:` / `rialto-web:` filters from each app's real `workspace:*` dependency list instead of a hand-written three-package subset — `apps/hospitality/package.json` depends on eight workspace packages (`rialto`, `api-client`, `auth`, `cancellation-policy`, `observability`, `rialto-catalog`, `sentry`, `types`, plus `config`) and the workflow watches three, so `f4eb21bef` (#4826, the run's Critical activate-400 fix, `packages/api-client` only) produced no deploy run at all and reached production 25 minutes later only because #4829 happened to touch `apps/hospitality/`; second measured instance of the class the backlog already carries for `deploy-services.yml` + `packages/sentry` (#4927/#4931), which is the argument for a generated filter (or `packages/**`) plus a test that diffs it against `pnpm -r list --json` rather than another hand fix (from: feature:venue-onboarding-floor-plan)
- Stop `venue-journey.yml` from losing a day's evidence to one apt mirror — `pnpm --dir apps/hospitality exec playwright install --with-deps chromium` re-fetches dl.google.com's chrome-stable index on every scheduled run, and run 34384782119 (2026-09-09) died there on a `Hash Sum mismatch` with the journey step skipped, so the feature's only production signal was blank for the day with nothing to distinguish "environment" from "regression" except a human reading the step log; cache `~/.cache/ms-playwright` keyed on the Playwright version, or install chromium without `--with-deps` on a runner image that already carries the libs, and retry `apt-get update` if the deps step must stay (from: feature:venue-onboarding-floor-plan)
- Surface the advisory `Hospitality E2E` job when it is red on consecutive distinct PR heads — `onboarding.spec.ts` failed byte-identically on #4965, #4967 and #5009 from 2026-09-01 to 2026-09-04 (a real, deterministic StrictMode bug, #5017 → #5044) and nothing blocked, paged, or filed; `CI Gate` is the only required check and the job lives in `e2e.yml`, so consecutive redness is invisible by construction, while a single red is legitimately noise (auth bypass, secrets); a sensor that counts consecutive failures per job across distinct heads and files one `ci-fix` issue at N ≥ 2 would have caught this on day one instead of day four (from: feature:venue-onboarding-floor-plan)
- Commit each run artifact at its own stage boundary and make autorun refuse to advance past an uncommitted one — eight of this run's nine artifacts (idea → review, plus the brief) lived untracked in a stale checkout for nine days and entered git only when Ship happened to run there; per ADR-0026 the artifacts are the state, so for nine days the run had no state anywhere but one disk, `next` could not have oriented from a fresh clone, and a lost worktree would have erased the whole record with only issue bodies to reconstruct from; each stage skill already knows its artifact path, so the commit (explicit path, never `git add -A`) belongs at the end of every stage, not at Ship (from: feature:venue-onboarding-floor-plan)
- Add a StrictMode double-mount unit test for `LaunchStep` (render under `<React.StrictMode>`, press Launch, assert the celebration fires) and decide whether `apps/hospitality`'s vitest harness should wrap every component render in StrictMode by default — #5044 (`4315cd765`) fixed `unmountedRef` never being reset on mount, a defect one line from the ref the run's own review examined, and shipped with zero unit tests (two files: `LaunchStep.tsx` and the E2E spec), so the exact bug can return with 1936 unit tests green and only the advisory E2E job to notice; `LaunchStep.tsx` is currently the only `unmountedRef` guard under `apps/hospitality/src`, so the test is cheap now and the harness decision is what prevents the next one (from: feature:venue-onboarding-floor-plan)
- Make the pre-push hook name its real cause when `packages/agent-core/dist` is missing — in a fresh worktree `pnpm regen --check` fails because `mbe pack` cannot import `@mbe/agent-core`, but the hook reports it as "llms-txt stale", which sends the agent toward a regen it cannot run rather than the `pnpm build --filter @mbe/cli...` that fixes it; the pre-commit `check-adr --staged` fails on the same missing dist with a clearer error, so the two hooks disagree about one condition (surfaced by this run's Ship stage, 2026-09-09; the fix is a dist-existence check before the regen comparison, printing the build command) (from: feature:venue-onboarding-floor-plan)
- Have the daily journey exercise a template with tables — `venue-journey.spec.ts:93` picks "Blank — no tables" so drag-snap stays a unit concern (correct), but it means the Launch sequence's Tables stage (`POST /api/v1/tables` × N) and its duplicate-400 resume rule have never run against production by any machine after nine green days, while the run's one live failure was precisely a client↔service contract that only the real Fastify parser could reject; alternate Blank and Cafe by run parity, or add a second lighter journey, and extend the sweep to a venue that now carries tables (`floorPlanService.delete` already cascades) (from: feature:venue-onboarding-floor-plan)
- Have one human complete the six-step wizard on production, observed, before the next feature behind Auth0 opens — this run closed with nine green synthetic walks and zero observed humans (population: one), the four drawn templates (Restaurant / Cafe / Bar / Patio, invented by UX from a one-sentence backlog entry) have been chosen by nobody, no real Konva drag has happened anywhere, and the ≥ 1024 px editing threshold was computed from CSS, never seen; the walk takes under five minutes, and what to record is which template they pick, whether they touch the canvas or ship it untouched, where they hesitate, and whether the editor landing is where they expected to be (from: feature:venue-onboarding-floor-plan)
- Hoist `DEFAULT_LAYOUT` into `floor-plan-geometry.ts` so `launch-sequence.ts` and `NewFloorPlanDialog.tsx` import one definition — the run re-derived an identical `{ width: 800, height: 600, gridSize: 20, showGrid: true }` literal because the architecture froze the dialog's file and the constant is module-private there, then pinned the copy with a test; two pinned copies of one constant is the shape that drifts the day someone edits the unpinned one (breakdown.md § Design gaps flagged it as a backlog candidate at Operate) (from: feature:venue-onboarding-floor-plan)
- Make a maintenance run that closes with its defect still live say so somewhere that outlives the run — this run met all four of its exit criteria and the surface it exists to restore is 100% dead three days after merge and 119 days after the routes shipped, because criteria 1-4 grade a _preparation_ and criterion 4 (`nothing is applied`) is literally _satisfied_ by the defect remaining live; that is the same shape as the vacuous `ingress-coverage.test.ts` this run was opened to kill, one level up, and the protocol's completion rule (`retro.md` exists → COMPLETE) cannot distinguish it from a delivered fix; the fix is a required fifth criterion for any prepare-and-stop maintenance run that stays explicitly unmet, names its human owner, and points at a carrier outside the run directory (from: maintenance:public-ingress-never-applied)
- Give a `deferred` review finding a carrier that survives run close — `review.md` classified the post-deploy `--base` probe gap MAJOR and deferred it "to a follow-up Implement work item / `ready` issue", `release.md` carried it as release step 5 and as a precondition to trusting the gate, and no issue was ever filed (searched 2026-09-12: `check-api-surface-invariants` returns only the nightly drift bot, `reachable-through-edge` only the thirteen breach issues, nothing names the gap); artifacts stop being read the moment the run completes, so a finding recorded only in `review.md` has a half-life of exactly one run and this one evaporated in three days (from: maintenance:public-ingress-never-applied)
- Drop `--base https://api.mattbutlerengineering.com` from `post-deploy-check.yml:206-207` so each probe's own `origin` governs, adjusting the pinning test `resolveBase > lets an explicit --base override even a probe that pins its own origin` deliberately — `resolveBase()` is `baseOverride ?? probe.origin ?? DEFAULT_BASE`, so in CI the `reachable-through-edge` probe is sent to `api.` and the Cloudflare gate is never measured; run locally without `--base` on 2026-09-12 the same probe correctly reports `status-mismatch` (200, the marketing SPA) at the apex while CI reports `wrong-service` at `api.`, i.e. the deployed gate currently verifies the DO half twice and the edge half never and will report green over a dead edge the moment the apply lands (from: maintenance:public-ingress-never-applied)
- Assert declared-vs-live infrastructure after every apply, the way this run's probe now asserts declared-vs-live API surface — the defect was three months of `infrastructure/pulumi/index.ts` declaring an ingress rule that `ignoreChanges: ["spec"]` silently discarded, and nothing anywhere compared the rules in source against `doctl apps spec get`; a post-apply check that reads every `ingress.rules` entry out of `index.ts` and fails when the live spec lacks it would have caught this on 2026-05-16 instead of 2026-08-24, and generalises to every other spec field the narrowing still ignores (from: maintenance:public-ingress-never-applied)
- Treat `pulumi up` reporting `success` with `digitalocean:index:App … unchanged` on a commit whose diff touched that resource as a detectable contradiction rather than a green — run 32693610715 on `2c984392` (the PR that added the `/public` rule) ended `Resources: ~ 1 updated, 15 unchanged` with the App among the unchanged, and reported job-level success because nothing failed; it simply had nothing to do, and that reading is available mechanically from the `up` summary plus the commit's changed paths (from: maintenance:public-ingress-never-applied)
- Make a silent-degradation `catch` on a network boundary report before it degrades — `apps/hospitality/src/hooks/useVenuePolicy.ts`'s `catch { return null; }` is why a 100%-dead public booking surface produced zero Sentry events for 119 days, and Sentry's continuing quiet (13 new issues in 7 days, every one an `HTTP 429` credential-scanner probe against the `/` catch-all, every one 0 users, none touching `/public`) is not evidence of health but evidence that the only client discards its own errors; a `captureException` before the fallback costs one line and converts a permanently invisible outage into a filed issue (from: maintenance:public-ingress-never-applied)
- Escalate a human-gated blocker that outlives the run that found it — #4848's two orphaned Auth0 state records (`Tenant`/`Branding`, created by #4924, source removed by revert #5165, state left behind) have failed `Pulumi Refresh` with `403 Insufficient scope` on every `pulumi-up.yml` run on `main` since 2026-09-09T17:05Z, which blocks _every_ infrastructure deploy and not just the run that discovered it; the run reported it exactly and then closed, and nothing in the pipeline re-raises it, so a three-day total IaC delivery outage is currently recorded only inside a closed run's `release.md` step 0 (from: maintenance:public-ingress-never-applied)
- Read a burst of identical auto-filed issues as an alarm before deduping it — `post-deploy-check.yml` filed twelve SHA-keyed `API surface invariant breach` issues in 36 hours (#5168 … #5231), all one cause (the unapplied ingress), and the response was #5189 → #5236 `fix(automation): dedupe post-deploy-check issues by failure signature, not commit SHA`, which correctly stopped the flood at #5237; the keying fix was right and the reading was wrong — twelve issues in 36 hours was a true alarm about a dead production surface that got processed as tracker noise, and nobody who saw the volume opened one and read what it said (from: maintenance:public-ingress-never-applied)
