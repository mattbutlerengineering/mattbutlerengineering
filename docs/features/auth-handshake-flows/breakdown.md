---
stage: decompose
run: feature:auth-handshake-flows
date: 2026-08-30
tracking: "#4731"
assumptions:
  - "`AuthFailurePage` and `SignOutPage` live under `apps/hospitality/src/pages/` (the architecture's paths) and compose the gate stage from `../components/LoginGate.module.css`, the same way `SessionExpiredGate.module.css` composes from its sibling — `pages/` has no CSS stage of its own."
  - "`SignOutPage` mirrors `CallbackPage`'s layout (centred flex container, no card, `.handshake { width: min(100%, 320px) }`). ux.md's S7 spec says 'no card' while its conventions line lists a card; the S7 spec wins because it is the more specific statement and because the page is on screen for well under a second."
  - "The `DashboardLayout` refresh-failure banner changes copy only — no busy state on its action button — per architecture assumption 17. Item 5 is therefore a pure copy/constant change."
  - '`App.test.tsx` stubs the two new pages (`auth-failure` with `data-lane`, `sign-out-page`) the way it already stubs `CallbackPage` and `SessionExpiredGate`; the category/retry assertions move to `AuthFailurePage.test.tsx`. Rendering the real page instead is explicitly allowed in the issue, with the `"Try Again"` → `"Try again"` seam noted.'
  - "`hasAuthParams` is re-exported from `packages/auth/src/react/index.ts` only. The root barrel `packages/auth/src/index.ts` is deliberately selective (it does not re-export `isSafeReturnTo` either), and hospitality imports from `@mbe/auth/react`."
  - "The `/demos/session-expired` `Handshake` reuses hospitality's `SessionExpiredGate` aria-label, `Session with Identity has lapsed`. ux.md D3 says 'aria-label per ux.md' but never states the string; the demo mirrors that gate, so its label is the honest choice."
  - "The `/demos/auth-flow` `Handshake` aria-label is not specified anywhere (see Design gaps). The issue gives the worker `Authorization-code exchange between Identity, Browser, and API`, built from the page's own intro vocabulary, and asks the PR to flag it for UX override. Acceptance only requires a non-empty label distinct from the caption."
  - "`.lapse` is defined by item 10 (the only consumer), not by item 8; item 8 defines the two shared classes (`.instrumentSlot`, `.statusLine`). Items 9 and 10 chain behind 8 because they share `AuthLayout.module.css` lineage, and 9 does not touch the CSS file so 9 and 10 are disjoint once unblocked."
  - "Architecture components 11 and 12 (`handshakeFor` + `HANDSHAKE_STATIONS`, and the `AuthFlowPage` rewrite) merge into item 7: the projection has no value until the page consumes it, and splitting would leave a dead export on `main` between sittings."
  - "Architecture components 7, 8 and 9 (`SESSION_LAPSE_COPY`, `SessionExpiredGate` reading it, the banner reading it) merge into item 5: PRD criterion 5's 'one shared source' is only checkable when both consumers change in the same PR."
  - "Hospitality is cut horizontally (four leaves, then one policy item) rather than as vertical slices, because the parallel-safety rule forbids two unblocked items touching `App.tsx`/`App.test.tsx`, and every hospitality story ends in that file. Vertical slices were kept for the three rialto-web demos, where each page is self-contained."
  - "Every issue carries `model: sonnet` / `budget: 1.00` in its agent block, matching the house `/decompose` issues read (#4633–#4640); none of them scale the budget by size, so neither does this run."
  - "The tracking issue title follows the orchestrator's `[tracking] auth-handshake-flows: <short>` form rather than the older house `[Feature] <name>: tracking issue` form; child titles keep the house `[Feature] <name> [N/M]: <task>` form so `/implement-queue`'s zone derivation still falls back to the `## Files to Modify/Create` heading every body carries."
  - "No item runs `packages/rialto` gates: no item touches `packages/rialto/src`, and CI runs the rialto suite regardless. `apps/gen` typecheck+test are attached to item 1 because `apps/gen` mocks `useAuth()` and is the only other consumer of `@mbe/auth/react`."
  - "Blocked issues carry both `Blocked by: #n` (the tracker mirror the orchestrator asked for) and `Depends on: #n` (the line `implement-queue-worker` checks before starting); they are the same set in every case."
  - "No `priority:*`, `size:*`, or `tier:*` labels: none of the house `/decompose` issues read carry them, and `tier:*` is applied by `tier-classifier.yml` at PR time, not at issue time."
surfaced:
  - "Human step, not an issue: the Auth0 tenant `dev-ytbgmz5ls3wh4xdx.us.auth0.com` must list `https://mattbutlerengineering.com/hospitality/callback` and `http://localhost:3002/hospitality/callback` under Allowed Logout URLs and expose `end_session_endpoint` in its discovery document. Nothing in this repo can verify either; until it is done the signed-out gate (item 6) is unreachable in production and Auth0 shows its own error page instead."
  - "ADR-003 still describes `@auth0/auth0-react` while the code has used react-oidc-context via `@mbe/auth` since well before #4720. Out of scope for this run; the `adr-compliance-reviewer` may flag item 1 or 6 against it. Needs a separate ADR amendment."
  - "Whether the Hospitality E2E job is runnable on Verify day is unknown from this tree — it is advisory, frequently red for auth-bypass reasons, and its `auth.spec.ts` / `auth.setup.ts` are frozen by this run. The signed-out and failed-exchange paths have unit coverage only."
  - "How often real users hit the signed-out callback, a failed exchange, or a refresh failure is unmeasured (no Sentry or analytics event exists for any of them), so the PRD's 'no one sits on a stuck spinner' claim cannot be quantified before or after this run."
---

# Breakdown: auth-handshake-flows — Handshake on every remaining auth transition

Tracking issue: **#4731**. Tree: `main` at `1d6189203` (PR #4720 merged). Ten items, three milestones, one human step. Every child issue is self-contained — a worker branching from `main` never needs this directory.

## M1 — `@mbe/auth` seams

- [x] **@mbe/auth: deriveReturnTo callback-path rule + hasAuthParams re-export** — `deriveReturnTo` returns `"/"` whenever `location.pathname` equals the callback path of `redirectUri`; `hasAuthParams` re-exported from `@mbe/auth/react`; `packages/auth/CLAUDE.md` row + lifecycle sentence; `llms.txt`/`llms-full.txt` regenerated (tracker: #4721)
  - Accept: `deriveReturnTo({ pathname: "/hospitality/callback", search: "?code=abc&state=xyz", hash: "" }, "https://x/hospitality/callback") === "/"`; `hooks.test.tsx` proves a bare `signIn()` at `/hospitality/callback?code=…` calls `signinRedirect` with `{ state: { returnTo: "/" } }`; `import { hasAuthParams } from "@mbe/auth/react"` is react-oidc-context's own function; `pnpm regen --check` clean; every existing `packages/auth` and `apps/gen` test green.
  - Blocked by: —

## M2 — Hospitality

- [x] **LoginGate `signedOut` prop** — optional prop swaps the tagline for "You're signed out. Sign in again whenever you're ready."; nothing else moves (tracker: #4722)
  - Accept: `<LoginGate signedOut />` renders the signed-out tagline and not the default one; `login-prompt` and the button named exactly "Sign In" still present; default render unchanged; existing `LoginGate.test.tsx` green.
  - Blocked by: —
- [x] **AuthFailurePage** — new `src/pages/AuthFailurePage.{tsx,module.css,test.tsx}`: `Handshake` `failed` on the given lane with `describeAuthError` copy, "Try again" iff `canRetry` calling bare `signIn()`, "Technical details" disclosure, and a negotiating "retrying" variant while `activeNavigator === "signinRedirect"` (tracker: #4723)
  - Accept: for all four error categories the page renders `img` "Your sign-in could not be verified" with `data-state="failed"` and the prop's `data-lane`, the category title/body, and the raw message under "Technical details"; "Try again" present for three categories and absent for access denied; click calls `signIn` once with no arguments; `role="status"` reads "The exchange didn't go through"; retrying variant shows `negotiating`/lane 0/"Connecting your browser to Identity"/"Starting a fresh sign-in"/busy "Heading to sign-in".
  - Blocked by: —
- [x] **SignOutPage** — new `src/pages/SignOutPage.{tsx,module.css,test.tsx}`: `Handshake` negotiating Browser ⇄ Identity with status "Signing you out", mirroring `CallbackPage`'s layout (tracker: #4724)
  - Accept: renders `img` "Ending your session with Identity" with `data-state="negotiating"` and `data-lane="0"`, stations "Browser Identity", `role="status"` "Signing you out", `data-testid="sign-out-page"`.
  - Blocked by: —
- [x] **SESSION_LAPSE_COPY: one voice for SessionExpiredGate and the refresh banner** — new frozen constant at `src/constants/session-lapse-copy.ts`; `SessionExpiredGate` and the `DashboardLayout` banner read heading/body/lead/action from it; instruments and `signIn` calls untouched (tracker: #4725)
  - Accept: both test files import `SESSION_LAPSE_COPY` and assert the same `action` ("Sign back in"); banner text is "Your session couldn't renew and will end soon. Sign back in to pick up where you left off — this page is preserved."; `grep "Sign in again\|couldn't refresh"` over `apps/hospitality/src` source finds nothing; existing dismiss / `returnTo` / no-args-`signIn` assertions green.
  - Blocked by: —
- [x] **App.tsx gate order** — `isLoading → (signoutRedirect ? SignOutPage : isCallback ? CallbackPage : LoadingPage)`, `error → AuthFailurePage lane={isCallback ? 1 : 0}`, `isCallback && !isAuthenticated → (hasAuthParams(window.location) ? CallbackPage : <LoginGate signedOut/>)`, `sessionExpired`, `LoginGate`, `Outlet`; inline error JSX and `App.module.css` `.errorDetails` removed; `App.test.tsx` mock factory uses `importOriginal` so the real `hasAuthParams` runs; `apps/hospitality/CLAUDE.md` gate paragraph rewritten (tracker: #4728)
  - Accept: `/hospitality/callback` with empty search and not authenticated → `login-prompt` + signed-out tagline + "Sign In" button, no `callback-page`; `/hospitality/callback?code=abc&state=xyz` → `callback-page`, no `login-prompt`; `error` on the callback URL → `auth-failure` with `data-lane="1"`, on `/hospitality/reservations` → `data-lane="0"`; `isLoading` + `signoutRedirect` → `sign-out-page`; every existing lifecycle case green; `grep "Try Again"` finds nothing; E2E files untouched.
  - Blocked by: #4721, #4722, #4723, #4724

## M3 — rialto-web demos

- [x] **/demos/auth-flow: handshakeFor projection, Handshake replaces hand-rolled lanes** — `handshakeFor(step)` + `HANDSHAKE_STATIONS = ["Identity","Browser","API"]` in `authFlowMachine.ts` (table-tested over `HAPPY_PATH` + `ERROR_STEP`); `AuthFlowPage` renders one `<Handshake size="lg">` from it, station cards become legends (no LEDs, "Identity"), `ChannelLane`/framer-motion/`.groove`/`.pulse` removed, layout CSS kept (tracker: #4726)
  - Accept: `handshakeFor` matches the contract for all eight steps; `grep -cE '\.(pulse|groove|lane|laneIdp|laneApi|laneLabel)\b' AuthFlowPage.module.css` = 0 and `grep -c framer-motion AuthFlowPage.tsx` = 0; page test shows `idle` on load, `negotiating`/lane 0 after Next, `failed` with `[data-station="Browser"][data-variant="danger"]` on the error step; play/pause/reset/pause-when-hidden green; no `.png` baseline diff.
  - Blocked by: —
- [x] **/demos/login: phase table drives Handshake** — `phase: idle|submitting|verifying|rejected|verified` + `SIGN_IN_PHASES` lookup; fixed `Handshake` slot (`size="md"`, Browser — Identity) + `role="status"` line; `verifiedRow` removed; `.instrumentSlot` and `.statusLine` added to `AuthLayout.module.css` (tracker: #4727)
  - Accept: per phase `data-state` and status text as specified ("Sending your credentials" / "Checking your code" / "The exchange didn't go through" / "Verified") under fake timers; `000000` → `failed` with the didn't-match hint and no toast; `123456` → `settled` then the toast after 700 ms; `grep -c 'state: "settled"'` and `'state: "failed"'` in `SignIn.tsx` each = 1; existing `SignIn.test.tsx` green.
  - Blocked by: —
- [x] **/demos/signup: phase table drives Handshake** — `phase: idle|submitting|created` + `SIGN_UP_PHASES`; same slot/status shape reusing `.instrumentSlot`/`.statusLine`; `created` → `settled` "Account created" before the toast; inline validation never lights the instrument (tracker: #4729)
  - Accept: `idle` on load with empty status; `negotiating` + "Creating your account" before 1500 ms; `settled` + "Account created" after, with the existing toast assertion; invalid form leaves `idle` and no toast; `AuthLayout.module.css` unchanged in the PR.
  - Blocked by: #4727
- [x] **/demos/session-expired: Handshake idle beside the lapse LED, shared copy** — `Handshake` idle ("Session with Identity has lapsed") in the slot above the warning `StatusLED`; heading "Your session ended", body "Sign back in to pick up where you left off.", button "Sign back in" → `DEMO_ROUTES.signIn`; undefined `.sessionExpired`/`.sessionCopy` replaced by a new `.lapse` in `AuthLayout.module.css`; test line 73 `/sign in/i` → `/sign back in/i` (tracker: #4730)
- [x] **Verify-fix: `/demos/signup` created-phase label ≠ status line** — `SIGN_UP_PHASES.created.ariaLabel` → "Account created — your browser and Identity agree"; table-driven test pins `ariaLabel !== status` for every phase with a status; `SignUp.test.tsx` settled assertion updated (mirrors `SignOutPage.test.tsx:39`). Routed back from Verify (PRD criterion 11 FAIL). (tracker: #4770)
  - Accept: `img` label with `data-state="idle"`; `status-led` still `warning` + `pulse`; heading role present; body exact and "this page is preserved" absent; button navigates to the sign-in route; `grep -c 'sessionExpired\|sessionCopy' SessionExpired.tsx` = 0.
  - Blocked by: #4727

## Design gaps found

- **D4 `Handshake` aria-label is unspecified.** ux.md gives per-phase aria-labels for D1, D2, S3 and S7 and says D3's is "per ux.md" (which resolves to nothing), but for `/demos/auth-flow` neither ux.md nor architecture.md names the string, and the architecture only requires that it "describe the exchange" and differ from the caption. Not blocking: issue #4726 hands the worker `Authorization-code exchange between Identity, Browser, and API` and asks the PR to flag it; UX or the Architect can override in review. Recorded here, not designed around.
- **D3 aria-label points at a string that does not exist** ("aria-label per ux.md"). Resolved by assumption (hospitality's `SessionExpiredGate` label is reused) rather than by inventing new copy; noted in #4730 so the reviewer sees it was a choice.
- **Found at review of #4750 (2026-08-31):** the `created` phase of `/demos/signup` gives the `Handshake` img label and the `role="status"` line the same sentence ("Account created" / "Account created") — the issue contract and ux.md D2 say so verbatim, but prd.md criterion 11 and architecture.md § Demo phase tables require _two different sentences_ (a screen-reader user hears it twice). The worker complied with the contract; the defect is in the spec. Fix at Verify/Review: give the `created` label a distinct sentence, e.g. "Account created — your browser and Identity agree" (mirrors SignIn's `verified` label), and PATCH the D2 row. Not filed as an issue yet.
- **Found at determinism review of #4755 (2026-08-31) — Operate backlog candidate, out of run scope:** `scripts/check-regen-needed.mjs` reads only `git diff --name-only origin/main...HEAD` (line 51), so a NEW source file that is staged or untracked is invisible and it answers `check` while `pnpm regen --check` (which shells `mbe pack --check` against the working tree) is stale. Reproduced: all changes staged at merge-base → `check`; PR head committed → `full`. Not a prefix gap (`needsFullRegen(["apps/hospitality/src/constants/session-lapse-copy.ts"])` → `true`). Fix sketch: union the committed diff with `git diff --name-only HEAD` and `git ls-files --others --exclude-standard`, plus a unit test asserting an untracked `apps/x/src/y.ts` yields `full`.

## Coverage

Checked component-by-component against `architecture.md` §Components (1–16) and criterion-by-criterion against `prd.md` §Success criteria (1–12). Every component appears in exactly one item; every criterion is covered by at least one item's acceptance line.

| Architecture component                                  | Item                                                       |
| ------------------------------------------------------- | ---------------------------------------------------------- |
| 1 `deriveReturnTo` callback-path rule                   | #4721                                                      |
| 2 `hasAuthParams` re-export                             | #4721                                                      |
| 3 `packages/auth/CLAUDE.md` + llms regen                | #4721                                                      |
| 4 `App.tsx` gate order + `App.test.tsx` seam            | #4728                                                      |
| 5 `AuthFailurePage`                                     | #4723                                                      |
| 6 `SignOutPage`                                         | #4724                                                      |
| 7 `SESSION_LAPSE_COPY`                                  | #4725                                                      |
| 8 `SessionExpiredGate` reads the copy                   | #4725                                                      |
| 9 `DashboardLayout` banner reads the copy               | #4725                                                      |
| 10 `LoginGate signedOut` + `apps/hospitality/CLAUDE.md` | #4722 (prop), #4728 (CLAUDE.md)                            |
| 11 `handshakeFor` + `HANDSHAKE_STATIONS`                | #4726                                                      |
| 12 `AuthFlowPage` rewrite + CSS + test seams            | #4726                                                      |
| 13 SignIn demo phases (D1)                              | #4727                                                      |
| 14 SignUp demo phases (D2)                              | #4729                                                      |
| 15 SessionExpired demo (D3)                             | #4730                                                      |
| 16 `AuthLayout.module.css` additions                    | #4727 (`.instrumentSlot`, `.statusLine`), #4730 (`.lapse`) |

| PRD criterion                                                                                 | Covered by                                                                                      |
| --------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| 1 Signed-out callback shows the gate synchronously, no timer                                  | #4728 (App gate), #4722 (tagline), #4721 (`hasAuthParams`)                                      |
| 2 Real callback with params still shows `CallbackPage`; lifecycle tests unchanged             | #4728                                                                                           |
| 3 Failed exchange renders `Handshake` failed in place, retry only when `canRetry`             | #4723 (page), #4728 (wired, lane), #4721 (retry goes home, never back to `/callback`)           |
| 4 Sign-out wait shows Browser ⇄ Identity, not the generic loader                              | #4724 (page), #4728 (keyed on `signoutRedirect`)                                                |
| 5 One voice for both session lapses from one shared source                                    | #4725                                                                                           |
| 6 `settled` has a product surface                                                             | #4727 (`grep 'state: "settled"'` in `SignIn.tsx`)                                               |
| 7 Demos use `Handshake` for every simulated exchange (D1–D3)                                  | #4727, #4729, #4730                                                                             |
| 8 Hand-rolled auth-flow motion gone (layout may remain)                                       | #4726                                                                                           |
| 9 `Handshake` remains the single visualisation; no rialto source or changeset                 | every issue's Hard constraints; #4726 removes the competing one                                 |
| 10 E2E contract (`login-prompt`, "Sign In", `auth-layout`) and both E2E files untouched       | every hospitality issue's Hard constraints + explicit `git diff --stat` AC (#4722–#4725, #4728) |
| 11 `pnpm regen --check` clean; no visual-baseline drift                                       | #4721 (regen gate); #4726, #4727, #4729, #4730 (`.png` AC)                                      |
| 12 Reduced-motion and a11y: every `Handshake` labelled, live text as separate `role="status"` | #4723, #4724, #4727, #4729, #4730 (status/label distinctness ACs); #4726 (label ≠ caption)      |

## Notes

- **Human step — Auth0 Allowed Logout URLs (no issue created).** On tenant `dev-ytbgmz5ls3wh4xdx.us.auth0.com`, add to **Allowed Logout URLs**: `https://mattbutlerengineering.com/hospitality/callback` and `http://localhost:3002/hospitality/callback`. Confirm the tenant's discovery document exposes `end_session_endpoint`. This is the only reason a signed-out `/hospitality/callback` ever loads; without it Auth0 renders its own error and item 6's gate is never reached. Recorded in #4731 as well.
- **Dispatch now, in parallel:** #4721, #4722, #4723, #4724, #4725, #4726, #4727 — seven `ready` issues with pairwise-disjoint file sets. `/implement-queue` claims at most three per batch; any three are safe.
- **Flip to `ready` later:** #4728 once #4721–#4724 are all merged (it is the only writer of `App.tsx`, `App.test.tsx`, `App.module.css`, `apps/hospitality/CLAUDE.md`); #4729 and #4730 once #4727 merges (they are disjoint from each other and can then run together).
- **Shared-file audit:** `apps/hospitality/src/App.tsx` + test → only #4728. `apps/rialto-web/src/pages/auth/AuthLayout.module.css` → #4727 then #4730 (chained). `apps/hospitality/CLAUDE.md` → only #4728. `packages/auth/CLAUDE.md` → only #4721. `LoginGate.module.css` is composed from (read) by #4723 but written by no one.
- **Regen:** only #4721 changes `packages/auth`; its gates build `@mbe/cli` first (`pnpm build --filter @mbe/cli... && pnpm regen && pnpm regen --check`), per the llms gotchas.
- **Not touched by any item** (per architecture): `packages/rialto/**`, `apps/rialto-web/src/routes.tsx`, `page-registry.ts`, `nav-sections.ts`, `data/demo-routes.ts`, `apps/rialto-web/e2e/**`, `CallbackPage.tsx`, `CallbackRedirect`, `return-to-store.ts`, `describe-auth-error.ts`, `constants/auth.ts`, `main.tsx`, `LoadingPage.tsx`, `wrangler.toml`, `apps/hospitality/e2e/auth.spec.ts`, `apps/hospitality/e2e/auth.setup.ts`.
- **Tracker mirror:** issues are a one-way mirror of this file (ADR-0026). If an item changes here, PATCH the issue body with `gh api -X PATCH repos/:owner/:repo/issues/<n> -F body=@file` — `gh issue edit`/`gh pr edit` are unreliable on this repo.

### Implement log (autorun, 2026-08-31)

- **#4721 → PR #4736** (`feat/auth-handshake-01-return-to`, head `22d106ba3`). Worker gates: auth lint/typecheck/test 165/165, hospitality + gen typecheck, root typecheck 48/48, `pnpm regen --check` clean, check-adr/check-deps clean. Worker assumptions: built `@mbe/types` + rialto locally (infra only; `registry.json` byte-identical); `packages/auth/llms*.txt` regenerated to identical bytes (pack skeleton omits re-exported third-party symbols). Review gate **pass 9/10**, no findings; `redirectUri` shapes probed (hospitality `origin + "/hospitality/callback"`, gen `origin + "/gen/callback"`, trailing-slash, origin-only, relative, empty) — only the intended delta. Nit, not retried: `isCallbackPath` re-parses `new URL(redirectUri)` that `appBasePath` parses again. Note: `packages/auth/tsconfig.json` excludes `src/**/*.test.ts` from typecheck (pre-existing). Determinism review **PASS** (`statementsPerFile = 2` cap in `pack.ts:185` is why `packages/auth/llms*.txt` are byte-identical: `isCallbackPath` is the 4th top-level statement; `index.ts` re-exports are not an extracted node kind). Auto-merge enabled.
- **#4723 → PR #4733** (`feat/auth-handshake-03-auth-failure-page`, head `e98a7b58f`). Review gate **pass 8/10**. One quality finding, not a defect: the test suite pins neither `describeAuthError(error).body` nor the root `data-testid="auth-failure"` — both are contract items #4728's `App.test.tsx` will rely on. Follow-up: #4728's worker should assert both when it wires the page. Worker assumption accepted: `import type { JSX } from "react"` is the only way to satisfy the literal `JSX.Element` signature under React 19.2 / @types/react 19.2.18 (no global JSX namespace). Issue-body path nit: it cites `src/pages/CallbackPage.test.tsx`; the page actually lives at `src/components/CallbackPage.tsx` — worker mirrored the correct stub. Auto-merge enabled.
- **#4727 → PR #4734** (`feat/auth-handshake-08-login-demo`, head `8149ace40`). Review gate **pass 9/10**, no findings. Deviation logged: `apps/rialto-web/src/routes.test.tsx` (not in the item's file list) gained a one-line `Handshake: () => null` mock stub — required because it renders `<SignIn />` against an explicit rialto mock. Design choice for #4729 to make deliberately: the disabled "Verify code" button stays mounted after verification (contract leaves it open; keeps "nothing below moves"). `.instrumentSlot` 48px vs md Handshake ≈ 38.7px, state-invariant height — verified. Auto-merge enabled.
- **External dependency satisfied out-of-run:** PR #4735 fixes `@mbe/api-client` floor-plan `setActive`/`bulkUpdatePositions` URLs (live 404s) — not an auth item; cited here only because its e2e-mock review (no drift) and determinism review ran in the same batch. Universal gate pass 9/10. Determinism: the specialist agent stalled (its scratch clone was reused by the #4736 review); replayed by hand in a fresh shared clone — head `03f438a18` merged with `main` @ `e06b2951d`, CLI built, `pnpm regen --check` clean. Auto-merge enabled.
- **#4722 → PR #4737** (`feat/auth-handshake-02-login-gate-signed-out`, head `dbb9dff9f`). Worker gates: hospitality test 1679/1679, typecheck, lint (0 errors, 124 pre-existing warnings), root typecheck 48/48, `pnpm regen --check` clean, e2e auth diff empty. Worker assumptions: `signedOut = false` via a `LoginGateProps = {}` default parameter keeps `<LoginGate />` byte-identical; also extracted `DEFAULT_TAGLINE` (issue asked only for `SIGNED_OUT_TAGLINE`) for ternary symmetry. Review gate **pass 9/10**, no findings (apostrophe byte-checked straight `'` across source, test, ux.md, architecture.md). Auto-merge enabled.
- **Merged so far (2026-08-31):** #4721 → #4736, #4723 → #4733, #4727 → #4734. Flipped `blocked` → `ready`: #4729, #4730. Batch 3 dispatched: #4725 (workers in flight: #4724, #4726, #4725).
- **#4726 → PR #4747** (`feat/auth-handshake-07-auth-flow-demo`, head `6d66cec19`, +98/−132 over the five listed files). Worker gates: rialto-web test 690/690, typecheck, lint 0 errors, root typecheck 48/48, `pnpm regen --check` clean, check-adr/check-deps clean; `.pulse/.groove/.lane*` grep = 0, `framer-motion` grep = 0. Worker assumptions: `Handshake` aria-label "Authorization-code exchange between Identity, Browser, and API" (spec fixes only label ≠ caption — UX may override); no `routes.test.tsx` stub needed (it never renders `AuthFlowPage`); page-local `LEGEND_STATIONS = ["idp","browser","api"]` drives legend hub order while the machine's `STATIONS` stays protocol order. Review gate **pass 9/10**, no findings. Two nits left out of scope: the `STATIONS` doc comment still says "in display order" (now hub order is display order); `LEGEND_STATIONS` and `HANDSHAKE_STATIONS` are parallel hand-maintained orderings with no test pinning them. Note: ux.md D4 says TOKENS → settled, architecture.md line 6 overrides to negotiating/0 — worker followed the override. Auto-merge enabled.
- **#4724 → PR #4748** (`feat/auth-handshake-04-sign-out-page`, head `6d1bedd57`, three new files). Worker gates: hospitality test 1678/1678, typecheck, lint 0 errors, root typecheck 48/48, `pnpm regen --check` clean, e2e auth diff empty. Worker assumptions: file under `src/pages/` per the issue (the mirrored `CallbackPage` lives under `src/components/` — #4728's import path must match `pages/`); `size="lg"` from the contract; return type omitted like siblings. Review gate **pass 10/10**, no findings (#4728 mocks `./pages/SignOutPage` — path assumption holds). Auto-merge enabled.
- Batch 3 fully dispatched: #4725, #4729, #4730 in flight.
- **#4730 → PR #4749** (`feat/auth-handshake-10-session-expired-demo`, head `aded9e461`). Worker gates: rialto-web test 683/683, typecheck, lint 0 errors, root typecheck 48/48, `pnpm regen --check` clean; `sessionExpired|sessionCopy` grep = 0. Worker assumptions: `Heading level={1} size={3}` by `AuthFlowPage` precedent (`AuthLayout` renders `title` as `<p>`); claimed "no `routes.test.tsx`" — it exists; reviewer to confirm it doesn't render `SessionExpired`. Review gate **pass 10/10**, no findings (`routes.test.tsx` exists but never mounts `SessionExpired` — 6/6). Auto-merge enabled. **MERGED** 2026-08-31T04:34Z as `5c102dfc3`; #4730 closed.
- **#4729 → PR #4750** (`feat/auth-handshake-09-signup-demo`, head `cac7c6ffd`, two files). Worker gates: rialto-web test 688/688, typecheck, lint 0 errors, root typecheck 48/48, `pnpm regen --check` clean. Design choice made deliberately: "Create account" stays mounted through `idle → submitting → created` (mirrors SignIn's mounted-disabled button). Worker assumptions: tests written as one batch (RED 5 → GREEN); test-file mock `Input` gained a `disabled` passthrough (pre-existing gap); phase never resets after `created` (matches SignIn + architecture table). Review gate **pass 9/10**, no findings; under-assertion noted (negotiating test checks one input `disabled`, implementation disables all). Auto-merge enabled. **MERGED** 2026-08-31T04:34Z as `8d0579abe`; #4729 closed.
- **#4728 → PR #4765** (`feat/auth-handshake-06-app-wiring`, head `aa46f3330`, four files: `App.tsx`, `App.test.tsx`, `App.module.css`, `apps/hospitality/CLAUDE.md`). Worker gates: hospitality test 1698/1698 (`App.test.tsx` 12/12), typecheck clean (root 48/48 tasks), lint 0 errors (124 pre-existing warnings, unchanged), `check-adr` + `check-deps` clean, `regen --check` up to date, `auth.spec.ts`/`auth.setup.ts` diff empty, `grep "Try Again"` empty, no `.errorDetails` rule. Worker assumption: issue's `importOriginal<typeof import("@mbe/auth/react")>()` mock snippet trips `consistent-type-imports` as a hard lint error → used the sibling `App.callback-redirect.test.tsx` pattern (`import type * as AuthReact` + `typeof AuthReact`), behaviour-equivalent. #4733 follow-up (assert `describeAuthError(...).body` + `auth-failure` testid) reported satisfied. Classifier: not low-risk, no specialists → universal `reviewer`. Review gate **pass 9/10** (all 8 ACs evidenced; #4733 follow-up assertions present at App.test.tsx:133/136/149/152; E2E contract intact, auth.spec/auth.setup untouched; `hasAuthParams` via `@mbe/auth/react`; nits: shadowed `describe` in the AuthFailurePage mock, lane-1 body assertion weakly discriminating). No specialists. Auto-merge enabled. **MERGED** 2026-08-31T04:53Z as `500affc76`; #4728 closed. Implement complete: 10/10 items merged.
- **#4728 dispatched** (`feat/auth-handshake-06-app-wiring`) once #4721–#4724 merged; carries the #4733 follow-up (assert `describeAuthError(...).body` + `auth-failure` testid in `App.test.tsx`).
- **#4725 → PR #4755** (`feat/auth-handshake-05-session-lapse-copy`, head `e51436e0e`; five source files + regenerated root and `apps/hospitality` `llms*.txt`). Worker gates: hospitality test 1691/1691, typecheck, lint clean, root typecheck 48/48, `pnpm regen` then `--check` clean ×3, check-adr/check-deps clean. Worker observations: `SessionExpiredGate` had no genuine RED (its hardcoded strings already equalled the constant byte-for-byte — wired anyway per the single-source AC); **`scripts/check-regen-needed.mjs` predicted `check` for a diff that adds a new `src/constants/*.ts` file, but `regen --check` was stale** — the fast-path heuristic under-predicts new-file additions (candidate backlog seed at Operate). Review gate **pass 10/10**, no findings (constant bytes verified: ASCII apostrophe, U+2014 dash; no e2e spec pins the old copy). Determinism review **PASS** (regen --check clean at head `e51436e0e` and on a real merge with `origin/main` `f8dca42a2`; byte-order sorts intact; manifest unchanged). **MERGED** 2026-08-31T04:44Z as `3986a98e4`; #4725 closed.
- **Verify stage (2026-08-31 ~05:15Z)** → `verification.md`: PRD 11 PASS / 1 FAIL / 0 unverifiable; all 10 breakdown-only ACs PASS; local gates green on `500affc76` (auth 165, hospitality 1698, rialto-web 698, Handshake 16, a11y 88; lints 0 errors; typecheck 48/48; regen clean); main-push CI `33358630401` success, deploy-static `33358630413` success; hospitality E2E via CI `E2E Screenshots` 71 passed on both `3986a98e4` and `500affc76`. **FAIL: criterion 11** on `/demos/signup` `created` (label == status "Account created", reproduced on production) → routed to Implement as **#4770** (above). Surfaced out-of-run: pre-existing rialto `PinInput` renders zero cells for `value=""` (`PinInput.tsx:76` `padEnd(length, "")` no-op) → `/demos/login` code group blank on production → filed as **#4771** (`ready`+`bug`, out of run scope; a `/demos/login` re-verify follows its deploy). Not verified: real Auth0 sign-out round-trip (no operator credentials), sub-second demo phases live, telemetry frequencies (none exists).
- **#4770 → PR #4772** (`fix/auth-handshake-signup-created-label`, head `7590ecca0`, two files). Worker gates: rialto-web test 700/700 (698 + 2 new `describe.each` cases), typecheck clean (root 48/48), lint 0 errors (152 pre-existing warnings), `check-adr`/`check-deps`/`regen --check`/instruction-rot clean. RED quoted: `expected 'Account created' not to be 'Account created'`. Deviation: `describe.each` callback param renamed `_phaseName` for `noUnusedParameters`; `SIGN_UP_PHASES` exported (the one permitted extra change). Review gate **pass 10/10** (diff is exactly the two permitted changes; `describe.each` iterates the real exported table — `submitting` + `created`, `idle` excluded; no stray old-label references repo-wide). No specialists. Auto-merge enabled. **MERGED** 2026-08-31T05:46Z as `14af05e3c`; #4770 closed.
- **#4771 → PR #4775** (out-of-run bug, `fix/rialto-pininput-empty-cells`, head `7ef23a03c`; `PinInput.tsx` one-line derivation `Array.from({ length }, (_, i) => value[i] ?? "")`, two RED-first tests, patch changeset). Worker gates: rialto PinInput 26/26, rialto suite 2201/2201 with one pre-existing unrelated failure (`ChatPanel/useChatStream.test.ts` — `@mbe/api-client` `./streaming` subpath unresolved in the worktree; worker confirmed identical on the base commit via stash — reviewer to confirm it is an unbuilt-dist artefact, not a `main` defect), typecheck clean (root 48/48), lint 0 errors, `pnpm --dir packages/rialto build` "exports map already in sync", rialto-web SignIn 13/13, `regen --check` clean. Deviation: issue's suggested test passed `aria-label` to `<PinInput>`, which `PinInputProps` lacks → tests written without it. Classifier + specialist: `rialto-prop-drift-detector`. Review gate: universal `reviewer` **pass 10/10** (PinInputProps byte-identical to `main`, no unknown props in any test, a11y matrix green on head); `rialto-prop-drift-detector` specialist died on the 2026-08-31 session rate limit → gate completed by hand (props diff `origin/main` vs head: IDENTICAL; every prop used across `PinInput.test.tsx`, `components.test.tsx:355`, `component-fixtures.tsx:601` exists in `PinInputProps`; no drift). CI Gate green. **MERGED** 2026-08-31 as `dc42d4fb4`; #4771 closed. Reviewer also found a pre-existing `main` defect out of this fix's scope: `PinInput.tsx:90` `!next.includes("")` is always false on a string, so `onComplete` never fires → filed as **#4782** (`ready`+`bug`).
