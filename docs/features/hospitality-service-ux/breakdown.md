---
stage: decompose
run: feature:hospitality-service-ux
date: 2026-09-04
tracking: "#5037"
assumptions:
  - "Sixteen items in five milestones, one sitting each. Rialto first (#5021, #5022), the E2E mock fix third (#5023), pure modules and seams before their consumers, `TimelineGrid.tsx` last (#5035) behind PR #4967, docs and regen last (#5036). Recommended serial order is numeric 1 → 16; every dependency edge points forward."
  - "The B1 guard ships in #5025 with a `PENDING_ADOPTION` list naming today's 20 hit files (measured with the architecture's regex over `src/**`, non-test), each later item deletes its own entries (#5027 Briefing; #5028 Reservations; #5029 thirteen files; #5030 Waitlist; #5031 three dialogs; #5034 TimelinePage) and #5036 deletes the list — so every item leaves `pnpm test` green instead of the guard landing red or last. The guard also fails when a pending entry no longer hits, so the list cannot go stale."
  - "The B1 sweep (#5029) covers three hit files the architecture's adopter list omits — `pages/SetupHoursPage.tsx:44`, `components/timeline/StaffDepositSection.tsx:50`, `components/reservations/NewReservationDialog.tsx:140` — by applying the guard's own rule (adopt `describeApiError`, do not allowlist). No allowlist entry beyond the architecture's eight."
  - "`TimelinePage`'s grid-coupled wiring (the `seatedIds` / `bottomInset` / `emptyNight` / `pendingTableId` props and the table-status handler with pending id, banner, announcement and focus) lands in the grid item (#5035), not the page item (#5034), so the page item typechecks against today's `TimelineGridProps` and the grid stays the last file touched. #5031 bridges the page's three handlers with a two-line rethrow so the tree compiles between the dialog and page items."
  - "`segmentForHour` stays an export of `pages/BriefingPage.tsx` as architected and is tested by direct import in `BriefingPage.test.tsx` inside the Briefing item (#5027); `tableStatusMenuItems` likewise stays in `TableStatusMenu.tsx` (#5033)."
  - "The walk-in intent's two producers (palette label + `groups`, HomePage link) ride with the pure modules in #5024 rather than the page item: they are two-line edits with existing tests, and the URL is consumed by #5034. `timeline-intent.spec.ts` lands with the consumer."
  - '`e2e/walkin.spec.ts` string renames ("Seat walk-in", "Seat now") land in the dialog item (#5031) in the same commit as the copy change, so the E2E suite never goes red between items.'
  - "The guest booking widget adopts the Host copy table verbatim in #5029 (the architecture's stated position); the `network` row's guest wording is design gap G3, recorded, not solved."
  - 'The "New Floor Plan" half of PRD A6.2 is routed, not built: the PRD''s open question hands it to Decompose ("sequences behind the floor-plan run''s merge or routes them") and #4751 is still open. The "New Reservation" half has no such clause and no contract — design gap G1.'
  - "Tracker mirror created before the breakdown (children in order, then the parent) so `(tracker: #N)` and `Blocked by:` carry real numbers; child bodies follow the orchestrator's section list (Context, Spec, Acceptance criteria, Depends on, execution note) rather than the older #4745 shape."
  - "`useViewport` replaces only `TimelinePage`'s private `useIsMobile`; `TimelineGrid`'s copy stays (architecture: it sits in the #4967 file). Two viewport definitions coexist until a follow-up — recorded as G4, informational."
  - "Item 17 (#5038, appended 2026-09-04 after the Architect amendment closed G1) sits at the end of M3 by dependency (#5024, #5026, #5028 — all M2/M3) but is 17th in numbering; dispatch it after #5030 and before #5031 so #5036's final gates stay last."
surfaced:
  - 'G1 (see § Design gaps found) — PRD A6.2''s "New Reservation" action had no design; closed 2026-09-04 by architecture § Amendment 2026-09-04 and item 17 (#5038). Nothing in items 1–16 depends on it.'
  - 'PR #4967 is OPEN with `mergeStateStatus: BLOCKED` (re-measured at cut time); the floor-plan run''s tracking issue #4751 is OPEN, so the in-flight allowlist and the routed "New Floor Plan" action both hold.'
  - "No hospitality E2E workflow wiring is needed for the two new specs: `e2e.yml` runs bare `pnpm --dir apps/hospitality test:e2e` (auto-discovery), guarded by `apps/hospitality/e2e/workflow-coverage.test.ts`."
  - "`apps/hospitality/e2e/api-mocks.ts` has no `/tables/:id/status` route today; #5023 adds one so the seated rule is observable in E2E."
  - 'rialto `Text` spreads rest props onto its tag, so `PageHeader`''s `Text as="h1"` can carry `tabIndex={-1}` without a rialto change.'
---

# Breakdown: hospitality-service-ux — the service night on the Timeline

Tracking issue: **#5037**. Tree: `worktree-hospitality-service-ux` at `b9e969522` (`docs(hospitality-service-ux): architecture`). Seventeen items (item 17 appended 2026-09-04, see § Notes), five milestones, no human step before Implement. Every child issue is self-contained; the run implements them serially in this worktree as one PR.

## M1 — Rialto vocabulary — two patch changesets the app composes against

- [x] **Rialto `Drawer` gains `size="compact"`** — In `packages/rialto/src/components/Drawer/`: extend `DrawerProps.size` (`Drawer.tsx:34`) to `"default" | "wide" | "full" | "compact"`; add `.compact.bottom { height: min(40vh, 240px); }` and `.compact.right, .compact.left { width: min(320px, calc(100vw - 48px)); }` to `Drawer.module.css` beside the existing `.bottom` / `.wide.bottom` / `.full.bottom` rules; add `"compact"` to `argTypes.size.options` (`Drawer.stories.tsx:20`) and a story "Bottom sheet, compact"; TDD in `Drawer.test.tsx` (RED first): a compact bottom drawer carries the `compact` class, a compact right drawer carries it too, axe passes for the compact bottom drawer. Changeset `.changeset/drawer-compact-size.md` (`"@mattbutlerengineering/rialto": patch`, prose in the house changeset voice — see `.changeset/handshake-instrument.md`). Then `pnpm --dir packages/rialto build` (consumers typecheck against `dist`), `pnpm build --filter @mbe/cli... && pnpm regen` (rialto-catalog schemas, `packages/rialto/llms*.txt`, `packages/rialto-catalog/llms*.txt`), and stage the regenerated files by explicit path. No `setState` in a `useEffect` body (rialto pre-commit ban); no behaviour change for the three existing sizes. (tracker: #5021)
  - Accept: `Drawer.test.tsx` gains cases that were red before the CSS/prop change: `size="compact" side="bottom"` root has the `compact` class, `size="compact" side="right"` too, axe clean for the compact bottom drawer; `Drawer.module.css` contains `.compact.bottom { height: min(40vh, 240px) }` and the right/left `min(320px, calc(100vw - 48px))` rule, tokens/logical properties only; story "Bottom sheet, compact" exists and `argTypes.size.options` lists `"compact"`; `.changeset/drawer-compact-size.md` present, `patch`; `pnpm --dir packages/rialto lint`, `typecheck`, `test` green; `pnpm --dir packages/rialto build` succeeds; existing `default`/`wide`/`full` tests unchanged; `pnpm regen --check` clean after `pnpm build --filter @mbe/cli... && pnpm regen`; regenerated llms/catalog files staged by explicit path (NF rialto hygiene; A5.3 precondition).
  - Blocked by: —
- [x] **Rialto `CommandPalette` ranks matches (`rankCommandMatch`)** — In `packages/rialto/src/components/CommandPalette/CommandPalette.tsx`: replace the private `matchesQuery` (`:68-80`) with an exported pure `rankCommandMatch(label: string, query: string): 0 | 1 | 2 | 3 | null` — 0 label starts with the query, 1 a later word starts with it, 2 substring, 3 strict initials (every query character opens the next word and `query.length <= words.length`), `null` otherwise; case-insensitive; `"walk"` vs `"Waitlist"` → `null`, `"wg"` vs `"Walk-in guest"` → 3. `filtered` (`:108`) maps items to `{ item, rank }`, drops `null`, stable-sorts by rank; `grouped` (`:114-139`) orders groups by the best rank they contain when the query is non-empty (stable on `groups` order on a tie) and by `groups` order otherwise; `flatItems` (`:142`) follows the new order so ArrowDown/Enter pick the top-ranked item. `index.ts` already re-exports `*`. TDD in `CommandPalette.test.tsx` (RED first): the four tiers and `null`, the two named cases, query "walk" renders "Walk-in guest" (group Actions) first and "Waitlist" (group Navigation) not at all, ArrowDown + Enter selects the top-ranked item; the existing 19 cases unchanged. Story "Ranked results" in `CommandPalette.stories.tsx`. Changeset `.changeset/command-palette-ranking.md` (`patch`). Then the rialto build + `pnpm regen` sequence from item 1 (serial execution means the two regens never collide). (tracker: #5022)
  - Accept: `rankCommandMatch` is exported from `@mattbutlerengineering/rialto` and unit-tested for 0 / 1 / 2 / 3 / `null`, case-insensitivity, `"walk"`/`"Waitlist"` → `null`, `"wg"`/`"Walk-in guest"` → 3; with items "Walk-in guest" (Actions) and "Waitlist" (Navigation) and query "walk", the listbox shows "Walk-in guest" first and no "Waitlist"; ArrowDown + Enter select it (A6.3 rialto half; P14); empty query keeps every item in `groups` order — the 19 existing tests pass unchanged; story "Ranked results" and `.changeset/command-palette-ranking.md` (`patch`) present; rialto lint/typecheck/test/build green; `pnpm regen --check` clean; regenerated files staged by explicit path (NF rialto hygiene).
  - Blocked by: —

## M2 — Facts, one voice, outcomes — pure modules, the error helper, the hooks, and an honest E2E harness

- [x] **E2E mock — local-day re-dating and a table-status overlay (`api-mocks.ts`)** — In `apps/hospitality/e2e/api-mocks.ts`: `todayReservations()` (`:279-280`) computes `today` with `new Date().toLocaleDateString("en-CA")` and a comment naming `src/utils/local-clock.ts` `localDateString` as its twin (app and mock agree on every runner, UTC or Pacific); add a `PATCH **/api/v1/tables/*/status` route backed by a per-context `tableUpdates` Map (none exists today — `grep 'tables/.*status'` finds nothing) that returns the table with its new status and makes the tables list GET reflect it; `buildReservationsList()` (`:297`) overlays `tableUpdates` onto each reservation's embedded `table.status` so a seated party reads OCCUPIED on refetch; the walk-in route (`:350`) returns the created reservation dated today with the request's `tableId` and the matching `table` object. Delete no fixture. Add one assertion to `timeline.spec.ts` that fixes the clock at `${localToday}T23:30:00` with `page.clock.setFixedTime` before `goto` and still finds the fixture blocks (the P01 class). Run the seven existing specs (`timeline`, `walkin`, `timeline-interaction`, `timeline-keyboard-nav`, `reservations`, `waitlist`, `dashboard`) green; no workflow wiring — `e2e.yml` runs bare `pnpm --dir apps/hospitality test:e2e` (auto-discovery, guarded by `workflow-coverage.test.ts`). Dispatch `e2e-selector-drift-reviewer` on the diff. (tracker: #5023)
  - Accept: `todayReservations()` uses `toLocaleDateString("en-CA")` and no `toISOString().slice(0, 10)` remains in `api-mocks.ts`; the comment names `localDateString` as its twin; `timeline.spec.ts` at a fixed 23:30 local clock still renders the fixture reservation blocks (P01 mock half); `PATCH **/api/v1/tables/*/status` stores the status; the next `GET /api/v1/reservations?*` returns `table.status` equal to the stored value for every reservation with that `tableId`; the tables GET reflects it (A4.2 precondition); walk-in response has `date === localToday`, `startTime` on today and `tableId`/`table` from the request body; the request shapes the mock accepts are unchanged (A4.3); all seven existing specs green locally; `e2e-selector-drift-reviewer` findings addressed; no fixture file deleted.
  - Blocked by: —
- [x] **Pure facts — `local-clock`, `seated`, `timeline-intent` — and the two walk-in intent producers** — Three new pure modules under `apps/hospitality/src/utils/`, each TDD (RED test first; `vi.useFakeTimers()` + `vi.setSystemTime` where a clock matters): `local-clock.ts` — `localDateString(now)` (`toLocaleDateString("en-CA")`), `localHour(now)` (`getHours()`), `isLocalToday(date, now)`; an invalid `Date` makes `isLocalToday` false and never throws; tests at 23:30 and 00:30 local across a UTC boundary. `seated.ts` — `isSeated(reservation, tableStatus, now)` = `status === "CONFIRMED" && tableStatus === "OCCUPIED" && now ∈ [startTime − 15 min, endTime]`, and `seatedReservationIds(reservations, tables, now): ReadonlySet<string>` looking each reservation up by `tableId` in the authoritative tables list (absent table → not seated; invalid `startTime` → false); tests at both window edges, missing table, non-CONFIRMED. `timeline-intent.ts` — `parseTimelineIntent(params): { walkIn: boolean; selectedId: string | null }` (`walkin=true` exact, `selected` trimmed or `null`) and `stripTimelineIntent(params)` returning a new `URLSearchParams` with both keys removed and every other key intact (`date` stays with `useUrlParams`). Plus the two producers: `hooks/use-command-palette.ts:55-58` label becomes "Walk-in guest" (it already navigates to `/timeline?walkin=true`) and `groups` (`:113`) becomes `["Actions", ...sections]` so Actions win a rank tie; `pages/HomePage.tsx:64-65` "New Walk-In" becomes "Walk-in" navigating to `/timeline?walkin=true`. Update `use-command-palette.test.ts` (label, `groups[0]`, path) and `HomePage.test.tsx`. Consumers of the three modules arrive in items 7, 12, 14 and 15; no rialto import in the utils. (tracker: #5024)
  - Accept: `local-clock.test.ts`, `seated.test.ts`, `timeline-intent.test.ts` exist and were red before green; `localDateString(new Date("2026-09-04T06:30:00Z"))` under `TZ=America/Los_Angeles` is `"2026-09-03"` and `isLocalToday("2026-09-03", …)` is true (A1.1/A1.2 fact); `localHour` reads `getHours()` (A2.1 fact); `isSeated` is false at `startTime − 16 min` and `endTime + 1 s`, true at `startTime − 15 min` and `endTime`, false for PENDING or a non-OCCUPIED table; `seatedReservationIds` ignores `reservation.table` and uses the tables list (A4.2 rule; ux.md Decision (a)); `parseTimelineIntent(new URLSearchParams("walkin=TRUE"))` → `walkIn: false`; `selected= x ` → `"x"`; `stripTimelineIntent` keeps `date`, removes both keys and returns a different instance (A6.1 contract; xcut A); `use-command-palette.test.ts` asserts label "Walk-in guest", `groups[0] === "Actions"`, path `/timeline?walkin=true` (A6.3 app half); `HomePage.test.tsx` asserts the Walk-in quick action navigates to `/timeline?walkin=true` (A6.4 producer); `pnpm --dir apps/hospitality lint`, `typecheck`, `test` green.
  - Blocked by: —
- [ ] **`describeApiError`, the evolved `ErrorRetryBanner`, and the B1 source guard** — New `apps/hospitality/src/lib/describe-api-error.ts` (mirror `describe-auth-error.ts`): `describeApiError(error: unknown): ApiErrorDescription` `{ category: ErrorCategory | "network" | "timeout" | "unknown"; detail; retryable; recovery: "retry" | "edit" | "sign-in" | "refresh" | "none"; raw }` — categorises `ApiClientError.category` (`@mbe/api-client`), a network `TypeError`, a `DOMException` named `TimeoutError`/`AbortError`, everything else `unknown`; `detail` is the ux.md § Copy row verbatim, except `problemDetails.detail` verbatim for `conflict`/`validationError`/`badRequest` when non-empty; `raw` = `error.message` or `String(error)`; never throws, never puts `undefined` or a bare status code in `detail`. TDD `describe-api-error.test.ts`: one case per copy row, network/timeout/non-error, `problemDetails.detail` precedence, a snapshot of every sentence. Evolve `components/ErrorRetryBanner.tsx` in place: `{ title?: string; error: string; details?: string; onRetry?: () => void; onDismiss?: () => void }` — `Alert variant="error"` with `title`, `error` as the body, `details` inside a `Collapsible` (trigger "Show details", body `Text variant="caption"`), Retry `Button variant="secondary" size="md"` only when `onRetry`, dismiss only when `onDismiss`; a `@media (pointer: coarse), (max-width: 1024px)` 44 px min-size rule in a new `ErrorRetryBanner.module.css`; the nine existing string callers keep compiling (`onRetry` now optional). Extend `ErrorRetryBanner.test.tsx` (6 cases today). Add `lib/describe-api-error.guard.test.ts`: walks `src/**/*.{ts,tsx}` excluding `*.test.*`, fails on `/\b(?:err|error|fetchError|queryError|loadError|saveError|e)\s*\??\.message\b/` outside (a) the permanent allowlist with a reason per entry — `lib/describe-api-error.ts`, `lib/describe-auth-error.ts`, `pages/AuthFailurePage.tsx`, `components/booking-widget/PaymentStep.tsx`, `pages/FloorPlanEditorPage.tsx`, `components/floor-plan/**`, `components/venue-onboarding/**`, `hooks/useFloorPlans*` — and (b) a `PENDING_ADOPTION` list naming the 20 files that hit today: `pages/TimelinePage.tsx`, `pages/WaitlistPage.tsx`, `hooks/useBookingFlow.ts`, `pages/SettingsPage.tsx`, `pages/ProfilePage.tsx`, `pages/FloorPlansPage.tsx`, `pages/SetupHoursPage.tsx`, `pages/ReservationsPage.tsx`, `pages/HomePage.tsx`, `pages/GuestsPage.tsx`, `pages/BriefingPage.tsx`, `pages/BookingWidgetDemoPage.tsx`, `pages/AdminPage.tsx`, `hooks/use-form-state.ts`, `components/timeline/WalkInDialog.tsx`, `components/timeline/StaffDepositSection.tsx`, `components/timeline/EditReservationDrawer.tsx`, `components/timeline/CancelReservationDialog.tsx`, `components/reservations/NewReservationDialog.tsx`, `components/booking/WaitlistJoinView.tsx`. Each later item deletes its own entries and item 16 deletes the list; the guard also fails when a `PENDING_ADOPTION` entry no longer hits, so the list cannot go stale. (tracker: #5025)
  - Accept: `describeApiError` returns the ux.md § Copy sentence for every `ErrorCategory` row plus `network` (a `TypeError`, retryable), `timeout` (`DOMException` `TimeoutError`/`AbortError`) and `unknown` (`undefined`, a string, a plain object); a 409/422/400 with non-empty `problemDetails.detail` returns that exact `detail`; `raw` is the original message; nothing throws; no `detail` contains `undefined` or a bare status code (B1.1, B1.2); the snapshot of every sentence matches ux.md § Copy verbatim (NF TONE); `ErrorRetryBanner` renders `title`, body, "Show details" (only with `details`), Retry (only with `onRetry`), dismiss (only with `onDismiss`); `role="alert"`; axe clean; Retry min-size 44 px under the coarse-pointer query; the nine existing callers typecheck unchanged; `describe-api-error.guard.test.ts` is green with the permanent allowlist (reasons in source) and the 20-entry `PENDING_ADOPTION` list, and goes red when any pending entry is removed without adopting the helper — verified once by hand and reverted (B1.4); `pnpm --dir apps/hospitality lint`, `typecheck`, `test` green.
  - Blocked by: —
- [ ] **Outcome hooks — `useStatusMessage` + `LiveStatus`, `useFocusAfter`, `useViewport` — and a focusable `PageHeader` h1** — `hooks/useStatusMessage.ts` → `{ status: { seq, text } | null, announce(text) }` (two calls in one tick keep the last) and `components/LiveStatus.tsx` rendering `<div role="status" aria-live="polite" className={visuallyHidden}><span key={seq}>{text}</span></div>`, mounted from first render so later changes are announced (the keyed span remounts, so an identical sentence is announced again). `hooks/useFocusAfter.ts` → `{ focusAfter(target) }` with `FocusTarget = { kind: "element"; element } | { kind: "testId"; testId } | { kind: "pageHeading" }`; an effect after every commit resolves the pending target (`element`, `[data-testid="…"]`, or the `PageHeader` `h1`), focuses it and clears it; an unresolved target is kept until replaced. `hooks/useViewport.ts` → `"phone" | "tablet" | "desktop"` from `(max-width: 767px)` / `(max-width: 1024px)` — `useState` initialised from `matchMedia` plus `change` listeners (the shape of the private `useIsMobile` in `TimelinePage.tsx:33`; do not touch `TimelineGrid.tsx`'s copy). `components/PageHeader.tsx`: `Text as="h1"` gains `tabIndex={-1}` (rialto `Text` spreads rest props onto the tag — verified) so `pageHeading` targets resolve. TDD: `useStatusMessage.test.tsx`, `useFocusAfter.test.tsx` (element / testId / pageHeading, deferred resolution, replacement), `useViewport.test.ts` (mocked `matchMedia`), `PageHeader.test.tsx` (h1 focusable; existing cases unchanged). Consumers arrive in items 7, 8, 10, 11 and 14. (tracker: #5026)
  - Accept: `announce("Seated.")` twice yields two `seq` values and `LiveStatus` remounts the span (DOM identity changes); `LiveStatus` has `role="status"`, `aria-live="polite"` and is visually hidden; two `announce` calls in one tick keep the last (B3.1 seam); `focusAfter({ kind: "testId", testId: "reservation-block-1" })` requested before the element exists focuses it once it renders and only once; `{ kind: "element" }` focuses immediately after commit; `{ kind: "pageHeading" }` focuses the `PageHeader` `h1`, which carries `tabIndex="-1"`; a replaced target is dropped (B3.2 seam; P06/P16 mechanism); `useViewport` returns `phone` at 500 px, `tablet` at 900 px, `desktop` at 1200 px and updates on the `change` event; listeners removed on unmount (A5.3 viewport); `PageHeader.test.tsx` existing cases unchanged; the `h1` is focusable and still the page's only `h1`; lint/typecheck/test green.
  - Blocked by: —

## M3 — Surfaces off the grid — Briefing, Reservations, the B1 sweep, Waitlist

- [ ] **Briefing — local segments, one formatter, guest signals, honest empty/error states (`briefing.spec.ts`)** — Extract `components/crm/guest-signals.ts` from `GuestCard.tsx` (`ALLERGY_KEYWORDS` `:22`, `getSegmentLabel` `:38`, `getSegmentVariant` `:45`, plus a new `isAllergyTag(tag)`), `GuestCard` importing instead of defining; TDD `guest-signals.test.ts`. In `pages/BriefingPage.tsx`: export the pure `segmentForHour(hour): "early" | "dinner" | "late"` (`< 18` early, `18–20` dinner, `≥ 21` late) replacing `getSegmentForTime`'s `getUTCHours` (`:34`), fed by `localHour(new Date(entry.startTime))` (item 4); drop the local `formatTime` (`:43`) for `utils/format.ts` `formatTime` ("5:30 PM"); `BriefingCard` (`:67`) shows the segment `Badge` via `getSegmentLabel`/`getSegmentVariant`, the occasion `Badge`, and per-tag dietary `Tag`s (`isAllergyTag` → `variant="error"` prefixed "Allergy:", else `"default"`), other `guest.tags` hidden (replaces the all-or-nothing `hasAllergy` at `:69`/`:120`); loading and empty copy (whole day / one segment) per ux.md Screen 1; error → `ErrorRetryBanner` title "Couldn't load tonight's briefing." with `describeApiError(error).detail`, `details` raw, Retry calling `useBriefing`'s `refetch`, a `useStatusMessage` announcement on success with `LiveStatus` mounted; remove `pages/BriefingPage.tsx` from the guard's `PENDING_ADOPTION`. `components/DashboardLayout.tsx:34` `ROUTE_LABELS.briefing = "Tonight's Service"`. Tests: `BriefingPage.test.tsx` (`vi.setSystemTime` local; 17:30 / 18:30 / 21:00 bucketing with the printed "6:30 PM"; VIP badge; per-tag allergy variant; times; Retry + announcement), `DashboardLayout.test.tsx` (breadcrumb), `GuestCard.test.tsx` unchanged. New `e2e/briefing.spec.ts` (segments, VIP, allergy, breadcrumb; `page.clock.setFixedTime` local 20:00; test ids where a label repeats). Dispatch `e2e-selector-drift-reviewer`. (tracker: #5027)
  - Accept: bookings at local 17:30 / 18:30 / 21:00 file under Early / Dinner / Late and print "5:30 PM" / "6:30 PM" / "9:00 PM" (A2.1; P08) from the same `formatTime` the Timeline uses (A2.2, A10.3); `tags: ["VIP"]` renders the VIP badge on the card (A10.1); `dietaryRestrictions: ["nut allergy", "vegetarian"]` renders exactly one `error` tag "Allergy: nut allergy" and one `default` tag (A10.2); breadcrumb on `/briefing` reads "Tonight's Service" (A10.4); `useBriefing` 500 → one `role=alert` with "Couldn't load tonight's briefing." and the `serverError` sentence, no request line in the DOM; Retry → cards and a `role=status` sentence (B1.1 Briefing); guard `PENDING_ADOPTION` loses `pages/BriefingPage.tsx` and stays green; `briefing.spec.ts` green locally; drift reviewer findings addressed; lint/typecheck/test green.
  - Blocked by: #5024, #5025, #5026
- [ ] **Reservations — `KpiStat`, honest loading/error/empty states, `ReservationList` labels (`reservations.spec.ts`)** — New `components/KpiStat.tsx` `{ label; value: number | string | null | undefined; size? }` rendering rialto `Stat` with `value ?? "—"` and, only when the value is nullish, a conditional spread of `aria-label` = "<label>, unavailable" (an explicit `undefined` would erase `Stat`'s own label); TDD `KpiStat.test.tsx`. `pages/ReservationsPage.tsx`: the four `Stat`s (`:150-154`) become `KpiStat`s (dashes while loading or failed); loading (`:134`) → five `Skeleton variant="text"` rows inside `role="status" aria-busy`; error → `ErrorRetryBanner` title "Couldn't load reservations." with the helper's sentence, `details` raw, Retry → `useReservations().refetch()`; on Retry success announce via `useStatusMessage` + `LiveStatus` and `focusAfter({ kind: "pageHeading" })`; `EmptyState` copy per ux.md Screen 8 (`:202-204`); remove `pages/ReservationsPage.tsx` from `PENDING_ADOPTION`. `components/dashboard/ReservationList.tsx:64-65` renders `STATUS_LABEL[r.status]` (`utils/reservation-display.ts`). Tests: `ReservationsPage.test.tsx` (500 → banner + Retry + four dashes with "unavailable" labels; Retry → rows + KPIs + announcement + heading focus), `ReservationList.test.tsx` (no raw enum). `e2e/reservations.spec.ts`: 500 → banner + Retry + dashes; a stateful route override that flips to 200 → rows + KPIs without reload. Dispatch `e2e-selector-drift-reviewer`. (tracker: #5028)
  - Accept: `KpiStat` with `null` renders "—" and `aria-label="<label>, unavailable"`; with `12` renders `Stat`'s own label untouched (A9.1 `KpiStat` half); `/reservations` with `GET /api/v1/reservations*` → 500 shows one `role=alert` with "Couldn't load reservations." and the `serverError` sentence, a Retry, four "—" KPIs, and no request line anywhere in the DOM (B2.1; T3); Retry after the mock flips to 200 restores rows and KPIs without `page.reload()`, a `role=status` sentence appears within 1 s and `document.activeElement` is the `h1` (B2.2; B3.1/B3.2 Reservations); loading renders five text skeletons inside `role="status"`; empty renders the Screen 8 `EmptyState` (R1); `ReservationList` never renders `CONFIRMED`/`PENDING`/`COMPLETED` raw (B1.3); guard loses `pages/ReservationsPage.tsx`; `reservations.spec.ts` green locally; drift reviewer findings addressed; lint/typecheck/test green.
  - Blocked by: #5025, #5026
- [ ] **B1 adoption sweep — every remaining page, the guest booking widget, and the shared catches** — Replace the raw `err.message` / `error.message` renders with `describeApiError(…)` in: `pages/HomePage.tsx:59` (`ErrorRetryBanner` title "Couldn't load the dashboard.", `error={description.detail}`, `details={description.raw}`), `pages/GuestsPage.tsx`, `pages/SettingsPage.tsx`, `pages/ProfilePage.tsx`, `pages/FloorPlansPage.tsx` (the page only — `components/floor-plan/**` stays untouched), `pages/AdminPage.tsx`, `pages/BookingWidgetDemoPage.tsx`, `pages/SetupHoursPage.tsx:44`, `components/timeline/StaffDepositSection.tsx:50`, `components/reservations/NewReservationDialog.tsx:140`, `hooks/use-form-state.ts:49` (generic fallback → `describeApiError(err).detail`), `hooks/useBookingFlow.ts:450,485,534` and `components/booking/WaitlistJoinView.tsx:74` (guest widget, Host table verbatim — the `network` row's guest wording is design gap G3, recorded, not solved here). Surface titles from ux.md § Copy; where a page has a banner keep `ErrorRetryBanner`, where a dialog or form shows inline text keep its element and feed it the sentence. Remove these 13 files from `PENDING_ADOPTION`. Update each file's existing tests (`HomePage.test.tsx`, `SettingsPage.test.tsx`, `ProfilePage.test.tsx`, `GuestsPage.test.tsx`, `useBookingFlow.test.ts`, …) to assert the sentence and never the raw message; add one manager-side E2E assertion (profile save 500 → sentence, the R4 class) in `e2e/dashboard.spec.ts` or a small new spec. Touch no in-flight file and nothing under `services/**` or Prisma. (tracker: #5029)
  - Accept: after this item `PENDING_ADOPTION` contains exactly `pages/TimelinePage.tsx`, `pages/WaitlistPage.tsx`, `components/timeline/WalkInDialog.tsx`, `components/timeline/EditReservationDrawer.tsx`, `components/timeline/CancelReservationDialog.tsx`, and the guard is green; for each adopted surface a unit test asserts a ux.md § Copy sentence renders on a `serverError` and the raw message does not (B1.1; T11, R4); `use-form-state` on a 422 carrying `problemDetails.detail` shows that detail (B1.2); the booking widget (`useBookingFlow`, `WaitlistJoinView`) shows the `serverError` sentence on a 5xx (B1.1 "including the booking widget"); `git diff --name-only` contains no path under `components/floor-plan/`, `components/venue-onboarding/`, `pages/FloorPlanEditorPage.tsx`, `hooks/useFloorPlans*`, `pages/VenueOnboardingPage.tsx`, `services/`; lint/typecheck/test green; the E2E assertion green locally.
  - Blocked by: #5025
- [ ] **Waitlist — spoken outcomes, focus targets, a toast to the Timeline, sentence errors (`waitlist.spec.ts`)** — `pages/WaitlistPage.tsx`: mount `LiveStatus`; `useStatusMessage` sentences from ux.md § Copy after add / seat / notify / cancel; `useFocusAfter` targets per the ux.md table (the Guest name field after add; the next entry card or the "No one waiting" heading after seat/cancel; Notify after notify); `handleSeat` (`:235`) keeps the `Reservation` returned by `api.reservations.walkIn` (`:244`) and on success calls `useToast().toast({ variant: "success", action: { label: "View on Timeline", onClick } })` navigating to `/timeline?date=<localDateString(new Date())>&selected=<reservation.id>`; errors via `describeApiError` into `ErrorRetryBanner` (titles "Not added." / "Not seated." / "Not notified.") replacing `setSubmitError(err.message)` (`:96`) and the row-level equivalents; the split-failure sentence (`:256`) reworded per ux.md Screen 9; the two `aria-live` regions (`:386`/`:392`) collapse into `LiveStatus`; a coarse-pointer 44 px rule for row buttons in `WaitlistPage.module.css`; remove `pages/WaitlistPage.tsx` from `PENDING_ADOPTION`. Tests: `WaitlistPage.test.tsx` (one sentence per outcome within one `act`, focus per target, toast action navigates to the `selected=` URL, error titles, split-failure wording). `e2e/waitlist.spec.ts`: sentences in `role=status` within 1 s; `document.activeElement` after add/seat/notify/cancel; toast with "View on Timeline". The `selected=` consumer lands in item 14 (the URL is produced now, consumed then). Dispatch `e2e-selector-drift-reviewer`. (tracker: #5030)
  - Accept: after each of add / seat / notify / cancel a `role=status` element contains the ux.md sentence within 1 s (B3.1; P09/P09b) and `document.activeElement` is the named control, never `<body>` (B3.2); seat success shows a success toast whose "View on Timeline" action navigates to `/timeline?date=<localToday>&selected=<id>` with the id from the walk-in response; a 500 on add shows title "Not added." with the `serverError` sentence and no request line; seat / notify failures show their titles (B1.1 Waitlist); the split failure (reservation created, entry not updated) shows the Screen 9 wording; row controls ≥ 44 × 44 at 1024 × 768 (NF INCLUSIVE); guard loses `pages/WaitlistPage.tsx`; `waitlist.spec.ts` green locally; drift reviewer findings addressed; lint/typecheck/test green.
  - Blocked by: #5024, #5025, #5026
- [ ] **⌘K "New Reservation" opens the dialog — reservations intent** — Per architecture § Amendment 2026-09-04 (closes G1). `utils/timeline-intent.ts` (from #5024) gains `parseReservationsIntent(params: URLSearchParams): { newReservation: boolean }` (`new=true` exact, case-sensitive value) and `stripReservationsIntent(params)` (a new `URLSearchParams` without `new`, every other key intact); TDD in `timeline-intent.test.ts` (RED first). `hooks/use-command-palette.ts`: `action-new-reservation` navigates to `/reservations?new=true` (today `/timeline`); label unchanged; `use-command-palette.test.ts` asserts the path. `pages/ReservationsPage.tsx` (after #5028's rewrite): `useSearchParams` beside `useUrlParams`; `parseReservationsIntent` on render — derived, no state, no effect; render `NewReservationDialog` when `(intent.newReservation || showNewReservationDialog) && selectedVenueId && !tablesLoading` (`useTables`' `isLoading` — the dialog seeds `tableId` from `tables` once at mount, so a URL-open must not race the tables query); `onClose` and `handleCreateReservation` both clear the state and call `setSearchParams(prev => stripReservationsIntent(prev), { replace: true })`; close-without-create from a URL-opened dialog → `focusAfter({ kind: "element", element: <New reservation button ref> })` (#5026), a click-opened dialog keeps the dialog's own restore; create success → `focusAfter({ kind: "pageHeading" })`. No new `role=status` sentence (ux.md § Copy has none for a booked reservation). Tests: `ReservationsPage.test.tsx` (`MemoryRouter initialEntries={["/reservations?new=true"]}` → dialog once tables have loaded, not while `isLoading`; close → URL without `new`, focus on the New reservation button; confirmed create → focus on the `h1`; no venue → no dialog, param kept). E2E: one step in `e2e/reservations.spec.ts` — `Meta+K` (rialto palette: `role="dialog"` "Command palette", `role="combobox"` "Search commands"), type "new res", Enter → `/reservations` with `getByRole("dialog", { name: "New Reservation" })` visible and focus inside; Escape → URL without `new`. Dispatch `e2e-selector-drift-reviewer`. Touch nothing under `components/floor-plan/**`; "New Floor Plan" stays routed (#4751). (tracker: #5038)
  - Accept: `timeline-intent.test.ts` gains cases that were red before green: `parseReservationsIntent(new URLSearchParams("new=true"))` → `{ newReservation: true }`, `new=TRUE` / `new=1` / absent → `false`, `stripReservationsIntent` returns a different instance without `new` and with `date` / `status` intact; `use-command-palette.test.ts` asserts `action-new-reservation` → `/reservations?new=true`; `ReservationsPage.test.tsx`: `/reservations?new=true` renders the dialog once `useTables` has loaded and not while `isLoading`, closing leaves no `new` in the URL and `document.activeElement` is the New reservation button, a confirmed create leaves `document.activeElement` on the `h1`, no venue → no dialog and the param stays; `reservations.spec.ts`: ⌘K, "new res", ⏎ ends on `/reservations` with the New Reservation dialog open and focus inside it, Escape leaves no `new` in the URL (A6.2 second clause; P07's shape; A6.1's strip rule by mirror); drift reviewer findings addressed; `git diff --name-only` contains no path under `components/floor-plan/`; `pnpm --dir apps/hospitality lint`, `typecheck`, `test` green; `reservations.spec.ts` green locally.
  - Blocked by: #5024, #5026, #5028

## M4 — The Timeline — dialogs, leaves, the page, and the grid last

- [ ] **Timeline dialogs own their failure — rethrow contract, in-dialog banners, walk-in first focus and 44 px targets** — `components/timeline/WalkInDialog.tsx`: `onConfirm` may reject — the dialog catches (`:90-92`), renders `ErrorRetryBanner` title "Walk-in not seated." with `describeApiError(err)` (`details` raw), resets `isLoading` in `finally`, keeps field values, stays open, leaves focus on the submit button (409/422 → the Table control); title "Seat walk-in" (`:116`), primary "Seat now" / `loadingText="Seating…"` (`:183-185`); party-size `Button size="md"` with `aria-pressed` (`:141-145`) and a `@media (pointer: coarse), (max-width: 1024px)` 44 px rule in `WalkInDialog.module.css`; an effect declared after `useFocusTrap` (`:51`) focuses the pressed party-size control; the no-table caption (`Text variant="caption"`) also referenced by the disabled button's `aria-describedby`; delete `previouslyFocusedRef` (`:40-47`) — the page owns focus return. `CancelReservationDialog.tsx`: same rethrow/catch/banner ("Reservation not cancelled."), delete `previouslyFocusedRef` (`:36-43`), catch at `:57-58`. `EditReservationDrawer.tsx`: same ("Changes not saved."), catch at `:85-86`. `TimelinePage.tsx` is not rewritten here — keep it compiling with a two-line bridge per handler (rethrow after the existing `setError`), removed in item 14. Remove the three dialog files from `PENDING_ADOPTION`. Tests: `WalkInDialog.test.tsx` (28 today — add: rejecting `onConfirm` → banner sentence, button back to "Seat now" within a tick, values kept, focus on the button; 409 with `problemDetails.detail` → that detail and focus on the Table control; initial focus on the pressed control surviving a double-run effect; caption + `aria-describedby` when no table fits), `CancelReservationDialog.test.tsx`, `EditReservationDrawer.test.tsx`. `e2e/walkin.spec.ts`: rename the strings ("Seat walk-in", "Seat now") in the same commit so the suite never goes red. (tracker: #5031)
  - Accept: `WalkInDialog` with `onConfirm` rejecting an `ApiClientError` 500: dialog stays open, one `role=alert` with "Walk-in not seated." and the `serverError` sentence, no request line, "Seat now" resting within 1 s, party size and table unchanged, `document.activeElement` is the submit button (A3.1 dialog half; P03; xcut C, C2); a 409 carrying `problemDetails.detail` shows that detail and focus lands on the Table control (B1.2); on open, `document.activeElement` is the pressed party-size button, also under a double-invoked effect (B3.3 optional; xcut E; P16); every party-size button ≥ 44 × 44 at 1024 × 768 (NF INCLUSIVE; xcut H); no `document.activeElement` capture remains in the three files; Cancel / Edit dialogs render "Reservation not cancelled." / "Changes not saved." on rejection and stay open (A3.2 cancel/edit half); guard loses the three dialog files; `walkin.spec.ts` green with the renamed strings; lint/typecheck/test green.
  - Blocked by: #5025
- [ ] **Timeline leaves — `ReservationDetails` extraction, `ReservationSheet`, `TimelineSkeleton`, `TimelineMobileView` Seated rule** — Move the private `ReservationDetails` (`TimelinePage.tsx:63-186`) to `components/timeline/ReservationDetails.tsx` with: status word "Seated" when the new `seated: boolean` prop is true (computed by the page from `seatedReservationIds`) else `STATUS_LABEL`; the caption "Table 4 is still occupied — turn it or move the party." when the table (looked up in the authoritative `tables` prop) is OCCUPIED and the party is not seated; Seat Guest rendered when `status ∈ {PENDING, CONFIRMED}` and the table is not OCCUPIED; times via `formatTime`; `onSeat: () => Promise<void>` with a local `ErrorRetryBanner` title "Guest not seated." on rejection, focus staying on the button; "table unknown" when the table is absent. New `components/timeline/ReservationSheet.tsx`: `Drawer side="bottom" size={expanded ? "default" : "compact"} title` (item 1) with the summary row, tag row (segment badge + allergy tags via `guest-signals`), action row (each ≥ 44 px), and **More ▾** toggling `expanded` (reveals `GuestCard`, email, phone, notes) — height change instant, no transition. New `components/timeline/TimelineSkeleton.tsx`: `Skeleton variant="rect" height={40}` + five `height={60}` inside `<div role="status" aria-busy="true" data-testid="timeline-skeleton">` with visually-hidden text "Loading tonight's grid…". `components/timeline/TimelineMobileView.tsx`: `STATUS_FILTERS` (`:9-13`) chip label "Seated" → "Confirmed" (still filters CONFIRMED ∪ COMPLETED, `:146`); new `seatedIds?: ReadonlySet<string>` prop → a "Seated" mark on a card only when its id is in the set. Export the new components from `components/timeline/index.ts`. Edit `TimelinePage.tsx` only to import `ReservationDetails` from its new file and pass `seated={false}` for now (the real set arrives in item 14). TDD: `ReservationDetails.test.tsx`, `ReservationSheet.test.tsx` (compact ↔ default on More, axe), `TimelineSkeleton.test.tsx`, new `TimelineMobileView.test.tsx` (chip "Confirmed" filters CONFIRMED ∪ COMPLETED; "Seated" mark only for ids in `seatedIds`); extend the `../components/timeline` mock factory in `TimelinePage.test.tsx` with the new exports. (tracker: #5032)
  - Accept: CONFIRMED + OCCUPIED + seated → no Seat Guest and status "Seated"; PENDING + AVAILABLE → Seat Guest shown; CONFIRMED + OCCUPIED outside the window → the "Table 4 is still occupied — turn it or move the party." caption and no Seat Guest; missing table → "table unknown" (A4.1; A4.2 sidebar half; P05); a rejected `onSeat` renders "Guest not seated." with the sentence and focus stays on the button (A3.2 seat half); `ReservationSheet`'s drawer has the `compact` class until More is pressed, `default` after, `compact` again on the second press; every action ≥ 44 px under the coarse-pointer query; axe clean (A5.3 sheet; P13; xcut H); `TimelineSkeleton` has `role="status"`, `aria-busy="true"`, the hidden loading text, `data-testid="timeline-skeleton"` and no count text (A9.1 skeleton half); the phone chip reads "Confirmed" and a card shows "Seated" only for ids in `seatedIds` (A4.2 phone half; P12); times print "5:30 PM" (A10.3); `TimelinePage.test.tsx` (49 cases) still green after the import swap; lint/typecheck/test green.
  - Blocked by: #5021, #5024, #5025, #5027
- [ ] **Grid leaves — `TableStatusMenu` (+ `tableStatusMenuItems`), `TimelineEmptyNight`, `useScrollToNow`** — New files only — no `TimelineGrid.tsx` edit (that is item 15). `components/timeline/TableStatusMenu.tsx`: exported pure `tableStatusMenuItems(status: TableStatus): { id: TableStatus; label: string }[]` — one item per `TABLE_VALID_TRANSITIONS[status]` (`@mbe/types`) labelled "Mark <state>" (lower-case state word), unknown status → `[]`; component `{ tableId; tableName; status; pending?: boolean; onChange(next: TableStatus) }` = rialto `DropdownMenu` whose `trigger` is `Button variant="ghost"` carrying `StatusLED` (variant by status) + the status word + a chevron, `aria-label="Table 3: Occupied. Change status"`, `data-testid="table-status-<tableId>"`, min 44 × 44 inside the 60 px row via `TableStatusMenu.module.css` (`@media (pointer: coarse), (max-width: 1024px)`), `pending` → the gold in-flight treatment (`--rialto-accent`) and a disabled trigger; nothing is sent on trigger activation — `onChange` fires only on item select. `components/timeline/TimelineEmptyNight.tsx`: `EmptyState variant="flat" size="sm"` overlay with `{ variant: "today" | "otherDate"; dateLabel; onWalkIn; onToday }` → "Quiet so far." + Walk-in (primary) or "Nothing on the book for <dateLabel>." + Back to today (secondary), copy per ux.md Screen 7, `data-testid="timeline-empty-night"`, no `alert` role. `components/timeline/useScrollToNow.ts`: `useScrollToNow(scrollRef, currentTimeOffset | null)` — on mount and whenever the offset becomes non-null sets `scrollLeft = max(0, tableColumnWidth + offset − 0.25 × clientWidth)` with `behavior: "instant"` when `useMotionPreset().precision.duration === 0`, else `"smooth"`; no-op when the offset is null. Barrel exports. TDD: `TableStatusMenu.test.tsx` (`tableStatusMenuItems` per state incl. unknown; accessible name; Enter/ArrowDown opens the menu with no `onChange`; selecting calls `onChange` once with the chosen state; Escape closes with no callback; `pending` disables; axe), `TimelineEmptyNight.test.tsx` (both variants, actions, no alert role), `useScrollToNow.test.tsx` (25 % rule, clamp ≥ 0, instant under reduced motion with `useMotionPreset` mocked, no-op without a now-line). (tracker: #5033)
  - Accept: `tableStatusMenuItems("AVAILABLE")` is the `TABLE_VALID_TRANSITIONS.AVAILABLE` projection with "Mark …" labels; unknown → `[]`; the trigger's accessible name states current state and action, e.g. "Table 3: Occupied. Change status" (A8.1; P15); Enter/ArrowDown opens the menu and calls nothing; choosing an item calls `onChange` exactly once with that state; Escape closes with no callback (A8.2 unit half); the trigger's min-size is 44 × 44 under the coarse-pointer query (A8.3 unit half); `pending` disables the trigger and applies the accent treatment; axe clean; `TimelineEmptyNight` `today` renders "Quiet so far." and a Walk-in button, `otherDate` renders "Nothing on the book for <dateLabel>." and Back to today; neither has `role="alert"` (A7.1/A7.2 leaf half; P02); `useScrollToNow` sets `scrollLeft` to `tableColumnWidth + offset − clientWidth / 4` with `"smooth"`, `"instant"` under reduced motion, never below 0, and does nothing when the offset is null (A5.1 unit); no diff in `TimelineGrid.tsx`; lint/typecheck/test green.
  - Blocked by: —
- [ ] **`TimelinePage` orchestrator — selection by id, URL intent, load vs mutation axes, spoken and focused outcomes (`timeline-intent.spec.ts`)** — `hooks/useTimelineData.ts`: add `refetch(): Promise<void>` (both queries) and make `createWalkIn` (`:149-157`) return the `Reservation` from `api.reservations.walkIn` before `invalidateAll()`; request shapes unchanged. `pages/TimelinePage.tsx`: selection by id (`selectedId: string | null`, `selectedReservation = reservations.find(...)`, replacing the object state at `:196`); `useSearchParams` beside `useUrlParams` — `parseTimelineIntent` on render, `selected=` selects, `intent.walkIn || showWalkInDialog` opens the dialog once `!isLoading && selectedVenueId`, every close/success path calls `setSearchParams(prev => stripTimelineIntent(prev), { replace: true })`; Escape from a URL-opened dialog → `focusAfter` the Walk-in button; replace `useIsMobile` (`:33`) with `useViewport`: phone → `TimelineMobileView` + right `Drawer`; tablet → grid + `ReservationSheet`; desktop → grid + `Card` sidebar (`TimelinePage.module.css` hides `.sidebar` below 1025 px, today 768); the grid area by fetch state only, replacing the ternary (`:410-416`): `isLoading` → `TimelineSkeleton`; `fetchError && tables.length === 0` → `EmptyState` "Couldn't load tonight." with Retry (`refetch`) and Walk-in disabled with its caption; `fetchError` with tables → `ErrorRetryBanner` "Couldn't load tonight's reservations." above a grid that stays mounted; `tables.length === 0` → "No tables yet." (action Floor plans); else the grid; delete the `error` state (`:195`) and item 11's bridges — dialogs rethrow and the page handles success only: `handleWalkIn` → `createWalkIn` → `setSelectedId`, announce (the sentence names the table), `focusAfter({ kind: "testId", testId: "reservation-block-<id>" })`; `handleSeat` → announce, focus the block (rejection is rendered by `ReservationDetails`); `handleCancel` → clear selection, announce, focus the block; `handleEdit` → announce, focus the Edit button; `seatedIds = seatedReservationIds(reservations, tables, useNow())` passed to `ReservationDetails`, `ReservationSheet` and `TimelineMobileView` (the grid prop comes in item 15); mount `LiveStatus`; the stats row → four `KpiStat`s ("—" while loading; layout unchanged so the bounding box does not move); header: `IconButton` "Previous day"/"Next day", Today `Button variant="secondary"`, Walk-in `variant="primary"`, all `size="md"` with a coarse-pointer 44 px rule in the module CSS; `todayStr`/`handleToday`/day arrows via `localDateString`. Remove `pages/TimelinePage.tsx` from `PENDING_ADOPTION`. Tests: `TimelinePage.test.tsx` (49 today; add: `MemoryRouter initialEntries={["/timeline?walkin=true"]}` opens the dialog and the param is gone after close; `selected=` selects; a rejecting walk-in leaves `timeline-grid` mounted; each failing handler leaves the grid; `fetchError` with tables → banner beside the grid, without tables → empty state; `isLoading` → skeleton and four dashes; seated party hides Seat Guest, pending shows it; one announcement per mutation; focus targets; Escape from a URL-opened dialog focuses Walk-in). E2E: `timeline.spec.ts` (skeleton before 1 s with `/tables` delayed 5 s; stats row bounding box identical before/after; fetch failure vs "No tables yet." distinguishable), `walkin.spec.ts` (500 → sentence in the dialog, block count unchanged, resting ≤ 1 s, Escape → reopen shows no stale error; success → new block selected, in viewport, table named, `role=status` sentence, `document.activeElement` is the block), new `timeline-intent.spec.ts` (`/timeline?walkin=true`; ⌘K "walk" ⏎; Dashboard "Walk-in" in ≤ 2 activations — each ends with the dialog open and focus inside), `timeline-interaction.spec.ts` (Seat Guest hidden for CONFIRMED + OCCUPIED, shown for PENDING). Dispatch `e2e-selector-drift-reviewer`. (tracker: #5034)
  - Accept: xcut A — `/timeline?walkin=true` opens `getByRole("dialog", { name: /walk-in/i })` and after Escape the URL carries no `walkin` (A6.1); `?selected=<id>` opens the panel on that reservation; P07/P14 — ⌘K "walk" ⏎ and the Dashboard "Walk-in" (≤ 2 activations, A6.4) each end with the dialog open and focus inside it (A6.2 walk-in half — the "New Reservation" / "New Floor Plan" halves are design gap G1); P03 — a walk-in 500 leaves `timeline-grid` rendered with the same block count and the error inside the dialog (A3.1 page half; xcut D — success shows no confirmation dialog, only the selected block + sentence); P11 — with `/tables` delayed 5 s, before 1 s the grid area is `timeline-skeleton` and the stats row shows four "—" (A9.1); the stats row bounding box is identical before and after load (A9.2); a page-level fetch error renders `role=alert` beside a mounted grid, is dismissible and is cleared by the next attempt (A3.3); with no tables loaded, "Couldn't load tonight." with Retry and a disabled Walk-in with its caption; "No tables yet." when the venue has none (A7.2 page half); P04/A5.2 — after a walk-in resolves the new block is selected, within the scroll viewport, the `role=status` sentence names the table and `document.activeElement` is the block; P06 — after cancel/edit focus lands on the ux.md target, never `<body>` (B3.1, B3.2); P13 — at 1024 × 768 with a selection the page renders `ReservationSheet` and ≥ 4 hours of grid stay visible (A5.3); seated party hides Seat Guest, pending shows it (A4.1 page half); `useTimelineData` sends only today's request shapes (A4.3 — `api-mocks` asserts bodies); no `err.message` remains in `TimelinePage.tsx` (B1.1 Timeline); guard loses `pages/TimelinePage.tsx`; `TimelinePage.test.tsx` green with the added cases; `timeline.spec.ts`, `walkin.spec.ts`, `timeline-intent.spec.ts`, `timeline-interaction.spec.ts` green locally; drift reviewer findings addressed; lint/typecheck/test green.
  - Blocked by: #5024, #5025, #5026, #5028, #5031, #5032
- [ ] **`TimelineGrid` (last, #4967-aware) — local now-line, scroll-to-now, `TableStatusMenu`, `TimelineEmptyNight`, `isSeated` blocks — and the page's table-status flow** — First re-measure: `git fetch origin && gh pr view 4967 --json state,mergeStateStatus` and `git log origin/main -3 -- apps/hospitality/src/components/timeline/TimelineGrid.tsx`; if #4967 has merged, rebase the worktree branch onto `origin/main` before touching the file. `components/timeline/TimelineGrid.tsx` edits at the anchors in architecture § Sequencing (≥ 3 lines from #4967's H1 ≈ 125 and H2 ≈ 239–256): `isToday = date === localDateString(currentTime)` (`:141`, replaces `toDateString`); `useScrollToNow(scrollRef, isToday ? currentTimeOffset : null)` after the offset memo (`:148`); `ref={scrollRef}` + `style={{ scrollPaddingBlockEnd: bottomInset }}` on `.gridWrapper` (`:158`); `TableStatusMenu` replacing the `TableStatusBadge` block (`:206-213`) with `pending={pendingTableId === table.id}` and `onChange={(next) => onTableStatusChange(table.id, next)}`; `TimelineEmptyNight` after the rows when `emptyNight` is set; the now-line (`:261-264`) gains `data-testid="now-line"`, an `aria-hidden` `formatTime(currentTime)` label and `--rialto-accent` (was `--rialto-error`, `TimelineGrid.module.css:107/118`); `isSeated={seatedIds?.has(reservation.id) ?? false}` on `<ReservationBlock>` — the one line inside H2, re-applied by hand if #4967 lands first. New props `seatedIds?`, `bottomInset?`, `emptyNight?`, `pendingTableId?`; `onTableStatusChange(tableId, next)`. Leave the grid's private `useIsMobile` (`:35`) alone. `ReservationBlock.tsx`: `isSeated` prop → `StatusLED variant="success" size="xs"` before the name, aria-label suffix ", seated" (`:56`), `arePropsEqual` (`:71`) compares it. `TimelinePage.tsx` (its grid-coupled remainder): pass `seatedIds`, `bottomInset` (the sheet's height on tablet), `emptyNight` (variant by `isToday`, `dateLabel`, `onWalkIn`, `onToday`) when `reservations.length === 0`, and `pendingTableId`; `handleTableStatusChange(tableId, next)` (`:291`) → set the pending id, `updateTableStatus`, on failure a dismissible `ErrorRetryBanner` "Table status not changed." with Retry re-sending the same transition, on success announce and `focusAfter({ kind: "testId", testId: "table-status-<id>" })`. `TableStatusBadge` stays for its other consumers. Tests: `TimelineGrid.test.tsx` (42 today; swap the `../TableStatusBadge.js` mock for `./TableStatusMenu.js`; add: now-line at fake 20:00 local, hour-parameterised 11–23 presence, absence on another date, `now-line` left within the grid width, `seatedIds` → glyph + suffix, `emptyNight` overlay keeps the rows, `onTableStatusChange` receives `(tableId, next)`), `ReservationBlock.test.tsx` (`isSeated`, `arePropsEqual`), `TimelinePage.test.tsx` (table-status pending / banner / announce / focus). E2E: `timeline.spec.ts` (now-line at `page.clock` 20:00 and 14:00 with the date label unchanged; `scrollLeft` puts it at ≈ 25 %; empty night keeps the rows and offers Walk-in + Today; empty vs failed vs other date distinguishable by role and text), `timeline-interaction.spec.ts` (status control accessible name; one activation opens a menu and sends no PATCH; choosing sends exactly one PATCH; ≥ 44 × 44 at 1024 × 768; tablet sheet leaves ≥ 4 hours visible). Dispatch `e2e-selector-drift-reviewer`. (tracker: #5035)
  - Accept: P01 — with `page.clock.setFixedTime(<localToday>T20:00:00)` the now-line is present and the date label unchanged; the same at 14:00; absent on another date; present for every local hour 11–23 in the unit sweep (A1.1, A1.2); on load and on Today the now-line's x is inside the viewport at ≈ 25 % of `clientWidth` (A5.1); P02 — tables present and zero reservations → "Quiet so far." + Walk-in + Today with the rows still rendered; a fetch failure and another date read differently by role and text (A7.1, A7.2); P15 — trigger name "Table 3: Occupied. Change status"; one activation opens a menu and the mock records no PATCH; choosing "Mark dirty" records exactly one `PATCH /tables/:id/status` (A8.1, A8.2); the trigger is ≥ 44 × 44 at 1024 × 768 (A8.3; P10; xcut H); a table-status failure → dismissible "Table status not changed." beside the mounted grid, Retry re-sends the same transition; success announces and focuses `table-status-<id>` (A3.2/A3.3 table-status half; B3.1/B3.2); a seated block carries the success LED and its accessible name ends "…confirmed, seated" (A4.2 block half; P05); the now-line uses `--rialto-accent` (xcut G; NF tokens); no edit inside #4967's H1 and none inside H2 except the one `isSeated` line; `TimelineGrid.test.tsx` mocks `./TableStatusMenu.js`; `timeline.spec.ts` and `timeline-interaction.spec.ts` green locally; drift reviewer findings addressed; lint/typecheck/test green.
  - Blocked by: #5023, #5024, #5032, #5033, #5034

## M5 — Docs and generated artifacts

- [ ] **Docs, generated artifacts, and the guard's clean slate** — `apps/hospitality/docs/USER-FLOWS.md`: Flow 3 step 1 (`:82`) → the shipped walk-in entry (Timeline header Walk-in, ⌘K "Walk-in guest", or Dashboard Walk-in — all open the "Seat walk-in" dialog); Flow 4 step 4 (`:113`) → Seat Guest sets CONFIRMED + OCCUPIED and the block reads "seated" while the table is occupied; one line each. `apps/hospitality/CLAUDE.md` untouched unless a flow step there changed (architecture: none — leave the stale `:122` "P0: Error recovery" line to #4992). Delete `PENDING_ADOPTION` from `lib/describe-api-error.guard.test.ts` (the list must already be empty — a leftover entry is a defect in an earlier item; fix it there). Regenerate: `pnpm build --filter @mbe/cli...` then `pnpm regen`, then `pnpm regen --check`; stage `apps/hospitality/llms.txt`, `apps/hospitality/llms-full.txt`, `packages/rialto/llms*.txt`, `packages/rialto-catalog/llms*.txt`, `llms.txt`, `llms-full.txt` by explicit path (never `git add -A` — the prettier hook keeps ~170 files dirty). `pnpm exec prettier --check docs/features/hospitality-service-ux/` clean. Final gates from inside `apps/hospitality` and `packages/rialto`: lint, typecheck, test; `pnpm --dir apps/hospitality test:e2e` locally. (tracker: #5036)
  - Accept: both `USER-FLOWS.md` lines updated, one line each, naming the shipped copy ("Seat walk-in", "Walk-in guest"); `apps/hospitality/CLAUDE.md` diff empty (NF docs); `describe-api-error.guard.test.ts` has no `PENDING_ADOPTION` symbol and is green (B1.4 final state); `pnpm regen --check` exits 0; only intended paths staged (`git status --short` reviewed); `pnpm exec prettier --check docs/features/hospitality-service-ux/` clean (NF regen); `pnpm lint`, `typecheck`, `test` green in `apps/hospitality` and `packages/rialto`; `test:e2e` green locally (NF gates).
  - Blocked by: #5035

## Design gaps found

- **G1 — closed by amendment 2026-09-04 + item 17 (#5038).** PRD A6.2 requires ⌘K "New Reservation" → new-reservation dialog open (and "New Floor Plan" → new-floor-plan dialog open); the architecture designed only the walk-in intent. Architecture § Amendment 2026-09-04 now gives `/reservations?new=true` the walk-in contract's shape (`parseReservationsIntent` / `stripReservationsIntent` in `utils/timeline-intent.ts`; `ReservationsPage` derives on render, strips on close; focus per ux.md Decision (d)) and item 17 builds it. The "New Floor Plan" half stays routed behind #4751 per the PRD's open question (line 427). Original finding, kept for the record: `utils/timeline-intent.ts` is the sole URL contract; the traceability row for A6.2 lists `use-command-palette.ts`, `HomePage`, `WalkInDialog` only; ux.md Screen 10 covers "Walk-in guest" only. The "New Reservation" trigger is `ReservationsPage.tsx:89` (`showNewReservationDialog`) with `components/reservations/NewReservationDialog.tsx` — neither in-flight, so the PRD's drop clause ("cannot be wired without touching `components/floor-plan/**`") does not apply. **Not designed around:** no item covers it; A6.2 is covered for the walk-in half by #5024, #5031, #5034. The "New Floor Plan" half is routed behind #4751 per the PRD's open question (line 427). **Blocks:** Verify's A6.2 pass, not the start of Implement — route to Architect for a one-paragraph `/reservations?new=true` contract (or to the PRD owner to narrow A6.2) so an item can be appended before M3 closes.
- **G2 — the guard regex hits three files the architecture never names as adopters:** `pages/SetupHoursPage.tsx:44`, `components/timeline/StaffDepositSection.tsx:50`, `components/reservations/NewReservationDialog.tsx:140`. Resolved by the guard's own rule (adopt in #5029); logged as an assumption. Non-blocking.
- **G3 — guest-variant `network` copy** (architecture § surfaced): ux.md's copy table has no guest row, so the booking widget will read "Check the venue's connection" on a guest's phone. Non-blocking; one UX row closes it.
- **G4 — two viewport definitions coexist:** `useViewport` (767/1024) in `TimelinePage` and the private `useIsMobile` (768) in `TimelineGrid` until #4967 lands. Informational; a one-line follow-up once the grid file is free.

## Coverage

| Architecture component                                                                                                                                                                                                                                                | Item                                                                       |
| --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| `packages/rialto` `Drawer size="compact"` + changeset + story + test + regen                                                                                                                                                                                          | #5021                                                                      |
| `packages/rialto` `CommandPalette` `rankCommandMatch` + ranked groups + changeset + story + tests                                                                                                                                                                     | #5022                                                                      |
| `apps/hospitality/e2e/api-mocks.ts` (local day, table-status overlay, walk-in response)                                                                                                                                                                               | #5023                                                                      |
| `utils/local-clock.ts` (`localDateString`, `localHour`, `isLocalToday`)                                                                                                                                                                                               | #5024 (consumed by #5027, #5034, #5035)                                    |
| `utils/seated.ts` (`isSeated`, `seatedReservationIds`)                                                                                                                                                                                                                | #5024 (consumed by #5032, #5034, #5035)                                    |
| `utils/timeline-intent.ts` (`parseTimelineIntent`, `stripTimelineIntent`) + producers (palette, HomePage)                                                                                                                                                             | #5024 (consumed by #5030, #5034)                                           |
| `utils/timeline-intent.ts` (`parseReservationsIntent`, `stripReservationsIntent`) + palette producer + `ReservationsPage` consumer (architecture § Amendment 2026-09-04)                                                                                              | #5038                                                                      |
| `lib/describe-api-error.ts` + `ApiErrorDescription`                                                                                                                                                                                                                   | #5025                                                                      |
| `components/ErrorRetryBanner.tsx` (evolved: `title`, `details`, optional `onRetry`, 44 px)                                                                                                                                                                            | #5025                                                                      |
| `lib/describe-api-error.guard.test.ts` (+ pending list lifecycle)                                                                                                                                                                                                     | #5025; shrinks in #5027, #5028, #5029, #5030, #5031, #5034; final in #5036 |
| `hooks/useStatusMessage.ts` + `components/LiveStatus.tsx`                                                                                                                                                                                                             | #5026                                                                      |
| `hooks/useFocusAfter.ts` (+ `PageHeader` `h1` `tabIndex={-1}`)                                                                                                                                                                                                        | #5026                                                                      |
| `hooks/useViewport.ts`                                                                                                                                                                                                                                                | #5026                                                                      |
| `components/crm/guest-signals.ts` (extracted from `GuestCard`)                                                                                                                                                                                                        | #5027                                                                      |
| `pages/BriefingPage.tsx` (`segmentForHour`, `formatTime`, `BriefingCard` badges/tags, states) + `ROUTE_LABELS.briefing`                                                                                                                                               | #5027                                                                      |
| `components/KpiStat.tsx`                                                                                                                                                                                                                                              | #5028 (consumed by #5034)                                                  |
| `pages/ReservationsPage.tsx` + `components/dashboard/ReservationList.tsx`                                                                                                                                                                                             | #5028                                                                      |
| `pages/HomePage.tsx` (banner), `AdminPage`, `ProfilePage`, `GuestsPage`, `SettingsPage`, `FloorPlansPage`, `BookingWidgetDemoPage`, `SetupHoursPage`, `StaffDepositSection`, `NewReservationDialog`, `use-form-state.ts`, `useBookingFlow.ts`, `WaitlistJoinView.tsx` | #5029                                                                      |
| `pages/WaitlistPage.tsx` (sentences, focus, toast to `selected=`, titled banners)                                                                                                                                                                                     | #5030                                                                      |
| `WalkInDialog.tsx`, `CancelReservationDialog.tsx`, `EditReservationDrawer.tsx` (rethrow contract, banners, first focus, targets)                                                                                                                                      | #5031                                                                      |
| `components/timeline/ReservationDetails.tsx` (extracted) + `ReservationSheet.tsx`                                                                                                                                                                                     | #5032                                                                      |
| `components/timeline/TimelineSkeleton.tsx`                                                                                                                                                                                                                            | #5032                                                                      |
| `components/timeline/TimelineMobileView.tsx` ("Confirmed" chip, `seatedIds` mark)                                                                                                                                                                                     | #5032                                                                      |
| `components/timeline/TableStatusMenu.tsx` + `tableStatusMenuItems`                                                                                                                                                                                                    | #5033                                                                      |
| `components/timeline/TimelineEmptyNight.tsx`                                                                                                                                                                                                                          | #5033                                                                      |
| `components/timeline/useScrollToNow.ts`                                                                                                                                                                                                                               | #5033                                                                      |
| `hooks/useTimelineData.ts` (`refetch`, `createWalkIn: Promise<Reservation>`)                                                                                                                                                                                          | #5034                                                                      |
| `pages/TimelinePage.tsx` (orchestrator) + `TimelinePage.module.css`                                                                                                                                                                                                   | #5034 (grid-coupled remainder in #5035)                                    |
| `components/timeline/TimelineGrid.tsx` (edited at the § Sequencing anchors) + `ReservationBlock` `isSeated`                                                                                                                                                           | #5035                                                                      |
| E2E specs: `timeline`, `walkin`, `timeline-interaction`, `reservations`, `waitlist` (updated); `timeline-intent`, `briefing` (new)                                                                                                                                    | #5023, #5027, #5028, #5030, #5031, #5034, #5035                            |
| Generated: rialto exports/catalog/llms; hospitality + root llms; `USER-FLOWS.md`                                                                                                                                                                                      | #5021, #5022 (rialto); #5036 (final)                                       |

Every component in architecture § Components maps to at least one item; no item touches a file outside the architecture's change surface except the three G2 files (swept by the guard's rule) and the test/story/changeset files each item names.

| PRD success criterion                                                                                                                              | Item(s)                                                                                                                                                                             |
| -------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| A1.1 now-line at 20:00 and 14:00, date label unchanged                                                                                             | #5024, #5035 (harness: #5023)                                                                                                                                                       |
| A1.2 any local hour 11–23 on the local day; not on another date                                                                                    | #5024, #5035                                                                                                                                                                        |
| A2.1 17:30 / 18:30 / 21:00 → Early / Dinner / Late with printed times                                                                              | #5024 (`localHour`), #5027                                                                                                                                                          |
| A2.2 segment and printed time from one clock                                                                                                       | #5027                                                                                                                                                                               |
| A3.1 walk-in 500 leaves the grid, human error in the dialog, button resting ≤ 1 s                                                                  | #5025, #5031, #5034                                                                                                                                                                 |
| A3.2 seat / cancel / edit / table-status failures keep the grid                                                                                    | #5031 (cancel, edit), #5032 (seat), #5034, #5035 (table status)                                                                                                                     |
| A3.3 page-level error beside the grid, dismissible, cleared by the next attempt                                                                    | #5025, #5034, #5035                                                                                                                                                                 |
| A4.1 Seat Guest hidden for CONFIRMED + OCCUPIED, shown for PENDING                                                                                 | #5024, #5032, #5034                                                                                                                                                                 |
| A4.2 "Seated" only when the table is OCCUPIED; chip renamed                                                                                        | #5024, #5032 (sidebar, phone), #5035 (block); harness #5023                                                                                                                         |
| A4.3 no new status writes                                                                                                                          | #5023 (mock asserts shapes), #5034                                                                                                                                                  |
| A5.1 now-line inside the viewport on load and on Today                                                                                             | #5033, #5035                                                                                                                                                                        |
| A5.2 new reservation selected, in viewport, table named                                                                                            | #5026, #5034                                                                                                                                                                        |
| A5.3 ≥ 4 hours visible at 1024 × 768 with a selection                                                                                              | #5021, #5026, #5032, #5034                                                                                                                                                          |
| A6.1 `/timeline?walkin=true` opens the dialog; param gone after close                                                                              | #5024, #5034                                                                                                                                                                        |
| A6.2 ⌘K "Walk-in Guest" / Dashboard "New Walk-In" end with the dialog open and focused; ⌘K "New Reservation" / "New Floor Plan" open their dialogs | walk-in half: #5024, #5031, #5034; "New Reservation": #5038 (architecture § Amendment 2026-09-04, closes G1); "New Floor Plan": routed behind #4751 per the PRD's own open question |
| A6.3 "walk" ⏎ → walk-in; prefix > word-start > substring                                                                                           | #5022, #5024                                                                                                                                                                        |
| A6.4 dashboard path ≤ 2 activations                                                                                                                | #5024, #5034                                                                                                                                                                        |
| A7.1 empty night: message + two actions, rows visible                                                                                              | #5033, #5035                                                                                                                                                                        |
| A7.2 empty / failed / wrong date distinguishable                                                                                                   | #5033, #5034, #5035                                                                                                                                                                 |
| A8.1 status control names state and action                                                                                                         | #5033, #5035                                                                                                                                                                        |
| A8.2 one activation never commits                                                                                                                  | #5033, #5035                                                                                                                                                                        |
| A8.3 ≥ 44 × 44 at 1024 × 768                                                                                                                       | #5033, #5035                                                                                                                                                                        |
| A9.1 skeleton before 1 s, no counts                                                                                                                | #5028 (`KpiStat`), #5032 (skeleton), #5034                                                                                                                                          |
| A9.2 stats row does not move                                                                                                                       | #5034                                                                                                                                                                               |
| A10.1 VIP tag on the card                                                                                                                          | #5027                                                                                                                                                                               |
| A10.2 only the allergy tag is `error`                                                                                                              | #5027                                                                                                                                                                               |
| A10.3 "5:30 PM" everywhere                                                                                                                         | #5027, #5032                                                                                                                                                                        |
| A10.4 breadcrumb "Tonight's Service"                                                                                                               | #5027                                                                                                                                                                               |
| B1.1 no request line rendered; sentences on the named surfaces incl. the booking widget                                                            | #5025, #5027, #5028, #5029, #5030, #5031, #5034                                                                                                                                     |
| B1.2 `problemDetails.detail` shown; never `undefined` / status code                                                                                | #5025, #5029, #5031                                                                                                                                                                 |
| B1.3 `ReservationList` labels                                                                                                                      | #5028                                                                                                                                                                               |
| B1.4 regression guard                                                                                                                              | #5025, #5036                                                                                                                                                                        |
| B2.1 Reservations 500 → banner + Retry + dashes                                                                                                    | #5028                                                                                                                                                                               |
| B2.2 Retry → rows + KPIs without reload                                                                                                            | #5028                                                                                                                                                                               |
| B3.1 `role=status` sentence per mutation ≤ 1 s                                                                                                     | #5026, #5028, #5030, #5034, #5035                                                                                                                                                   |
| B3.2 focus never on `<body>` after success                                                                                                         | #5026, #5028, #5030, #5031, #5034, #5035                                                                                                                                            |
| B3.3 (optional) initial focus / Drawer restore                                                                                                     | #5031 (initial focus; Drawer restore is rialto's existing `useReturnFocus`, #4970 stays routed)                                                                                     |
| NF lint / typecheck / test green; TDD; affected E2E updated; none deleted                                                                          | every item; #5036 final gates                                                                                                                                                       |
| NF INCLUSIVE parity, reduced motion, 44 px, tokens, rialto only, gold for focus/active/in-flight                                                   | #5021, #5025, #5030, #5031, #5032, #5033, #5034, #5035                                                                                                                              |
| NF TONE — house voice, wording owned by UX                                                                                                         | #5025 (snapshot), #5026, #5027, #5030, #5031, #5033, #5034                                                                                                                          |
| NF rialto change hygiene — changeset, a11y matrix, story, build before consumers                                                                   | #5021, #5022                                                                                                                                                                        |
| NF repo — `regen --check`, prettier on the docs dir, `USER-FLOWS.md` lines, PR / CI Gate / Review / Ship                                           | #5036; PR, CI Gate, Review and Ship are stages, not items                                                                                                                           |

**Totals:** 42 PRD criteria — 42 covered by at least one item (A6.2 since 2026-09-04: walk-in half #5024 / #5031 / #5034, "New Reservation" half #5038; the "New Floor Plan" half is routed behind #4751 by the PRD's own open question, not a gap). 31 architecture components (30 + the amendment's intent pair) — 31 mapped to items. Milestones independently valuable: M1 ships two reusable rialto sizes/rankings; M2 fixes the Briefing clock, the mock, and gives the app one error voice; M3 makes four surfaces honest without touching the grid; M4 delivers the Timeline; M5 documents.

## Notes

- **Dispatch order:** numeric 1 → 16, serial in this worktree. Ready-now set (no dependencies): #5021, #5022, #5023, #5024, #5025, #5026, #5033. Every edge points forward; the longest chain is 1/4/5/7 → 12 → 14 → 15 → 16.
- **2026-09-04 — item 17 (#5038) appended:** the Architect amendment of the same date closed G1, so the ⌘K "New Reservation" intent is now an item. It sits at the end of M3 by dependency — #5024 (`utils/timeline-intent.ts` and the palette test file it edits), #5026 (`useFocusAfter`, the focusable `PageHeader` h1), #5028 (the `ReservationsPage.tsx` rewrite and `reservations.spec.ts`) — but is 17th in numbering; nothing was renumbered. Dispatch it after #5030 and before #5031 so #5036's final gates stay last; nothing in M4/M5 depends on it. Shared files it adds to the audit: `use-command-palette.ts` + test (#5024 then #5038), `ReservationsPage.tsx` + test (#5028 then #5038), `e2e/reservations.spec.ts` (#5028 then #5038), `utils/timeline-intent.ts` + test (#5024 then #5038).
- **Shared-file audit:** `TimelinePage.tsx` is edited by #5031 (bridge), #5032 (import swap), #5034 (rewrite), #5035 (grid-coupled remainder) — sequential, never parallel. `TimelineGrid.tsx` only in #5035. `lib/describe-api-error.guard.test.ts` shrinks across six items by design. `components/timeline/index.ts` gains exports in #5032 and #5033. `e2e/timeline.spec.ts` is touched by #5023, #5034, #5035; `walkin.spec.ts` by #5031, #5034; `timeline-interaction.spec.ts` by #5034, #5035.
- **Build order per item:** rialto items run `pnpm --dir packages/rialto build` before anything in `apps/hospitality` typechecks; hospitality items run `pnpm lint`, `pnpm typecheck`, `pnpm test` from inside `apps/hospitality` (vitest does not typecheck — run typecheck yourself); E2E items run `pnpm exec playwright test <spec>` from `apps/hospitality` after `pnpm build --filter @mattbutlerengineering/rialto`. Regen (`pnpm build --filter @mbe/cli... && pnpm regen && pnpm regen --check`) after each rialto item and in #5036; stage llms/catalog files by explicit path, never `git add -A`.
- **#4967 rebase note:** #5035 starts by re-measuring `gh pr view 4967 --json state,mergeStateStatus` and `git log origin/main -- apps/hospitality/src/components/timeline/TimelineGrid.tsx`. If merged, rebase the worktree branch first and re-apply the one overlapping line (`isSeated` on `<ReservationBlock>`) against #4967's `reservationStyleById` JSX; if still open, edit at the anchors and expect a one-line conflict on whichever lands second. Never silently overwrite.
- **Untouched paths (every item):** `pages/VenueOnboardingPage.tsx`, `components/venue-onboarding/**`, `components/floor-plan/**`, `hooks/useFloorPlans*`, `pages/FloorPlanEditorPage.tsx` (floor-plan run, #4751 open), `services/**`, Prisma schema/migrations, `packages/rialto` beyond `Drawer` and `CommandPalette`. The guard allowlists the in-flight files with that reason; once #4751 merges, a follow-up removes those entries and adopts the helper there (backlog seed, not an item).
- **Tracker mirror:** one issue per item (#5021 … #5036) and the parent #5037, labels `ready` + `feature` / `tracking` + `feature`; bodies generated from the same source as this file. Mirror is one-way (ADR-0026): this file's checkboxes are the state; Implement flips `ready` → `in-progress` → `has-pr` and commits `Closes #N` per item. `gh pr edit` is broken on this repo — use `gh api -X PATCH` if a PR body ever needs editing.
- **E2E workflow wiring:** none needed — `e2e.yml` runs bare `pnpm --dir apps/hospitality test:e2e`; `workflow-coverage.test.ts` proves auto-discovery. New specs: `timeline-intent.spec.ts` (#5034), `briefing.spec.ts` (#5027). Dispatch `e2e-selector-drift-reviewer` on every spec change.
- **Verify hand-off:** harness probe → item: P01 #5035; P02 #5034/#5035; P03 #5031/#5034; P04 #5034; P05 #5032/#5035; P06 #5034; P07/P14 #5022/#5024/#5034; P08 #5027; P09/P09b #5030; P10 #5035; P11 #5034; P12 #5032; P13 #5032/#5034; P15 #5033/#5035; P16 #5031/#5034; xcut A #5034; C/C2 #5025/#5031; D #5034; E #5031; G/H #5035; manager T3/R1 #5028; T11/R4 #5029.
- **Not this run** (routed, per routing.md): #4970 (`useFocusTrap` options), #4992 (error recovery on other pages beyond B1's rendering rule), the venue-zone clock (backlog seed (b)), `useDashboardStatsQuery.getTodayString` (seed (b)), the Rialto `Stat valueLabel` idea (solved app-side by `KpiStat`).
- **2026-09-04 — item 1 (#5021) RED lived at the typecheck gate, not in vitest:** rialto's vitest config does not process CSS, so CSS-module imports resolve to a key-echoing proxy and `variantClass(styles, "compact", "default")` already returned `"compact"` before any rule existed — the three new `Drawer.test.tsx` cases passed on first run. The failing-for-the-right-reason signal was `pnpm --dir packages/rialto typecheck`: 3 × `TS2322: Type '"compact"' is not assignable to type '"default" | "wide" | "full"'` at the three new `size="compact"` usages, green after the prop widened. The class assertions pin the DOM state the new selectors match (the seam rialto already uses — see `NeonSign.motion.test.tsx`'s "jsdom sees no keyframes" comment); the CSS rule text is verified by inspection per the Accept clause. Also: `pnpm regen --check` reports `rialto-registry` stale on an uncommitted-but-regenerated `registry.json` (non-llms families check `git diff` against HEAD, `scripts/regen.mjs:106-113`), so the "clean" reading is taken after the item's commit.
- **2026-09-04 — item 2 (#5022) ungrouped items in the ranked order:** the contract ranks _groups_ by the best match they contain and is silent on items without a `group`. Implemented uniformly: the ungrouped section is one more section — first on an empty query and on a rank tie (so the `groups` prop doc's "ungrouped items appear first" still holds), otherwise placed by its own best rank. Hospitality's palette groups every item, so nothing in the app depends on the choice. Gate note: rialto lint gains one warn-level `react-refresh/only-export-components` on the `rankCommandMatch` export from `CommandPalette.tsx` — architecture.md § Components places the function there by name, and rialto already carries 3 instances of the same warning; 0 errors.
- **2026-09-04 — item 3 (#5023) the "add a PATCH route" step was a conversion:** `api-mocks.ts` already carried a _stateless_ `/api/v1/tables/:id/status` route (always answered `tables.data[0]` as OCCUPIED, stored nothing) — the "none exists today" reading in the item text was stale. Converted in place to the per-context `tableUpdates` Map the item specifies; the tables list, the table-by-id route (previously returned `tables.data[0]` for any fixture id) and each reservation's embedded `table` now read through one `allTables()` view. The overlay only touches reservations whose table a PATCH changed — untouched fixture rows keep their shape (three of the four have no embedded `table`, and the mock invents none).
- **2026-09-04 — item 3 (#5023) the seven-spec Playwright run could not execute in this worktree:** `auth.setup.ts` calls `validateAuth0Config()` at import and no `E2E_AUTH0_*`/`E2E_AUTH_*` variable is set in the shell or in `apps/hospitality/.env` (key names checked, values never read), so even `playwright test --list` aborts. Verified instead by (1) a new vitest harness, `e2e/fixtures/api-mocks-table-status.test.ts`, that drives the real `mockApi` through a stand-in `Page` with Playwright's LIFO dispatch, `route.fallback()` chaining and 1.62 glob semantics (`?` is a literal — the file's "walk-in registered last" comment holds), covering every Accept clause the mock owns (PATCH → tables GET, PATCH → reservations GET overlay, walk-in date/`tableId`/`table`, 23:30-local re-dating under `vi.setSystemTime`, source scan for the UTC slice) — 5 RED (stateless PATCH, UTC date `2026-09-05`, fixture table on the walk-in) → 5 GREEN; (2) `playwright test --list --project=chromium --no-deps` over the seven specs collects 24 tests in 7 files including the new 23:30 case. The browser-level run of those seven specs is deferred to the run PR's advisory `Hospitality E2E` job (which has the secrets); Verify should read that job before trusting P01's mock half. `e2e-selector-drift-reviewer` dispatched on the diff.
- **2026-09-04 — item 3 (#5023) reviewer outcome:** `e2e-selector-drift-reviewer` found 0 strict-mode collisions, 0 volatile-text locators, no regression across the seven specs; 2 should-fix + 3 nits. Adopted: the harness pins `process.env.TZ = "America/Los_Angeles"` at module top and fixes the clock at the UTC instant `2026-09-05T06:30:00Z` (23:30 Pacific), so the local-vs-UTC split is exercised on UTC CI, not only on a Pacific laptop; `withTableStatus` embeds the store's full table view and leaves an unknown `tableId` untouched (no partial `table` for the client schema to reject); the walk-in route's "registered LAST" comment corrected (`me*`/`?*` follow it but cannot match the path). Declined, logged: re-dating to the request's `date=` param would make every date carry the fixtures and let an app-side regression to the UTC day pass unnoticed — the item's local-day semantics stay; consequence: the new `timeline.spec.ts` 23:30 case pins app↔mock agreement on a non-UTC runner (Matt's machine, the Verify harness) and is a no-op on UTC CI, where the vitest harness is the guard. The twin comment names `src/utils/local-clock.ts` one commit before item 4 creates it — required by the Accept clause.
- **2026-09-04 — item 4 (#5024) the `TZ=America/Los_Angeles` clause is pinned inside the test, not the runner:** `local-clock.test.ts` sets `process.env.TZ = "America/Los_Angeles"` at module top (Node 22 honours a runtime `TZ` change, and vitest's default `forks` pool with `isolate: true` gives every file its own process, so the pin cannot leak). The Accept fact — `localDateString(new Date("2026-09-04T06:30:00Z")) === "2026-09-03"`, `isLocalToday("2026-09-03", …)` true — therefore holds on a UTC CI runner too; verified by running the file under `TZ=UTC` from the shell (7/7). `seated.test.ts` gives `res_2` a stale embedded `table` claiming OCCUPIED while the authoritative list says AVAILABLE, so A4.2 ("ignores `reservation.table`") is an assertion, not an inference. `seated.ts` types its inputs as `Pick<Reservation, …>` / `Pick<Table, "id" | "status">` from `@mbe/types` so items 7/12/14/15 can pass either the API objects or the E2E fixture shapes; no rialto import in any of the three utils.
